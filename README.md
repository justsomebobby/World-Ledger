/*
WorldLedger Clean Core — Build 10.0A-R3D Effort Reward / XP Calibration
[Full original script content from the user's upload]
*/
(function () {
  'use strict';

  var WL = {
    PROJECT_NAME: 'World-Ledger',
    BUILD_NAME: 'WorldLedger Clean Core',
    BUILD_VERSION: 'clean-core-build-10-0a-r3d-effort-reward-xp-calibration',
    BUILD_STAGE: 'Build 10.0A-R3D — Effort Reward / XP Calibration',
    SCHEMA_VERSION: 29,
    SCHEMA_ID: 'worldledger.clean-core.schema.build10_0a',
    ROOT_KEY: 'worldledger',
    STATUS: 'BUILD_PHASE_ONLY_NOT_RELEASE'
  };

  // [The full original code the user uploaded earlier]

})();