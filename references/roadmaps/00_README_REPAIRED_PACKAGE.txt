# AIDRPG Repaired Design Bible Package

This repaired package contains the original ordered 32-chunk AIDRPG Design Bible plus an added binding repair layer:

- Chunk 33: Final Pre-Build Lockdown, Exactness Repair, Runtime Safety, and Implementation Contract
- Repair Summary and Authority Order
- Repaired Master Index

The original 32 chunks are preserved as the gameplay design bible.

Chunk 33 is the authoritative addendum that resolves the main planning risks found during review:

- excessive first-build scope
- unclear reducer ownership boundaries
- missing exact event proposal schema
- missing operation key rules
- missing context budget rules
- missing generated card write limits
- missing parser confidence and resolver rules
- missing strictness modes and Safe Mode
- missing cleanup policy
- missing MVP/build phase order
- underdefined multi-protagonist scope
- insufficient pre-build test gates

Use this package as the final repaired planning package before writing the Developer Spec.

Recommended next artifact:

AIDRPG Developer Spec v1 — exact state schema, reducer APIs, parser contracts, commands, context/card implementation, formulas, and tests.
