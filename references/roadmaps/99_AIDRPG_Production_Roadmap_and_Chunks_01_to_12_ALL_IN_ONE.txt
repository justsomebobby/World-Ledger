====================================================================================================
00_AIDRPG_Feature_Implementation_Wave_Map_v1.txt
====================================================================================================

# AIDRPG Feature Implementation Wave Map v1

## Purpose

This document is the bridge from WorldLedger V0.17 Foundation Candidate to the full AIDRPG build. It prevents the project from restarting the original Design Bible Chunks 1–32/33 as blind manual testing sessions.

The original Design Bible Chunks remain the full design source. This roadmap converts them into controlled production waves with owners, gates, tests, and forbidden shortcuts.

## Current authority stack

1. Final Build Control Map v1 — old failures become owner/rule/test/release gates.
2. WorldLedger V0.17 — current foundation candidate and hook/export baseline.
3. Build Chunks 1–17 — implementation safety overlay and Code Pass 1 rules.
4. Gap Chunks A–G — old failure protections.
5. Regression Matrix / QA files — pass/fail authority.
6. Formula Appendix — progression/stat math authority.
7. Developer Spec / MVP Spec — intended implementation behavior.
8. Design Bible Chunks 1–32/33 — full game destination.
9. World Bible / Story Cards JSON — lore/reference source, not direct gameplay truth.
10. Old scripts and tester feedback — evidence only, not blind code source.

## Remaining production count

From V0.17 onward:

```text
24 production chunks
+ 1 final packaging/release chunk
= 25 total remaining production chunks
```

This is not 32 manual play/build sessions. It is a finite feature-wave implementation plan.

## Wave overview

### Wave A — Foundation Freeze and Core Surface Polish

```text
01 Foundation Acceptance and Freeze
02 Command Surface and Player Setup Expansion
03 State Schema Deepening for Full System Readiness
```

### Wave B — Core Character and Moment-to-Moment Gameplay

```text
04 Player Identity, Body Logic, and Status Depth
05 Inventory, Equipment, Scene Items, and Custody Depth
06 Ability Candidate, Training, Use, Failure, and Stability Depth
07 Quest, Objective, Reward, and Promise/Payment Depth
08 Actor Presence, Social Memory, Witnessing, and Relationship Depth
09 Place, Travel, Time, Scene, and Deadline Depth
```

### Wave C — Progression, Form, Combat Baseline, and World Threat

```text
10 Formula Matrix and Progression Baseline
11 Stats, Derived Values, Conditions, and Invariant Checker
12 Combat Event Model and Damage/Healing Dispute Resolver
13 Body Form, Race Logic, Transformation Consequence, and Non-Humanoid Constraints
14 Threat Benchmark, Encounter Logic, and World Scaling Boundaries
```

### Wave D — World Bible, Persistence, Lore, Factions, and Regions

```text
15 World Bible Import/Reference Layer and Knowledge Gating
16 Region, Faction, Nation, and World Pressure Model
17 Creature Ecology, Body Logic, and Encounter Context Model
18 Divine Influence, Broken Systems, Anomalies, and Era Logic
19 World Persistence, Consequence Escalation, and Long-Run Memory
```

### Wave E — Cards, Context, Migration, and Long-Adventure Stability

```text
20 Context Budget Manager and Context Priority Allocator
21 Protected CardSync Live Write Unlock Candidate
22 Migration, Quarantine, Old Save, and Story Card Recovery Depth
```

### Wave F — Advanced Systems Activation by Mini-Spec

```text
23 Economy, Crafting, Investigation, Reputation, and Domain Mini-Spec Activation Pack
24 Advanced Combat, Party/Summons, Absorption/Evolution, Multi-Protagonist, and Warfare Activation Pack
```

### Wave G — Final Release Hardening

```text
25 Final Regression, Install Package, Documentation, and Release Candidate
```

## Process required for every production chunk

Every production chunk must state:

```text
1. Exact goal.
2. Modules touched.
3. State fields added/changed.
4. Parser/proposal/validator changes.
5. Reducer changes.
6. Command/context/card changes.
7. Operation keys.
8. ReleaseCheck gates.
9. Regression tests.
10. Forbidden shortcuts.
11. Implementation order.
12. Pass/warn/fail criteria.
13. Handoff to the next chunk.
```

No production chunk may end with “just play and see what breaks.” Manual playtesting verifies feel; regression tests protect old failures.

## Final doctrine

```text
No blind 32-session rebuild.
No manual rediscovery loop.
No feature activation without owner/rule/test/gate.
No old patch pasted blindly.
No PASS on uncertainty.
Build the full game through controlled production waves.
```


====================================================================================================
01_AIDRPG_Production_Chunk_01_V0_17_Foundation_Acceptance_and_Freeze.txt
====================================================================================================

# AIDRPG Production Chunk 01 — V0.17 Foundation Acceptance and Freeze

## Document status

Production sequence: 01 of 25 remaining production chunks.
Build target: WorldLedger V0.17 / AIDRPG Core MVP Foundation Candidate.
Purpose: Accept, verify, freeze, and protect the V0.17 foundation before feature production begins.

## Doctrine

```text
Freeze the foundation before adding features.
Do not build upward from an unstable base.
Do not treat V0.17 as the full game.
Do not restart the 1–32 design chunks.
Do not make the user rediscover old failures manually.
```

## What V0.17 is allowed to be

Allowed:

```text
foundation candidate
hook-orchestration lock
safe state/runtime/command baseline
parser/proposal/validator/reducer boundary baseline
support-layer non-canon baseline
ReleaseCheck/regression baseline
```

Not allowed:

```text
complete full RPG engine
all 1–32 design chunks implemented
advanced combat engine
economy/crafting/reputation/domain engine
generated card engine
manual mutation/admin command engine
```

## Exact acceptance identity

Preserve or clearly evolve from:

```text
PROJECT_NAME: WorldLedger
BUILD_NAME: WorldLedger Core MVP
BUILD_VERSION: 0.17.0-foundation-codepass1-candidate
BUILD_STAGE: Foundation Code Pass 1 / Foundation Candidate and Hook-Orchestration Lock
SCHEMA_ID: worldledger.schema.v1.locked
ROOT_KEY: worldledger
```

If patched, recommended visible title:

```text
WorldLedgerV0.17.1 — Core MVP Foundation Script / Accepted Foundation Freeze
```

Do not rename the root to `aidrpg`. Preserve the bridge:

```text
AIDRPG = broader RPG-engine project goal.
WorldLedger = script-engine implementation name for the AIDRPG Core MVP rebuild.
```

## Foundation acceptance metadata

Add or verify:

```text
foundationAcceptance: {
  accepted: boolean,
  acceptedVersion: string,
  acceptedFromVersion: string,
  acceptedStage: string,
  acceptedAtTurn: number,
  acceptedByCommand: string,
  requiredChecks: string[],
  lastResult: string,
  warnings: string[],
  blockers: string[],
  notes: string[]
}
```

Recommended location:

```text
state.worldledger.meta.foundationAcceptance
```

This is diagnostic metadata, not gameplay truth.

## Active foundation systems to verify

```text
Constants and version identity
Feature flags and safe defaults
Utility helpers
State schema defaults
StateManager
Runtime hook contract
AuditLogger
OperationLedger
CommandRouter
Command renderers
ReleaseCheck foundation and gates
Resolver and owner-specific sanitizers
EventScannerLite safe shell
ProposalBuilder safe shell
CanonValidator safe shell
ReducerRegistry
Core reducer shells / limited reducers
DirtyQueue
ContextBuilderLite read-only shell
CardApiAdapter shell
CardSyncLite dry-run/protected shell
Migration/Quarantine conservative shell
CleanupManagerLite safe caps
Hook orchestration
Export/platform binding
```

If any are shell-only, ReleaseCheck must report honestly.

## Deferred systems that must stay disabled

```text
full combat automation
advanced progression scaling
level 1–1000 full stat engine
full economy/shop/barter/debt
crafting/material recipes
investigation/case/evidence automation
domain/labor/workers/projects
reputation/faction/title automation
absorption/evolution/devour systems
advanced party/summon controls
multi-protagonist support
warfare/group combat/army systems
generated Story Cards
manual mutation commands
live CardSync writes by default
full old-save conversion from prose
full WorldStructure settlement/project extraction
```

## Required command surface

Verify foundation commands are registered, safe, and prefixed with `[AIDRPG]`, including:

```text
/help /version /sheet /status /inventory /abilities /quests /actors /location /time
/debuglast /cardaudit /contextaudit /stateaudit /ledger /dirty /migration /quarantine
/releasecheck /startupaudit /schema /schemaaudit /owners /commandformat /commands
/statemanager /integrity /repairlog /runtime /hookaudit /runtimecheck /failsoft
/audit /ledgercheck /commandrouter /commandcheck /parser /parsercheck /proposal
/proposalcheck /validator /validatorcheck /resolver /resolvercheck /controlmap
/salvage /borrowledger /supportdepth /supportschemas /supportcheck
/playerbreakercheck /regressioncheck /runfoundationtests /gatecheck /releaseprofile
/knownfailures /regressionmatrix /bc16check /foundation /codepass /foundationcheck
/hookcheck /packagingcheck
```

Aliases should route safely and not bypass command suppression.

## Hook acceptance

Verify:

```text
globalThis.WorldLedger
WorldLedger.onInput
WorldLedger.onContext
WorldLedger.onOutput
WorldLedger.processOutput
```

Input hook initializes state, routes commands, suppresses command output, and never treats input as completed success.

Context hook builds read-only context and never mutates gameplay state.

Output hook skips command output, skips duplicate output consequences, scans only valid story output, validates before reducer, routes only accepted results, marks dirty targets, runs support-layer caps, and returns original output text.

## PC01 ReleaseCheck gates

```text
PC01-G01 V0.17 build identity active or accepted freeze version active
PC01-G02 AIDRPG/WorldLedger bridge present
PC01-G03 root key remains stable
PC01-G04 required foundation commands registered
PC01-G05 hook exports present
PC01-G06 deferred feature flags disabled
PC01-G07 CardSync dry-run/generated disabled
PC01-G08 manual mutation commands disabled
PC01-G09 command output suppression active
PC01-G10 duplicate output replay protection active
PC01-G11 player-breaker minimum set registered and passing
PC01-G12 regressioncheck registered and passing
PC01-G13 support layers non-canon
PC01-G14 ReleaseCheck does not fake PASS
PC01-G15 foundation accepted marker present only after checks pass
```

## Required tests

```text
PC01-001 Build identity stable
PC01-002 Foundation commands registered
PC01-003 Hook exports exist
PC01-004 Deferred flags remain disabled
PC01-005 Command output non-canon
PC01-006 Story Card text non-canon
PC01-007 Context text non-canon
PC01-008 Player-breaker minimum set passes
PC01-009 Regression baseline passes
PC01-010 ReleaseCheck no fake pass
PC01-011 Duplicate output no repeat effects
PC01-012 Foundation accepted marker writes once
```

Minimum player-breaker set:

```text
denied key -> no item
failed grab -> no item
abstract key -> no item
dream Crown -> no item
quoted key handoff -> no item
output-only Moonblade -> no stable ability
failed Void Lantern Veil -> no stable ability
visible chapel -> no scene overwrite
road then enter Drunken Huntsman -> inn, not road
quoted twenty years pass -> no aging
death rumor -> no death
reward promise -> no currency
reward payment repeated -> paid once
command/context/card text -> no canon mutation
```

## Beginner implementation order

```text
1. Copy the current V0.17 script into a working file.
2. Fix title/version/freeze metadata only if needed.
3. Add the acceptance metadata object.
4. Add acceptance status to /foundationcheck or equivalent.
5. Extend ReleaseCheck with PC01 gates.
6. Run foundation command tests.
7. Run non-canon source tests.
8. Mark accepted only if clean.
9. Save as WorldLedgerV0.17.1_FOUNDATION_ACCEPTED_FREEZE.txt.
10. Move to Production Chunk 02.
```

## Pass / warn / fail

PASS if hooks, commands, ReleaseCheck, foundationcheck, hookcheck, playerbreakercheck, regressioncheck, supportcheck, and packagingcheck pass with no unsafe active behavior.

WARN only for honest deferred systems or shell limitations.

FAIL if script does not load, exports are missing, commands crash, command/context/card text mutates state, duplicate output repeats effects, manual mutation is enabled, generated cards are enabled, live card writes are enabled by default, or ReleaseCheck fakes PASS.

## Final doctrine

```text
Accept the baseline.
Freeze the foundation.
Preserve the safety gates.
Keep deferred systems disabled.
Prove commands, hooks, parser boundaries, support layers, and duplicate locks are safe.
Then build upward.
```


====================================================================================================
02_AIDRPG_Production_Chunk_02_Command_Surface_and_Player_Setup_Expansion.txt
====================================================================================================

# AIDRPG Production Chunk 02 — Command Surface and Player Setup Expansion

## Document status

Production sequence: 02 of 25.
Build target: WorldLedger V0.17.1 foundation accepted freeze.
Purpose: Improve command readability, setup inspection, player identity display, and disabled-system messaging without unsafe mutation commands.

## Doctrine

```text
Commands explain truth.
Commands do not create truth.
Setup inspects and confirms.
Setup does not overwrite locked identity.
Displays must separate confirmed, candidate, pending, visible, promised, and deferred states.
```

## Why this comes before gameplay expansion

If commands show visible items as owned, candidates as stable, promised rewards as paid, or travel intent as arrival, later testing becomes unreliable. This chunk makes command outputs honest before deeper systems build on them.

## Allowed changes

```text
CommandRouter
Command aliases
Command renderers
Help text
Version/build status display
Setup/startup inspection display
Sheet/status/inventory/abilities/quests/actors/location/time displays
State audit display
ReleaseCheck command honesty gates
Disabled command messages
```

Forbidden:

```text
manual mutation commands
admin mutation commands
feature activation commands
generated card commands
live card write commands
unsafe character creation mutation flow
```

## Setup philosophy

Allowed read-only setup commands:

```text
/startup /startupaudit /setup /setupcheck /sheet /version /foundationcheck
```

They may show what is known, unknown, locked, confirmed, or pending. They must not overwrite protected identity.

Controlled setup confirmation may only accept structured startup facts before gameplay lock. Post-lock identity changes require a future manual override spec.

Forbidden examples:

```text
/playername Bob mutates locked identity casually
/race Dragon instantly changes body logic
/setform Wolf overwrites true identity
/addlineage Angel-Demon grants powers
```

## Player identity display requirements

`/sheet` must separate:

```text
Confirmed identity
Current form
Body form / body type
Race/species/lineage
Origin / outsider status
Disguise / illusion / temporary presentation
Candidate or pending traits
Unknown fields
Deferred systems
```

True identity is not current form. Current form is not disguise. Disguise is not body truth. Candidate lineage is not confirmed lineage.

## Origin and world knowledge display

Do not assume human, native, chosen, central, powerful, or omniscient. Display:

```text
Origin: unknown / outsider / native / world-emerged / scenario-defined
World knowledge: limited unless discovered
Narrative role: not automatically center of reality
Integration: grows through action, recognition, and consequence
```

## Command output standard

Every command response must:

```text
start with [AIDRPG]
be clearly non-canon
avoid prose that looks like story output
avoid wording that EventScannerLite might treat as a completed event
avoid raw JSON dumps unless diagnostic mode asks
be capped for AI Dungeon context limits
```

Diagnostics should include: `No gameplay state was changed.`

## Shared command formatter

Add/verify helper responsibilities:

```text
prefix command output
standardize section headings
standardize empty-state lines
standardize confirmed/candidate/pending/deferred labels
standardize no-mutation footer
escape or avoid story-like lines
cap command length
```

## Command requirements

`/help` shows active commands, diagnostics, build checks, disabled categories, current stage, and where to start.

`/version` shows WorldLedger engine, AIDRPG project bridge, version, schema, stage, root key, foundation accepted/frozen status, release profile, safe/deferred flag summary.

`/sheet` separates confirmed identity, origin, current form, body truth, race/species, lineage, disguise/illusion, candidate/pending traits, protected fields, unknown fields.

`/status` separates resources, fatigue/exhaustion, active conditions, injuries, recovery/resolved conditions, death boundary, warnings.

`/inventory` separates owned/carried, equipped, stored, visible nearby, available loot, NPC custody, lost/consumed/destroyed history, currency, warnings.

`/abilities` separates stable, unstable, candidate, hints, failed/collapsed attempts, sealed/unavailable, recent evidence, warnings.

`/quests` separates active, offered/rumored, objectives, ready-to-turn-in, completed, failed/abandoned, promised rewards, paid rewards.

`/actors` separates present important actors, scene-only actors, persistent actors, absent/reference-only actors, witness memories, relationship notes.

`/location` separates current location, current scene, travel intent, nearby/visible references, route/interstitial references, known places.

`/time` displays current time text, turn/sequence if used, last accepted time advance, deadlines, pending/nonliteral claims, warnings. It never advances time.

## Disabled command messages

Forbidden commands must return safe disabled messages and no mutation:

```text
/additem /setlevel /payreward /completequest /giveability /teleport
/livecards /generatedcards /craft /buy /sell /reputation /domain
/absorb /evolve /partycontrol /warfare
```

Each message should explain the system is disabled, why, planned production chunk, and that no gameplay state changed.

## PC02 ReleaseCheck gates

```text
PC02-G01 all listed active commands route through CommandRouter
PC02-G02 all command output starts with [AIDRPG]
PC02-G03 commands are inspect-only
PC02-G04 disabled command messages exist for forbidden command families
PC02-G05 /sheet separates identity/current form/disguise/candidate traits
PC02-G06 /status separates fatigued/exhausted/injured/death boundary
PC02-G07 /inventory separates owned/equipped/visible/available/custody/lost/consumed/currency
PC02-G08 /abilities separates stable/unstable/candidate/hint/failed/sealed
PC02-G09 /quests separates offered/active/objectives/promised/ready/paid/completed
PC02-G10 /actors separates present/scene-only/persistent/absent/reference-only/memory
PC02-G11 /location separates current/travel intent/nearby/visible/route
PC02-G12 /time does not advance time and shows pending/nonliteral claims safely
PC02-G13 setup/startup display does not mutate locked identity
PC02-G14 aliases do not bypass command suppression
PC02-G15 command output parser-safety cues present
```

## Required tests

```text
PC02-001 Help is honest
PC02-002 Version bridge clear
PC02-003 Sheet separates identity and form
PC02-004 Unknown race is not assumed human
PC02-005 Status separates fatigue and death boundary
PC02-006 Inventory separates visible and owned
PC02-007 Abilities separate candidate and stable
PC02-008 Quests separate promise and payment
PC02-009 Actors separate reference-only
PC02-010 Location separates travel intent
PC02-011 Time command does not advance time
PC02-012 Disabled mutation command safe
PC02-013 Alias safety
PC02-014 Command output cannot become canon
PC02-015 Setup display does not unlock identity
```

## Implementation order

```text
1. Copy accepted foundation baseline to V0.17.2 command-surface working file.
2. Add shared command formatting helper.
3. Update /help and /version.
4. Update /sheet, /status, /inventory, /abilities, /quests, /actors, /location, /time.
5. Add setup display/checklist.
6. Add disabled command messages.
7. Add PC02 ReleaseCheck gates.
8. Add PC02 regression tests.
9. Feed command outputs back through Output hook to prove non-canon behavior.
10. Save as WorldLedgerV0.17.2_COMMAND_SURFACE_ACCEPTED.txt.
```

## Pass / warn / fail

PASS if all active commands route safely, all command output is prefixed, commands are inspect-only, core displays are honest, setup is non-mutating, disabled commands are safe, aliases do not bypass suppression, and PC02 tests pass.

WARN if setupcheck is folded into sheet/startupaudit, advanced display sections are empty because deferred, or CardSync controls are display-only.

FAIL if any command mutates gameplay, command output lacks suppression cues, manual mutation is active, candidate ability displays as stable, visible item displays as owned, promised reward displays as paid, travel intent displays as current location, unknown race defaults to human, setup overwrites locked identity, or command output becomes canon.

## Final doctrine

```text
If it is confirmed, label it confirmed.
If it is candidate, label it candidate.
If it is pending, label it pending.
If it is visible, do not show it as owned.
If it is promised, do not show it as paid.
If it is intended, do not show it as arrived.
If it is unknown, say unknown.
If it is disabled, say disabled.
Commands explain the world; they do not change it.
```


====================================================================================================
03_AIDRPG_Production_Chunk_03_State_Schema_Deepening_for_Full_System_Readiness.txt
====================================================================================================

# AIDRPG Production Chunk 03 — State Schema Deepening for Full System Readiness

## Document status

Production sequence: 03 of 25.
Build target: WorldLedger V0.17.2 command-surface accepted build.
Purpose: Deepen state schema, migration metadata, invariant scaffolding, disabled placeholders, operation ledgers, and quarantine categories before live gameplay expansion.

## Doctrine

```text
Prepare state before features.
Add structure before automation.
Add placeholders without enabling unsafe systems.
Add migration metadata before migration complexity.
Add invariant shells before high-risk reducers.
Keep old prose quarantined, not imported.
```

## Allowed changes

```text
State schema factories
StateManager default creation and missing-field repair
schema version metadata
bucket version metadata
migration metadata
quarantine categories
operation ledger bucket structure
feature flag placeholders
invariant checker shell
state audit display
ReleaseCheck schema gates
cleanup cap metadata
```

Forbidden:

```text
active economy/crafting/combat/reputation/domain/absorption/generated cards/manual mutation
old card prose gameplay import
```

## Canonical root rule

There must be one canonical root:

```text
state.worldledger
```

Do not create competing active roots such as `state.aidrpg`, `state.tasis`, `state.tas`, `state.innerSelf`, or `state.autoCards`. Old roots may be detected and quarantined as legacy evidence.

## Required top-level buckets

```text
meta features runtime startup player body progression inventory abilities quests actors places time cards operationLedger audits migration quarantine dirty cleanup schema integrity
```

StateManager adds missing safe defaults without deleting valid existing data.

## Schema metadata bucket

Recommended shape:

```text
schema: {
  schemaId: 'worldledger.schema.v1.locked',
  schemaVersion: '1.0.0',
  stateVersion: '0.17.3-schema-readiness',
  createdBy: 'WorldLedger',
  lastCheckedTurn: 0,
  lastMigrationTurn: 0,
  bucketVersions: {},
  requiredBuckets: [],
  optionalDisabledBuckets: [],
  knownLegacyRoots: [],
  warnings: [],
  notes: []
}
```

## Bucket version metadata

Track versions for all major buckets:

```text
meta features runtime startup player body progression inventory abilities quests actors places time cards operationLedger audits migration quarantine dirty cleanup schema integrity
```

Use simple values such as `1` or `v1`.

## Deferred feature placeholders

Feature flags must distinguish active foundation systems, disabled future systems, shell diagnostics, and blocked unsafe systems. Required disabled flags:

```text
economyEnabled craftingEnabled investigationEnabled reputationEnabled domainEnabled absorptionEnabled advancedCombatEnabled warfareEnabled multiProtagonistEnabled partyAdvancedEnabled generatedCardsEnabled protectedCardWritesEnabled manualMutationCommandsEnabled = false
```

Add display metadata with planned chunk/reason, for example economy planned for 23A, crafting 23B, investigation 23C, reputation 23D, domain 23E, advanced combat 24A, party 24B, absorption 24C, multi-protagonist 24D, warfare 24E.

## Bucket readiness requirements

### Startup

```text
locked, firstGameplayInputSeen, setupSourceStatus, setupSourceKey, identityLockedAtTurn, startupOperationKeys, importedFields, unknownFields, warnings, notes
```

Post-lock setup cannot overwrite protected identity.

### Player

```text
id, displayName, aliases, identityStatus, origin, worldKnowledgeStatus, trueRaceSpecies, lineage, currentForm, bodyType, disguise, illusion, presentation, protectedFields, candidateTraits, pendingTraits, warnings, notes
```

Unknown race does not mean human. Current form does not overwrite true race/species.

### Body

```text
resources.hp/mp/ep, fatigue, conditions, injuries, deathBoundary, bodyTraits, formConstraints, warnings, notes
```

Structural clamp is not healing.

### Progression

```text
level, currentXP, totalXP, xpToNext, stats, baseStats, modifiers, derivedSnapshot, statXP, histories, formulaProfile, formulasEnabled, warnings, notes
```

Formulas remain disabled/limited until formula chunks.

### Inventory

```text
items, equipped, stored, sceneItems, custody, currency, itemHistory, warnings, notes
```

Future ItemRecord shape includes id, key, displayName, quantity, category, tags, stackable, durability, accessState, owner/custody/source metadata.

### Abilities

```text
records, stable, unstable, candidates, hints, failed, sealed, unavailable, evidenceLog, warnings, notes
```

Candidate is not stable.

### Quests

```text
records, active, offered, rumored, readyToTurnIn, completed, failed, abandoned, rewardLedger, warnings, notes
```

Promised reward is separate from paid reward.

### Actors

```text
records, sceneActors, present, absent, referenceOnly, memories, relationshipNotes, warnings, notes
```

Reference-only actor is not present.

### Places

```text
currentLocation, currentScene, records, travelIntent, visibleReferences, nearbyReferences, routeReferences, sceneHistory, warnings, notes
```

Travel intent is separate from current location.

### Time

```text
text, turn, sequence, lastAdvance, acceptedAdvances, pendingClaims, deadlines, agePolicy, warnings, notes
```

Time advance and age mutation are separate.

### Cards

```text
protectedCards, registry, dirtyCards, tombstones, duplicateWarnings, apiStatus, syncMode, lastDryRun, warnings, notes
```

Generated cards and protected live writes remain disabled.

## OperationLedger deepening

Required buckets:

```text
permanentKeys recentKeys rewardKeys cardKeys sourceUseKeys startupOperationKeys migrationOperationKeys itemKeys abilityKeys timeKeys questKeys actorKeys placeKeys progressionKeys combatKeys craftingKeys economyKeys reputationKeys
```

Disabled placeholder key buckets may exist but disabled systems must not write to them.

Durable keys must not be capped like recent noise.

## Migration metadata

```text
migration: {
  currentSchema,
  detectedSchema,
  lastMigrationStatus,
  migrationsApplied: [],
  repairLog: [],
  legacyRootsDetected: [],
  legacyPayloadsQuarantined: [],
  futureSchemaWarnings: [],
  failedMigrations: [],
  warnings: [],
  notes: []
}
```

Migrations must be deterministic and idempotent. Write new version only after success. Failed migration preserves old payload in quarantine/backup.

## Quarantine categories

```text
legacy_root legacy_card_prose malformed_bucket invalid_resource invalid_item invalid_ability invalid_quest invalid_actor invalid_place invalid_time invalid_progression unsafe_command_text unsafe_context_text unsafe_card_text future_schema manual_edit_conflict migration_failure
```

QuarantineRecord fields:

```text
quarantineId, category, source, sourceKey, originalPreview, reason, severity, suggestedAction, status, createdTurn, notes
```

Quarantined records are inactive.

## IntegrityChecker shell

Should check required buckets, safe feature flags, numeric resources, current <= max, nonnegative item quantities, duplicate stable ability keys, duplicate active quests, deathBoundary values, generated cards disabled, manual mutation disabled, durable ledger buckets, and inactive quarantine.

It may report PASS/WARN/FAIL/QUARANTINE. It may not award, heal, pay, or repair gameplay effects.

## PC03 ReleaseCheck gates

```text
PC03-G01 one canonical state root exists
PC03-G02 required top-level buckets exist
PC03-G03 schema metadata bucket exists
PC03-G04 bucket version metadata exists
PC03-G05 deferred system placeholders are disabled
PC03-G06 startup lock metadata exists
PC03-G07 player identity/form/disguise fields prepared
PC03-G08 body resource/status/injury fields prepared
PC03-G09 progression formula fields prepared but not over-activated
PC03-G10 inventory item/access/custody fields prepared
PC03-G11 abilities status/evidence fields prepared
PC03-G12 quest objective/reward fields prepared
PC03-G13 actor presence/reference/memory fields prepared
PC03-G14 place current/travel/reference fields prepared
PC03-G15 time/deadline/age policy fields prepared
PC03-G16 card mirror registry fields prepared and generated cards disabled
PC03-G17 operation ledger durable buckets exist
PC03-G18 migration metadata exists
PC03-G19 quarantine categories and records exist
PC03-G20 integrity checker shell exists
PC03-G21 cleanup protected paths exist
PC03-G22 old prose cannot import as truth
```

## Required tests

```text
PC03-001 Single root only
PC03-002 Missing bucket repair preserves data
PC03-003 Schema metadata exists
PC03-004 Deferred placeholders disabled
PC03-005 Startup lock metadata preserved
PC03-006 Player unknown fields stay unknown
PC03-007 Body resource structural clamp does not heal
PC03-008 Negative item quantity quarantined
PC03-009 Duplicate stable ability detected
PC03-010 Quest reward fields separate promise/payment
PC03-011 Actor reference-only structure exists
PC03-012 Place travel intent separate from current location
PC03-013 Time age policy cautious
PC03-014 Card mirror flags safe
PC03-015 Durable ledger buckets protected
PC03-016 Old card prose quarantined
PC03-017 Future schema warning safe
PC03-018 Integrity command non-mutating
PC03-019 Quarantine inactive display
PC03-020 ReleaseCheck PC03 gates
```

## Implementation order

```text
1. Copy V0.17.2 accepted command-surface build.
2. Add schema metadata defaults.
3. Add bucket version map.
4. Expand StateManager required bucket repair.
5. Add deferred system metadata.
6. Deepen startup/player/body/progression/inventory/abilities/quests/actors/places/time/cards/ledger/migration/quarantine/cleanup/integrity shapes.
7. Add quarantine categories.
8. Add integrity checker shell.
9. Update schema/integrity commands.
10. Add PC03 gates and tests.
11. Run foundationcheck, releasecheck, schemaaudit, integrity, regressioncheck, supportcheck, playerbreakercheck.
12. Save as WorldLedgerV0.17.3_SCHEMA_READINESS_ACCEPTED.txt.
```

## Final doctrine

```text
State shape comes before system behavior.
Migration metadata comes before migration complexity.
Quarantine comes before salvage.
Invariant checks come before high-risk reducers.
Disabled placeholders come before feature activation.
Cleanup protection comes before long-run play.
```


====================================================================================================
04_AIDRPG_Production_Chunk_04_Player_Identity_Body_Logic_and_Status_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 04 — Player Identity, Body Logic, and Status Depth

## Document status

Production sequence: 04 of 25.
Build target: WorldLedger V0.17.3 schema-readiness accepted build.
Purpose: Deepen player identity, body truth, current form, conditions, injuries, resources, fatigue/exhaustion, recovery, and death-boundary behavior without identity drift or false body canon.

## Doctrine

```text
True identity is not current form.
Current form is not disguise.
Disguise is not body truth.
Rumor is not body truth.
Dream is not body truth.
Quote is not body truth.
Fatigued is not exhausted.
Unconscious is not dead.
Dying is not dead.
Recovery requires a recovery event.
Cleanup is not healing.
```

## Allowed changes

```text
Player state helpers
Body state helpers
BodyReducer
Body CanonValidator rules
Body ProposalBuilder event types
Body EventScannerLite hints
/status and /sheet renderers
ContextBuilder player/body packets
InvariantChecker body/player checks
ReleaseCheck body gates
Regression/player-breaker body tests
```

Forbidden:

```text
full combat engine
full transformation/evolution engine
absorption/devour
race/lineage power automation
advanced progression formulas
manual /setrace /setform /heal /kill
reputation/social automation from body form
full non-humanoid equipment rules
```

## Identity model requirements

Player identity must distinguish:

```text
true identity
confirmed name
aliases
origin
world knowledge status
true race/species
lineage
current form
body type
body traits
disguise
illusion
presentation
candidate traits
pending traits
rumored traits
unknown traits
```

True identity cannot be overwritten by current form, disguise, illusion, rumor, NPC quote, dream, temporary wording unless validated, command display, card prose, or context prose.

Current form is the active body presentation or validated body state. It may affect future constraints, but this chunk only prepares/display constraints unless transformation is explicitly validated.

Disguise/illusion are presentation states unless validated as actual body/form changes.

Candidate and pending traits remain unconfirmed.

## Origin and world knowledge model

Fields or equivalents:

```text
origin.status
origin.type
origin.source
worldKnowledgeStatus
worldIntegrationStatus
centralityAssumption
```

Recommended values:

```text
origin.type = unknown | native | outsider | world_emerged | scenario_defined
worldKnowledgeStatus = unknown | limited | local | trained | expert | hidden_lore_unlocked
worldIntegrationStatus = unknown | outsider | tolerated | recognized | entangled | influential
centralityAssumption = none | local_only | scenario_defined
```

Unknown origin does not mean native. Unknown race does not mean human. Limited world knowledge is default unless setup proves otherwise.

## Body resource model

Resources:

```text
hp mp ep
```

Each supports current, max, known, source, lastChangedTurn, notes. Unknown numeric resources must display unknown rather than fake values like HP 100/100.

Rules:

```text
current cannot exceed max when both known
current cannot be below 0 when known
max cannot be below 1 when known
structural clamp is not healing
recovery requires accepted recovery/rest/healing event
```

## Fatigue/exhaustion model

States:

```text
rested winded fatigued weary exhausted collapsed unknown
```

Severities:

```text
none minor moderate major critical unknown
```

Fatigued/worn out does not become exhausted unless strong wording confirms it. Exhausted/can barely stand/drained completely may become exhausted if validated. Collapsed/unable to stand may become collapsed/critical if validated.

## Condition model

Condition examples:

```text
bleeding burned poisoned sick stunned blinded deafened frozen chilled cursed infected exhausted fatigued unconscious restrained
```

ConditionRecord:

```text
id, key, displayName, family, severity, status, sourceOperationKey, sourceEventId, createdTurn, updatedTurn, expiresTurn, location, cause, evidence, notes
```

Statuses:

```text
active worsening improving resolved suppressed pending rejected
```

Condition gain requires direct validated body/status event. Resolution requires recovery/resolution event. Cleanup cannot resolve active conditions.

## Injury model

Injury examples:

```text
wolf bite on left arm
burned palm
sprained ankle
broken rib
cut cheek
arrow wound in shoulder
```

InjuryRecord:

```text
id, key, displayName, bodyPart, severity, status, bleeding, infectionRisk, sourceOperationKey, sourceEventId, createdTurn, updatedTurn, healedTurn, cause, evidence, notes
```

Threats, attempts, metaphors, dreams, quotes, and card/context/command text do not create injuries.

## Death boundary model

Allowed:

```text
alive unconscious dying dead captured unknown
```

Rules:

```text
alive is not perfect health
unconscious is not dead
dying is not dead
dead requires direct validated death outcome
captured is not dead
unknown means unknown, not dead
```

Reject death from rumor, quote, dream, metaphor, threat, insult, prophecy, card, context, command, or story title text.

## Body trait and form constraint shell

May store/display traits and constraints such as claws, wings, tail, fur, scales, no human hands, cannot speak normally, quadruped movement, large body constraints. Deep mechanics are deferred. Illusions/disguises do not create physical constraints. Candidate traits do not create confirmed constraints.

## CanonValidator body expansion

Accept direct physical/body outcomes:

```text
Your arm bleeds from the wolf bite.
Your palm is burned and blistered.
Your legs shake and you are exhausted.
The poison fades from your veins.
Your bleeding stops.
You collapse unconscious.
```

Reject unsafe/nonliteral outcomes:

```text
Mara says "you are poisoned"
People say you died
You feel dead tired
In a dream, you burn alive
The prophecy says you will die
The card says you are wounded
[AIDRPG] Status: Bleeding
```

Pending uncertain wording:

```text
You feel strange.
A chill passes through you.
You might be poisoned.
The wound could be infected.
Your form seems different.
```

## Proposal event types

```text
body.status_gain_candidate
body.status_loss_candidate
body.injury_candidate
body.resource_change_candidate
body.resource_state_candidate
body.recovery_candidate
body.death_boundary_candidate
body.form_change_candidate
body.trait_candidate
identity.trait_candidate
identity.form_candidate
identity.disguise_candidate
identity.illusion_candidate
```

## BodyReducer behavior

Status gain creates/updates ConditionRecord, sets active/severity, records operation key, marks body_status/context dirty, audits compactly.

Injury gain creates/updates InjuryRecord, body part/severity/bleeding if known, marks body_status/context dirty.

Recovery sets condition/injury improving or resolved and does not delete history immediately.

Resource change applies validated delta/state, clamps safely, records source key.

Death boundary change requires direct validated outcome, durable key, dirty targets, high-severity audit. Unsafe flags cause reject/no-op.

## Operation keys

```text
body.status.add.<statusKey>.<clauseHash>
body.status.resolve.<statusKey>.<clauseHash>
body.injury.add.<injuryKey>.<clauseHash>
body.injury.resolve.<injuryKey>.<clauseHash>
body.resource.<resourceKey>.<deltaOrState>.<clauseHash>
body.deathboundary.<newStatus>.<clauseHash>
body.form.<formKey>.<sourceKey>
identity.trait.candidate.<traitKey>.<sourceKey>
identity.presentation.<presentationKey>.<sourceKey>
```

## Dirty targets

```text
body condition/injury/resource/fatigue -> body_status, context
player identity/form/disguise/trait -> player_sheet, context
critical death boundary -> body_status, player_sheet, context, audit
```

## Command/context updates

`/sheet` displays confirmed identity, origin/world knowledge, true race/species, lineage, current form, body type, disguise/illusion/presentation, candidate/pending traits, protected fields, unknown fields.

`/status` displays resources, fatigue/exhaustion, active conditions, active injuries, recovery/resolved recent statuses, death boundary, body traits/constraints, warnings.

ContextBuilder includes concise player/body packets and clearly labels candidate/pending truth.

## PC04 ReleaseCheck gates

```text
PC04-G01 player identity/form/disguise fields separated
PC04-G02 unknown race/species not defaulted to human
PC04-G03 world knowledge starts limited/unknown unless confirmed
PC04-G04 body resource model supports known/unknown values
PC04-G05 fatigue and exhaustion are separated
PC04-G06 condition record schema exists
PC04-G07 injury record schema exists
PC04-G08 death boundary allowed values enforced
PC04-G09 body CanonValidator rejects rumor/quote/dream/metaphor death/status
PC04-G10 BodyReducer rejects unsafe routed body/death events
PC04-G11 recovery requires accepted recovery event
PC04-G12 cleanup cannot heal/remove active body state
PC04-G13 body/player dirty targets created
PC04-G14 /sheet displays confirmed/candidate/unknown fields honestly
PC04-G15 /status displays body states honestly
PC04-G16 body context packet labels candidate/pending truth correctly
PC04-G17 operation keys exist for repeat-sensitive body effects
PC04-G18 player-breaker body/death tests registered
```

## Required tests

```text
PC04-001 Unknown race not assumed human
PC04-002 Current form does not overwrite true race
PC04-003 Disguise does not overwrite body truth
PC04-004 Candidate lineage stays candidate
PC04-005 Fatigued not exhausted
PC04-006 Exhausted explicit
PC04-007 Injury created from direct physical outcome
PC04-008 Threat does not injure
PC04-009 Recovery resolves condition
PC04-010 Cleanup does not heal
PC04-011 Rumor death rejected
PC04-012 Quote death rejected
PC04-013 Dream death rejected
PC04-014 Dead tired metaphor rejected
PC04-015 Unconscious distinct from dead
PC04-016 Dying distinct from dead
PC04-017 Direct death requires explicit policy
PC04-018 Resource clamp structural only
PC04-019 Command status non-canon
PC04-020 Card body prose non-canon
PC04-021 Context body prose non-canon
PC04-022 Body duplicate key skip
PC04-023 Body dirty targets
PC04-024 Status command display clarity
```

## Implementation order

```text
1. Copy V0.17.3 schema-readiness accepted build.
2. Add/verify player and body helper functions.
3. Update Body CanonValidator rules.
4. Update ProposalBuilder body event support.
5. Update BodyReducer.
6. Update /sheet and /status.
7. Update ContextBuilder body/player packets.
8. Update IntegrityChecker body/player checks.
9. Add PC04 ReleaseCheck gates.
10. Add PC04 tests.
11. Run foundationcheck, releasecheck, integrity, playerbreakercheck, regressioncheck, status, sheet.
12. Save as WorldLedgerV0.17.4_BODY_IDENTITY_ACCEPTED.txt.
```

## Pass / warn / fail

PASS if identity/form/disguise/candidates separate, unknown race is not assumed, resources support known/unknown, fatigue/exhaustion separate, conditions/injuries persist, recovery requires accepted event, death rejects unsafe sources, BodyReducer rejects unsafe routed events, displays are honest, context is concise/labeled, duplicate keys work, tests pass.

WARN if numeric HP/MP/EP unknown, transformation consequences deferred, race/lineage powers deferred, form/equipment constraints display-only, death automation safety-limited.

FAIL if current form overwrites true race, candidate trait displays confirmed, fatigue becomes exhaustion too easily, cleanup heals active conditions, rumor/quote/dream sets death, command/context/card text creates body state, duplicate injury repeats, or BodyReducer trusts unsafe data.

## Final doctrine

```text
The player is a protected identity with a body state.
The body can be injured, tired, changed, disguised, or recovering.
But none of those truths are created by rumor, quote, dream, command text, card text, context text, or cleanup.
```


====================================================================================================
05_AIDRPG_Production_Chunk_05_Inventory_Equipment_Scene_Items_and_Custody_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 05 — Inventory, Equipment, Scene Items, and Custody Depth

## Document status

Production sequence: 05 of 25.
Build target: WorldLedger V0.17.4 body/identity accepted build.
Purpose: Deepen inventory, equipment, scene-item, custody, item-history, and currency safety without false item grants or unsafe economy activation.

## Doctrine

```text
Visible is not owned.
Available is not acquired.
Offered is not received.
Promised is not paid.
NPC custody is not player custody.
Dropped is not deleted.
Consumed is not carried.
Destroyed is not usable.
Dreamed is not owned.
Quoted is not owned.
Denied is not acquired.
Failed grab is not acquisition.
```

Inventory truth must be conservative, durable, and auditable.

## Allowed changes

```text
Inventory schema helpers
InventoryReducer
ItemNameSanitizer
limited RewardNameSanitizer currency support
CanonValidator inventory rules
ProposalBuilder inventory event types
EventScannerLite inventory/item hints
/inventory renderer
ContextBuilder inventory packet
OperationLedger item/currency keys
DirtyQueue inventory targets
IntegrityChecker inventory checks
ReleaseCheck inventory gates
Regression/player-breaker inventory tests
```

Forbidden:

```text
full economy buy/sell/barter/debt
full crafting/material/recipe engine
full equipment stat modifiers
full non-humanoid equipment constraints
manual /additem or /giveitem
manual /money commands
generated card item creation
old card prose item import
```

Economy and crafting remain deferred to Production Chunk 23.

## Inventory state model

Preferred shape:

```text
inventory: {
  items: {},
  equipped: {},
  stored: {},
  sceneItems: {},
  custody: {},
  currency: {},
  itemHistory: [],
  warnings: [],
  notes: []
}
```

Arrays are acceptable if already used, but stable item keys are required.

## ItemRecord schema

```text
ItemRecord {
  id,
  key,
  displayName,
  normalizedName,
  quantity,
  category,
  tags,
  stackable,
  unique,
  durability,
  accessState,
  owner,
  holder,
  custodyKey,
  locationKey,
  equippedSlot,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  updatedTurn,
  historyKeys,
  warnings,
  notes
}
```

Keys are stable and family-scoped. They must not contain full raw output, quotes, dreams, command text, or card/context prose.

Quantity must be positive for active records. Negative quantity quarantines or repairs safely without granting new items. Unique items do not stack unless policy says so.

## AccessState model

```text
owned carried equipped stored visible available_loot offered npc_custody place_custody inaccessible lost consumed destroyed unknown
```

Owned/carried/equipped/stored are player access states.

Visible/available/offered are not owned.

NPC/place custody and inaccessible are not player-owned.

Lost/consumed/destroyed are not usable.

Unknown is not owned by default.

## Equipment shell

Equipment is a view/state of inventory, not a separate truth source.

Slots:

```text
mainHand offHand head body hands feet ring1 ring2 neck back customSlots
```

Equipped item must exist as owned/carried/equipped. Equipping requires accepted equip event or safe startup source. Equipment stat modifiers are deferred. Body/form equipment constraints are warnings/context labels only in this chunk.

## SceneItemRecord

```text
SceneItemRecord {
  id,
  key,
  displayName,
  quantity,
  accessState,
  locationKey,
  sceneKey,
  holderKey,
  visibility,
  reachable,
  sourceOperationKey,
  createdTurn,
  updatedTurn,
  expiresTurn,
  notes
}
```

Scene items are not player inventory. Visible/available items become inventory only through accepted acquisition/handoff/take events.

## CustodyRecord

```text
CustodyRecord {
  id,
  itemKey,
  itemDisplayName,
  holderType,
  holderKey,
  holderDisplayName,
  accessState,
  locationKey,
  sourceOperationKey,
  createdTurn,
  updatedTurn,
  notes
}
```

Holder types: actor, place, container, unknown, player. NPC custody is not player-owned. Transfer requires accepted transfer/handoff/acquisition.

## Currency model

Currency must be explicit and duplicate-safe. Acceptable simple shape:

```text
currency: { gold: 0, silver: 0, copper: 0, custom: {} }
```

or record shape with amount/source keys.

Rules:

```text
promised reward does not increase currency
ready reward does not increase currency
paid reward may increase currency only once
buy/sell/spend is deferred until economy mini-spec
```

## ItemHistoryRecord

```text
ItemHistoryRecord {
  historyId,
  itemKey,
  eventType,
  fromAccessState,
  toAccessState,
  actorKey,
  placeKey,
  operationKey,
  turn,
  summary,
  notes
}
```

Preserve unique item history. Compact/cap only through safe cleanup policy.

## Categories/tags

Categories:

```text
weapon armor shield tool key consumable quest_item currency treasure material book container food drink clothing jewelry misc unknown
```

Tags are descriptive only. Magical does not grant ability. Quest item does not complete quest. Material does not enable crafting yet.

## ItemNameSanitizer depth

Must handle:

```text
the rusted iron key, but the goblin snatches it away -> rusted iron key
red healing potion from Mira -> red healing potion
three silver arrows on the table -> silver arrows + quantity 3 if supported
jeweled crown that Mira refuses to give you -> jeweled crown
idea of a brass key -> brass key with abstract/metaphorical flag
```

Preserve meaningful modifiers: rusted, iron, red healing, jeweled, silver, old wooden, masterwork, cracked, black gate, cellar.

## Inventory CanonValidator expansion

Accept owned acquisition only when safe output/startup source confirms actual acquisition/handoff/receipt/pickup, target is bound, no denial/failure/revocation/hypothetical/quote/dream/abstract/visible/available/offered/custody/attempt-only flag, and operation key is not duplicate.

Reject ownership when any unsafe flag is present:

```text
deniedByLocalClause failedOrRevoked abstractOrMetaphorical quotedSpeech dreamOrVision visibleOnly availableOnly offeredOnly futureIntent attemptOnly command/context/card/audit/debug source
```

Use pending/scene item/custody for:

```text
You see a silver ring on the table.
The goblin drops a rusty dagger.
Mara offers you the key.
The chest contains a potion.
The jeweled crown rests behind glass.
```

## Mixed clause rule

```text
Mira refuses to give you the jeweled crown, but she hands you the red healing potion.
```

Expected:

```text
jeweled crown not owned
red healing potion acquired once if otherwise safe
```

Do not apply the handoff verb to the denied item or the denial to the accepted potion.

## Proposal event types

```text
inventory.acquire_candidate
inventory.handoff_candidate
inventory.transfer_candidate
inventory.drop_candidate
inventory.lose_candidate
inventory.consume_candidate
inventory.destroy_candidate
inventory.visible_candidate
inventory.available_loot_candidate
inventory.offered_candidate
inventory.custody_candidate
inventory.inaccessible_candidate
inventory.equip_candidate
inventory.unequip_candidate
currency.gain_candidate
currency.promise_candidate
currency.ready_candidate
currency.payment_candidate
```

## InventoryReducer behavior

Acquisition/handoff commit:

```text
1. Confirm accepted validation result.
2. Confirm safe source and no unsafe routed flags.
3. Confirm item target key/displayName.
4. Check operation key.
5. If duplicate, skipped_duplicate.
6. Add or merge item by stack/unique policy.
7. Set accessState carried/owned unless equip/stored validated.
8. Set owner/holder player.
9. Add item history.
10. Add operation key.
11. Mark dirty targets.
12. Audit.
```

Scene items update sceneItems and never inventory.items.

Custody updates custody records and never owned items unless player holder confirmed.

Consume/drop/lose/destroy update accessState/history rather than deleting immediately.

Equip/unequip shell requires item already owned/carried and accepted equip event. No stat bonuses.

## Currency and reward payment split

Preferred split-owner policy:

```text
QuestReducer handles reward status.
InventoryReducer handles currency changes.
```

Reward promise does not pay. Reward ready does not pay. Payment creates currency once. Repeated output skips. `/quests` display cannot pay.

## Operation keys

```text
item.acquire.<itemKey>.<sourceOrScene>.<clauseHash>
item.handoff.<itemKey>.<fromActor>.<toPlayer>.<clauseHash>
item.transfer.<itemKey>.<from>.<to>.<clauseHash>
item.visible.<itemKey>.<placeOrScene>.<clauseHash>
item.available.<itemKey>.<placeOrScene>.<clauseHash>
item.custody.<itemKey>.<holderKey>.<clauseHash>
item.consume.<itemKey>.<clauseHash>
item.destroy.<itemKey>.<clauseHash>
item.drop.<itemKey>.<clauseHash>
item.lose.<itemKey>.<clauseHash>
item.equip.<itemKey>.<slot>.<clauseHash>
item.unequip.<itemKey>.<slot>.<clauseHash>
currency.gain.<amount>.<currencyType>.<sourceKey>.<clauseHash>
reward.currency.pay.<questKey>.<payerKey>.<amount>.<currencyType>
```

## Dirty targets

```text
owned item/currency/equipment -> inventory_equipment, context
scene item -> inventory_equipment, current_location, context
custody -> inventory_equipment, context, maybe current_location
reward payment currency -> inventory_equipment, quest_log, context
```

## /inventory display

Required sections:

```text
Owned / Carried
Equipped
Stored
Visible Nearby
Available Loot
Offered
NPC / Place Custody
Lost / Consumed / Destroyed
Currency
Warnings
```

Visible, available, custody, promised, lost, consumed, destroyed must never display as currently owned/usable.

## Context packet

Include important owned items, equipment, quest items, relevant visible/available items, relevant custody facts, and relevant currency. Never include visible items as owned, promised rewards as currency, or quarantined old claims as truth.

## PC05 ReleaseCheck gates

```text
PC05-G01 ItemRecord schema exists or equivalent stable item record exists
PC05-G02 AccessState model separates owned/visible/available/offered/custody/lost/consumed/destroyed
PC05-G03 SceneItemRecord schema exists
PC05-G04 CustodyRecord schema exists
PC05-G05 Currency model exists and promise/payment separation is enforced
PC05-G06 ItemHistoryRecord or compact history exists
PC05-G07 ItemNameSanitizer preserves meaningful modifiers and flags abstract items
PC05-G08 CanonValidator rejects denied/failed/dream/quote/abstract/visible-only false grants
PC05-G09 mixed clause inventory split is covered
PC05-G10 InventoryReducer rejects unsafe routed inventory events
PC05-G11 scene items do not become owned inventory
PC05-G12 custody items do not become owned inventory
PC05-G13 reward/currency duplicate keys exist
PC05-G14 /inventory displays access states honestly
PC05-G15 command/context/card inventory text cannot become canon
PC05-G16 cleanup does not delete active inventory/durable keys
PC05-G17 equipment shell does not apply stat bonuses yet
PC05-G18 economy/crafting remain disabled
PC05-G19 player-breaker inventory tests registered
```

## Required tests

```text
PC05-001 Denied key no item
PC05-002 Failed grab no item
PC05-003 Abstract item no item
PC05-004 Dream item no item
PC05-005 Quoted handoff no item
PC05-006 Mixed clause split
PC05-007 Visible item scene only
PC05-008 Available loot not owned
PC05-009 Chest contains item not owned
PC05-010 Handoff acquired once
PC05-011 Handoff duplicate skipped
PC05-012 Currency promise no payment
PC05-013 Currency payment once
PC05-014 Currency payment duplicate skipped
PC05-015 Command inventory non-canon
PC05-016 Story Card item prose non-canon
PC05-017 Context item prose non-canon
PC05-018 Negative quantity quarantined
PC05-019 Equipped item must be owned
PC05-020 Equip shell no stat bonus
PC05-021 Consumed item not carried
PC05-022 Lost item not usable
PC05-023 Destroyed item not usable
PC05-024 Inventory display honest
PC05-025 Economy commands disabled
```

## Implementation order

```text
1. Copy V0.17.4 body/identity accepted build.
2. Add inventory helper functions.
3. Deepen ItemNameSanitizer.
4. Update inventory CanonValidator rules.
5. Update ProposalBuilder inventory event support.
6. Update InventoryReducer.
7. Update /inventory.
8. Update ContextBuilder inventory packet.
9. Update IntegrityChecker inventory checks.
10. Add PC05 ReleaseCheck gates.
11. Add PC05 tests.
12. Run foundationcheck, releasecheck, integrity, playerbreakercheck, regressioncheck, inventory.
13. Save as WorldLedgerV0.17.5_INVENTORY_DEPTH_ACCEPTED.txt.
```

## Pass / warn / fail

PASS if access states are separated, false grants rejected, mixed clauses split, scene/custody items not owned, currency promise/payment separated, duplicates prevented, InventoryReducer rejects unsafe data, display is honest, command/context/card item text non-canon, cleanup preserves active/durable data, tests pass.

WARN if equipment shell is display-only, category/tags are display-only, currency is simple but duplicate-safe, economy/crafting remain disabled, non-humanoid equipment constraints are deferred.

FAIL if visible/offered/available becomes owned, failed/denied/dream/quote/abstract becomes owned, custody item becomes player-owned, reward promise pays currency, reward duplicates, command/context/card creates item/currency, negative quantity becomes grant, cleanup deletes active item/durable reward key, buy/sell/craft mutates, or InventoryReducer trusts unsafe data.

## Final doctrine

```text
An item mention is not inventory.
A visible item is not inventory.
A promised reward is not currency.
A custody item is not owned.
A failed acquisition is not acquisition.
Only accepted, validated, duplicate-safe inventory events change player inventory.
```


====================================================================================================
06_AIDRPG_Production_Chunk_06_Ability_Candidate_Training_Use_Failure_and_Stability_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 06 — Ability Candidate, Training, Use, Failure, and Stability Depth

## 0. Document status

**Document name:** AIDRPG Production Chunk 06 — Ability Candidate, Training, Use, Failure, and Stability Depth  
**Production sequence:** 06 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.5 inventory-depth accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen ability records, training evidence, use evidence, failed attempts, candidate/unstable/stable separation, naming safety, duplicate-use protection, and ability command/context display without false ability unlocks.  
**Status:** Core gameplay-depth production chunk.  
**Follows:** Production Chunk 05 — Inventory, Equipment, Scene Items, and Custody Depth.  
**This document does not contain final script code.**

Production Chunk 06 exists because ability handling was one of the easiest places for old builds to become corrupted.

The system must never again treat every ability mention, output title, quote, dream, failed attempt, or training phrase as stable mastery.

Abilities must grow through validated evidence.

Training can create candidates. Repeated use can build evidence. Failures can be recorded without becoming unlocks. Only validated stabilization can create a stable ability.

---

# 1. Chunk doctrine

```text
Mentioned is not learned.
Named is not mastered.
Tried is not stable.
Failed is not unlocked.
Pretended is not usable.
Quoted is not known.
Dreamed is not real mastery.
Training is evidence, not automatic mastery.
Output-only “You learned X” is not enough by default.
Only validated stabilization creates a stable ability.
```

Ability truth must be conservative, evidence-based, duplicate-safe, and explainable.

---

# 2. Why this chunk comes now

Production Chunk 05 made inventory reliable. Abilities come next because later combat, progression, race/lineage powers, spellcasting, cooldowns, failures, quest abilities, and world-threat interactions all depend on clean ability state.

If abilities are not safe now, later systems will repeatedly create junk powers, duplicate evidence, false mastery, and broken progression.

---

# 3. What this chunk may change

Allowed changes:

```text
Ability schema helpers
AbilityReducer
AbilityNameSanitizer
CanonValidator ability rules
ProposalBuilder ability event types
EventScannerLite ability/training/use hints
CommandRenderers /abilities
ContextBuilder ability packet
OperationLedger ability keys
DirtyQueue ability targets
IntegrityChecker ability checks
ReleaseCheck ability gates
Regression/player-breaker ability tests
```

Allowed additions:

```text
AbilityRecord schema depth
AbilityEvidenceRecord schema
ability status model
training evidence model
use success/failure evidence model
reliability labels
cost/cooldown labels as descriptive shells
stabilization requirement model
sealed/unavailable status handling
ability audit summaries
```

Forbidden additions in this chunk:

```text
full magic economy
full cooldown/resource math
race/lineage power automation
absorption/evolution power gain
manual /giveability or /learn commands
combat damage automation
stat gain from ability training
generated ability Story Cards
old card prose ability import
```

Progression/stat effects remain deferred to later chunks.

---

# 4. Modules touched

Allowed modules:

```text
AbilityState helpers
AbilityReducer
AbilityNameSanitizer
CanonValidator ability rules
ProposalBuilder ability proposals
EventScannerLite ability hints
Resolver ability target binding
CommandRenderers: /abilities
ContextBuilderLite ability packet
OperationLedger ability keys
DirtyQueue ability dirty targets
AuditLogger ability lines
InvariantChecker ability checks
ReleaseCheckLite PC06 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to:

```text
ProgressionReducer except no-direct-XP guard
BodyReducer except cost/resource effects remain deferred unless explicitly validated
InventoryReducer except training tools/foci as references only
QuestReducer except quest-granted ability remains deferred unless validated
CombatReducer because advanced combat is not active yet
WorldBible import
CardSync live writes
manual mutation commands
```

---

# 5. Ability state model

The `abilities` bucket should support:

```text
records
stable
unstable
candidates
hints
failed
sealed
unavailable
evidenceLog
warnings
notes
```

Recommended shape:

```text
abilities: {
  records: {},
  stable: [],
  unstable: [],
  candidates: [],
  hints: [],
  failed: [],
  sealed: [],
  unavailable: [],
  evidenceLog: [],
  warnings: [],
  notes: []
}
```

If older code uses arrays, keyed records are still preferred. If converting to maps is risky, keep arrays but require stable ability keys and dedupe helpers.

---

# 6. AbilityRecord schema

Required fields:

```text
AbilityRecord {
  id,
  key,
  displayName,
  normalizedName,
  status,
  family,
  sourceType,
  evidence,
  evidenceScore,
  useCount,
  successCount,
  failureCount,
  trainingCount,
  reliability,
  costLabels,
  cooldownLabels,
  sealedReason,
  unavailableReason,
  sourceOperationKeys,
  createdTurn,
  updatedTurn,
  lastUsedTurn,
  lastTrainedTurn,
  warnings,
  notes
}
```

Rules:

```text
key is stable and family-scoped
key does not contain full raw output
key does not contain training purpose text unless it is truly part of the name
key does not contain quote/dream/command text
key does not contain item/place/object text from non-ability actions
displayName is human-readable
normalizedName is resolver-safe
sourceOperationKeys record the evidence that created/updated the record
```

Allowed statuses:

```text
hint
candidate
unstable
stable
sealed
unavailable
failed_only
unknown
```

Status rules:

```text
hint is not usable
candidate is not stable
unstable may be attempted but unreliable if gameplay rules allow
failed_only is not usable mastery
sealed means known but blocked/unusable
unavailable means mentioned or known as unavailable
stable requires explicit validated stabilization
```

---

# 7. AbilityEvidenceRecord schema

Required shape:

```text
AbilityEvidenceRecord {
  evidenceId,
  abilityKey,
  evidenceType,
  result,
  confidence,
  sourceOperationKey,
  sourceEventId,
  sourceTurn,
  localClauseSummary,
  safetyFlags,
  createdTurn,
  notes
}
```

Evidence types:

```text
training
use_attempt
use_success
use_failure
teacher_confirmation
controlled_demonstration
source_grant
seal
unavailable_claim
hint
rumor
```

Results:

```text
supports_candidate
supports_unstable
supports_stable
supports_failure
supports_sealed
supports_unavailable
noncanon_reference
rejected
```

Evidence is not always mastery.

---

# 8. Ability status transition model

## Hint to candidate

Allowed when the ability is mentioned with plausible training/use relevance, the player trains toward it, or a teacher/source discusses it as a real technique. No stable mastery yet.

## Candidate to unstable

Allowed when repeated training evidence exists, first unstable manifestation/use success exists, or validator marks unstable/candidate manifestation.

## Candidate/unstable to stable

Allowed only when CanonValidator accepts `ability.stabilize_candidate` or equivalent.

Strong sources may include:

```text
validated startup structured ability
teacher/training culmination plus demonstration
successful controlled manifestation
validated source-grant/transformation event
future mini-spec source such as absorption/evolution when enabled
```

## Any status to sealed/unavailable

Allowed from direct validated sealing/unavailability event or setup marking. Sealed/unavailable does not delete history.

---

# 9. AbilityNameSanitizer depth

AbilityNameSanitizer must be owner-specific and must not leak into shared cleanup.

It must clean:

```text
I train Ember Ward to protect allies from heat -> Ember Ward
I practice Frost Step by moving across slick stones -> Frost Step
I drill Stoneguard until my stance holds under impact -> Stoneguard
I train Moonblade by practicing careful crescent cuts -> Moonblade
I refine Crimson Lotus Fang through repeated strikes -> Crimson Lotus Fang
```

It must preserve meaningful ability names:

```text
Shield Against Dawn
Step Across Stars
Song for the Dead
Blade of the North
Ward from Below
```

It must not create abilities from non-ability actions:

```text
I travel to the smithy -> no smithy ability
I inspect the old wooden door -> no door ability
I use the iron key on the cellar door -> no key/door ability
I ask Mara about Moonblade -> hint/reference only unless training/use context exists
```

---

# 10. Ability context detection

Ability context exists when local clause includes clear ability/training/use language:

```text
train/practice/drill/refine/work on/meditate on/spar with
use/cast/channel/activate/manifest
spell/skill/technique/power/ability/talent/passive
learn/unlock/master/stabilize/seal/unseal
```

Weak mention is not enough for mastery:

```text
Mara mentions Moonblade.
The book is titled Crimson Lotus Fang.
A rumor speaks of Azure Dragon Step.
```

These can become hints/references, not stable abilities.

---

# 11. Ability CanonValidator expansion

Accept training evidence only when:

```text
source is safe input intent or safe story output
local clause has clear training/practice/refinement intent
ability target is bound by AbilityNameSanitizer
not pretend-only
not quoted-only
not dream-only
not denied/no usable technique
not command/context/card/audit/debug source
```

Training normally creates or updates candidate/evidence. Training does not create stable mastery by default.

Accept use success evidence only when:

```text
safe story output confirms ability use or manifestation succeeded
ability target is bound
not dream/quote/pretend/metaphor only
not failed/collapsed/no effect
operation key not duplicate
```

Use success may add evidence and reliability, but it does not automatically stabilize unless a separate stabilize result is accepted.

Accept use failure evidence when:

```text
safe input/output states attempt/failure/collapse/no effect
ability target is bound
not command/context/card source
```

Failure evidence must not create stable mastery.

Accept stable ability only when:

```text
structured startup ability before lock
validated teacher/training culmination
successful controlled demonstration with stabilization wording
validated grant/source event
future enabled mini-spec event
```

Reject stable ability when:

```text
output-only unsupported “You learned X”
quoted instruction only
dream/vision only
pretend use
failed/collapsed/no effect
scroll/book/title only
rumor only
manual command output
context/card/audit/debug source
input attempt only with no output support
non-ability target
wrong sanitizer used
```

---

# 12. Critical old-failure rules

## Output-only learning rule

Example:

```text
You learned Moonblade.
```

Default result:

```text
pending or rejected unsupported learning claim
no stable ability
no direct AbilityReducer stable commit
```

It may become evidence only if surrounding context supports it through training arc, teacher confirmation, demonstration, startup, or source-grant.

## Failed ability rule

Examples:

```text
You attempt Void Lantern Veil, but it collapses.
Azure Dragon Step flickers and fails.
Your spell fizzles with no effect.
```

Expected:

```text
failure evidence recorded
status may remain candidate/unstable/failed_only
stable ability not created
```

## Pretend / quote / dream rules

```text
You pretend to use Azure Dragon Step. -> no real ability use
Mira says, "Use Hollow Star Palm." -> hint/reference only
In a dream, you master Star Chain. -> no real stable ability
```

---

# 13. Ability ProposalBuilder event types

Add or verify:

```text
ability.hint_candidate
ability.train_intent
ability.train_candidate
ability.use_attempt
ability.use_success_candidate
ability.use_failure_candidate
ability.evidence_candidate
ability.output_only_learning_claim
ability.learn_candidate
ability.stabilize_candidate
ability.seal_candidate
ability.unavailable_candidate
ability.failed_only_candidate
```

Current chunk may safely commit hints, candidates, training evidence, use attempt evidence, use success evidence, use failure evidence, failed-only, sealed/unavailable when directly validated, and stable only from strict validated stabilize/source event.

---

# 14. EventScannerLite ability hints

EventScannerLite may detect ability/training/use hints, but detection is not canon.

Allowed scan families:

```text
training phrases
practice phrases
spell/skill/technique/power words
use/cast/channel/activate/manifest phrases
learn/unlock/master/stabilize phrases
failed/fizzle/collapse/no effect phrases
seal/unavailable phrases
```

Must flag:

```text
quotedSpeech
dreamOrVision
rumorOrClaim
pretendOnly
failedOrRevoked
noEffect
outputOnlyLearning
hypothetical
futureIntent
attemptOnly
nonAbilityContext
commandSource
contextSource
cardSource
auditSource
```

Parser cannot mutate abilities.

---

# 15. AbilityReducer detailed behavior

AbilityReducer owns:

```text
abilities.records
abilities.stable
abilities.unstable
abilities.candidates
abilities.hints
abilities.failed
abilities.sealed
abilities.unavailable
abilities.evidenceLog
abilities.warnings
```

Hint commit:

```text
create/update ability hint record
status hint unless stronger existing status exists
record evidence type hint
not make usable/stable
mark abilities/context dirty
audit compactly
```

Training commit:

```text
create/update ability record
status candidate unless existing status stronger
append training evidence
increment trainingCount
record sourceOperationKey
mark abilities/context dirty
audit compactly
```

Use success commit:

```text
append use_success evidence
increment useCount and successCount
increase reliability label if policy exists
keep status candidate/unstable unless stabilize event is accepted
mark abilities/context dirty
```

Use failure commit:

```text
append use_failure evidence
increment useCount and failureCount
set or keep status candidate/unstable/failed_only as appropriate
not create stable ability
mark abilities/context dirty
```

Stabilize commit:

```text
require validationResult.eventType = ability.stabilize_candidate or equivalent
check durable operation key
set status stable
add to stable list
remove from candidate/unstable display lists if needed
preserve evidence history
mark abilities/context dirty
audit high-importance line
```

Sealed/unavailable commit:

```text
set status sealed or unavailable
record reason
preserve evidence/history
mark abilities/context dirty
```

---

# 16. AbilityReducer defensive guards

AbilityReducer must reject/no-op if routed result has unsafe markers:

```text
command source
context source
card source
audit/debug source
quoted-only stable creation
dream-only stable creation
pretend-only stable creation
failed/no-effect stable creation
output-only unsupported stable creation
non-ability target
wrong sanitizer used
missing ability key
```

Reducer should not blindly trust upstream.

---

# 17. Reliability, cost, and cooldown shells

This chunk may add descriptive reliability labels:

```text
unknown
unproven
in_training
unstable
inconsistent
reliable
mastered
sealed
unavailable
```

Rules:

```text
reliability label is display/support only unless later mechanics use it
label does not replace ability status
failure evidence can lower label
success evidence can improve label
stable status still requires stabilization rules
```

Cost/cooldown labels are descriptive only:

```text
costLabels: ['drains stamina']
cooldownLabels: ['requires focus before reuse']
```

No numeric resource drain or cooldown enforcement unless future Body/Progression/resource systems explicitly validate and enable it.

---

# 18. Ability operation keys

Required operation keys:

```text
ability.hint.<abilityKey>.<sourceHash>
ability.train.<abilityKey>.<clauseHash>
ability.evidence.<abilityKey>.<evidenceType>.<clauseHash>
ability.use.<abilityKey>.<resultKind>.<outputHash>
ability.failure.<abilityKey>.<clauseHash>
ability.stabilize.<abilityKey>.<sourceKey>
ability.seal.<abilityKey>.<sourceKey>
ability.unavailable.<abilityKey>.<sourceKey>
```

Rules:

```text
stabilize keys are durable
training/use evidence keys are recent or durable depending importance
duplicate use evidence should not spam counts on replay
output-only rejected claim should not create stable key
```

---

# 19. Dirty targets and context

Ability updates should mark:

```text
abilities
context
player_sheet if stable ability materially changes player sheet
body_status only if validated ability cost/body effect occurs later
```

ContextBuilder should include concise ability facts, not the full evidence log.

Example packet:

```text
Abilities: Ember Ward is a candidate under training, not stable. Void Lantern Veil collapsed during an attempt and is not unlocked.
```

Do not include hints as stable powers, failed attempts as usable abilities, quoted/dream/rumor abilities as real mastery, or quarantined ability claims.

---

# 20. `/abilities` display requirements

Required sections:

```text
Stable abilities
Unstable abilities
Candidate abilities
Hints / mentions
Training evidence
Failed or collapsed attempts
Sealed / unavailable abilities
Warnings
```

Display rules:

```text
candidate abilities are not listed as stable
hints are not listed as usable
failed attempts are not listed as learned
sealed/unavailable abilities are not listed as usable unless later unsealed
output-only unsupported claims are displayed as pending/rejected evidence, not stable
```

Example:

```text
[AIDRPG] Abilities

Stable:
- None recorded

Candidate:
- Ember Ward — training evidence recorded; not stable yet

Failed / collapsed:
- Void Lantern Veil — attempt collapsed; no stable ability unlocked

Hints:
- Moonblade — unsupported learning claim; not confirmed stable
```

---

# 21. IntegrityChecker and Cleanup protections

IntegrityChecker must check:

```text
no duplicate stable ability keys
candidate/stable lists match record statuses
hint does not appear in stable list
failed_only does not appear in stable list
sealed/unavailable abilities not displayed as usable
ability records have stable keys
ability keys not polluted by training suffixes
ability records do not come from command/card/context source
output-only learning claim not stable unless validation source exists
```

Cleanup may compact low-value evidence or rejected-claim audits, but must not delete stable abilities, sealed/unavailable histories, important candidate training evidence, durable stabilization keys, or source evidence needed for audit.

Cleanup must not promote candidates to stable or remove failures to make an ability look cleaner.

---

# 22. ReleaseCheck gates for this chunk

Add or verify PC06 gates:

```text
PC06-G01 AbilityRecord schema exists or equivalent stable record exists
PC06-G02 AbilityEvidenceRecord schema exists or equivalent evidence log exists
PC06-G03 ability status model separates hint/candidate/unstable/stable/sealed/unavailable/failed_only
PC06-G04 AbilityNameSanitizer is owner-specific and does not live in shared cleaner
PC06-G05 training suffix cleanup works for Ember Ward/Frost Step/Stoneguard examples
PC06-G06 non-ability actions do not create abilities
PC06-G07 CanonValidator accepts training as evidence/candidate, not mastery
PC06-G08 output-only learning claim does not create stable ability by default
PC06-G09 failed/pretend/quote/dream ability claims do not create stable abilities
PC06-G10 AbilityReducer rejects unsafe routed ability events
PC06-G11 stabilization requires explicit validated stabilize/source event
PC06-G12 duplicate ability evidence/use/stabilization keys exist
PC06-G13 /abilities displays candidate/stable/failed/sealed honestly
PC06-G14 command/context/card ability text cannot become canon
PC06-G15 cleanup does not delete stable abilities or promote candidates
PC06-G16 progression/stat gains from ability training remain deferred unless separately validated
PC06-G17 player-breaker ability tests registered
```

ReleaseCheck must fail if output-only, failed, pretend, quoted, dreamed, command, card, or context ability claims can become stable abilities.

---

# 23. Required tests

```text
PC06-001 Ember Ward training -> candidate/evidence, not stable
PC06-002 Frost Step cleanup -> Frost Step, not full method phrase
PC06-003 Stoneguard cleanup -> Stoneguard
PC06-004 Shield Against Dawn preserved, not stripped to Shield
PC06-005 travel to smithy -> no smithy ability
PC06-006 inspect old wooden door -> no door ability
PC06-007 use iron key on cellar door -> no key/door ability
PC06-008 You learned Moonblade -> no stable Moonblade
PC06-009 scroll title says “You learned Crimson Lotus Fang” but you learn nothing -> no ability unlock
PC06-010 Void Lantern Veil collapses -> failure evidence, no stable
PC06-011 pretend Azure Dragon Step -> no unlock
PC06-012 quoted Hollow Star Palm instruction -> hint only/no unlock
PC06-013 dream Star Chain mastery -> no real stable ability
PC06-014 use success evidence does not auto-stabilize
PC06-015 explicit stabilization accepted only through stabilize validation
PC06-016 duplicate training does not spam evidence
PC06-017 duplicate stabilization skipped
PC06-018 /abilities command output cannot become canon
PC06-019 Story Card says mastered Moonblade -> no stable ability
PC06-020 Context says player knows Star Chain -> no ability mutation
PC06-021 /abilities display separates stable/candidate/failed/hint
PC06-022 ability cleanup does not affect item/place cleanup
PC06-023 sealed ability not usable
PC06-024 ability training does not award XP directly
PC06-025 cleanup does not promote/delete ability truth
```

Minimum expectation:

```text
25 PC06 tests registered
0 critical PC06 failures
```

---

# 24. Parser/proposal/validator changes

Allowed:

```text
ability/training/use/failure/stabilization detection refinement
owner-specific ability name cleanup
output-only learning safety
pretend/quote/dream/failure guards
operation key suggestions
```

Forbidden:

```text
full combat parser
race/lineage power parser
absorption/evolution parser
manual ability mutation parser
card/context ability import
```

---

# 25. Reducer changes

Allowed:

```text
AbilityReducer hint/candidate/training/use success/use failure/stabilization/sealed/unavailable behavior
AbilityReducer unsafe routed-data rejection
ability evidence logging
dirty targets
duplicate skip results
```

Forbidden:

```text
XP/stat award from training
resource cost drain without validated body/progression rule
manual give ability reducer
combat damage from ability use
auto quest completion from ability use
```

---

# 26. Command/context/card changes

Allowed:

```text
/abilities display depth
/context ability packet
/cardaudit ability mirror labels if dry-run
/releasecheck PC06 gates
/playerbreakercheck ability cases
```

Forbidden:

```text
/giveability
/learn
/master
/unlock
manual ability mutation commands
live card writes that create ability truth
```

---

# 27. Beginner implementation order

1. Copy `WorldLedgerV0.17.5_INVENTORY_DEPTH_ACCEPTED.txt` to `WorldLedgerV0.17.6_ABILITY_DEPTH_WORKING.txt`.
2. Add/verify ability helper functions: `ensureAbilitiesShape`, `makeAbilityRecord`, `makeAbilityEvidenceRecord`, `normalizeAbilityStatus`, `normalizeReliabilityLabel`, `isStableAbilityStatus`, `isUnsafeAbilitySource`, and `shouldPreserveAbilityNamePhrase`.
3. Deepen AbilityNameSanitizer and verify it does not affect shared/item/place cleanup.
4. Update ability CanonValidator rules for training, use attempt, use success, use failure, output-only learning, stabilization, sealed, and unavailable.
5. Update ProposalBuilder ability support so proposals carry ability target, evidence type, result kind, source/localClause/safety flags, sanitizerUsed, and operation key suggestion.
6. Update AbilityReducer to safely commit hint, candidate/training evidence, use success evidence, use failure evidence, stabilization, sealed, and unavailable.
7. Update `/abilities` display.
8. Update ContextBuilder ability packet.
9. Update IntegrityChecker ability checks.
10. Add PC06-G01 through PC06-G17 ReleaseCheck gates.
11. Add PC06-001 through PC06-025 tests.
12. Run `/foundationcheck`, `/releasecheck`, `/integrity`, `/playerbreakercheck`, `/regressioncheck`, and `/abilities`.
13. Save accepted build as `WorldLedgerV0.17.6_ABILITY_DEPTH_ACCEPTED.txt`.

---

# 28. What this chunk produces

Production Chunk 06 should produce:

```text
WorldLedger V0.17.6 ability-depth accepted script if code changes are made
ability helper depth
AbilityReducer evidence/status behavior
AbilityNameSanitizer depth
ability CanonValidator refinement
/abilities display improvement
ability context packet
ability integrity checks
PC06 ReleaseCheck gates
PC06 regression/player-breaker tests
ability acceptance report
updated planning zip with Production Chunk 06 added
```

It should not produce:

```text
full combat system
full magic resource economy
race/lineage power automation
absorption/evolution power gain
manual ability mutation commands
stat/XP training rewards
live CardSync writes
generated ability cards
old card prose import
```

---

# 29. Pass / warn / fail criteria

## PASS

Production Chunk 06 passes if:

```text
ability statuses are separated
training creates evidence/candidate, not mastery
output-only learning does not create stable ability
failed/pretend/quote/dream ability claims do not create stable ability
AbilityNameSanitizer cleans ability suffixes without global cleanup leaks
AbilityReducer rejects unsafe routed ability events
stabilization requires explicit validated stabilize/source event
duplicate evidence/use/stabilization keys work
/abilities displays candidate/stable/failed/sealed honestly
command/context/card ability text cannot become canon
cleanup preserves stable/candidate/failure truth safely
PC06 tests pass
ReleaseCheck has no PC06 FAIL/BLOCKED gates
```

## WARN

Warn but allow progress if:

```text
cost/cooldown labels are descriptive only
resource costs are deferred
progression XP/stat effects are deferred
race/lineage powers are deferred
combat ability effects are deferred
stabilization threshold is conservative
```

## FAIL

Fail and stop if:

```text
You learned Moonblade creates stable Moonblade by default
failed/pretend/quote/dream ability becomes stable
travel/inspect/use-object creates junk ability
ability cleanup breaks item/place names
candidate displays as stable
hint displays as usable
training awards XP directly
command/context/card text creates ability
duplicate stabilization or evidence spams records
cleanup deletes stable ability or promotes candidate
AbilityReducer trusts unsafe routed data
```

---

# 30. Handoff to Production Chunk 07

When this chunk passes, Production Chunk 07 begins:

```text
Production Chunk 07 — Quest, Objective, Reward, and Promise/Payment Depth
```

Production Chunk 07 may use ability facts for quest objectives such as “train Ember Ward” or “stabilize Moonblade,” but should not let quest text create stable abilities without ability validation.

Production Chunk 06 must leave abilities reliable enough that future chunks can ask:

```text
Is this ability only a hint?
Is it a candidate?
Was it tried and failed?
Is it unstable?
Is it stable?
Is it sealed or unavailable?
What evidence supports that status?
```

---

# 31. Final doctrine

Production Chunk 06 is where ability truth stops being a string-matching accident.

Final rule:

```text
Abilities are records with evidence and status.
Training is evidence.
Use is evidence.
Failure is evidence.
Hints are not powers.
Candidates are not mastery.
Stable abilities require validation.
No unsafe source can create a power.
```

Once this passes, the project can safely deepen quests, objectives, and rewards in Production Chunk 07.


====================================================================================================
07_AIDRPG_Production_Chunk_07_Quest_Objective_Reward_and_Promise_Payment_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 07 — Quest, Objective, Reward, and Promise/Payment Depth

## 0. Document status

**Document name:** AIDRPG Production Chunk 07 — Quest, Objective, Reward, and Promise/Payment Depth  
**Production sequence:** 07 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.6 ability-depth accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen quest records, objective states, quest offers, quest acceptance, objective progress, completion, reward promises, reward readiness, reward payment, and duplicate-safe reward behavior without rumor activation or promise/payment confusion.  
**Status:** Core gameplay-depth production chunk.  
**Follows:** Production Chunk 06 — Ability Candidate, Training, Use, Failure, and Stability Depth.  
**This document does not contain final script code.**

Production Chunk 07 exists because quest and reward handling is one of the most dangerous RPG automation areas.

Old systems often blurred:

```text
rumor
offer
accepted quest
active objective
objective progress
objective completion
ready to turn in
quest completion
promised reward
paid reward
duplicate paid reward
```

That cannot happen in the final system.

A quest must move through explicit states.

Rewards must be promised, ready, paid, denied, disputed, or duplicate-skipped with clear audit.

A promised reward is not currency.

A completed objective is not payment.

A command display is not reward collection.

---

# 1. Chunk doctrine

```text
Rumor is not a quest.
Offer is not acceptance.
Acceptance is not completion.
Progress is not completion.
Completion is not payment.
Ready reward is not paid reward.
Promised gold is not currency.
Paid once means paid once.
Quest text is not inventory.
Command display is not reward collection.
```

Quest and reward truth must be stateful, duplicate-safe, and explainable.

---

# 2. Why this chunk comes now

Production Chunk 05 made inventory/currency safe.

Production Chunk 06 made abilities safe.

Quest/reward depth comes next because it depends on both:

```text
quest item custody and ownership
currency payment through InventoryReducer
ability training objectives
body injury/recovery objectives
actor quest givers
location objectives
future progression XP rewards
future reputation/faction/domain consequences
```

If quest and reward states are not safe now, future systems will duplicate rewards, activate rumor quests, auto-complete objectives, and hide broken progression behind command displays.

---

# 3. What this chunk is allowed to change

Production Chunk 07 may change:

```text
Quest schema helpers
QuestReducerLite
Reward handling helpers
QuestObjectiveSanitizer
RewardNameSanitizer
CanonValidator quest/reward rules
ProposalBuilder quest/reward event types
EventScannerLite quest/reward hints
CommandRenderers /quests
ContextBuilder quest packet
OperationLedger quest/reward keys
DirtyQueue quest targets
IntegrityChecker quest/reward checks
ReleaseCheck quest/reward gates
Regression/player-breaker quest/reward tests
```

Production Chunk 07 may add:

```text
QuestRecord schema depth
ObjectiveRecord schema
RewardRecord schema
QuestStatus model
ObjectiveStatus model
RewardStatus model
quest giver/turn-in actor references
quest location references
quest progress history
reward promise/readiness/payment states
reward duplicate ledger
split-result reward payment contract with InventoryReducer
quest audit summaries
```

Production Chunk 07 may not add:

```text
full reputation automation
full economy buy/sell/barter/debt
full crafting quest material system
full progression XP/level automation unless explicit shell only
manual /completequest /payreward commands
quest generation engine
generated quest Story Cards
old card prose quest import
```

Progression, reputation, domain, economy, crafting, and generated cards remain deferred unless specifically scoped later.

---

# 4. Modules touched

Allowed modules:

```text
QuestState helpers
QuestReducerLite
Reward helpers
QuestObjectiveSanitizer
RewardNameSanitizer
CanonValidator quest/reward rules
ProposalBuilder quest/reward proposals
EventScannerLite quest/reward hints
Resolver quest/reward target binding
CommandRenderers: /quests and reward references in /inventory
ContextBuilderLite quest packet
OperationLedger quest/reward keys
DirtyQueue quest dirty targets
AuditLogger quest/reward lines
InvariantChecker quest/reward checks
ReleaseCheckLite PC07 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to:

```text
InventoryReducer except explicit split-result currency gain contract
ProgressionReducer except XP reward shell/deferred display
ActorReducer except quest giver/turn-in references
PlaceReducer except location objective references
AbilityReducer except ability objective references only
BodyReducer except injury/recovery objective references only
Economy/crafting/reputation/domain systems
CardSync live writes
WorldBible import
manual mutation commands
```

---

# 5. Quest state model

Quests bucket should support:

```text
records
active
offered
rumored
readyToTurnIn
completed
failed
abandoned
rewardLedger
warnings
notes
```

Recommended shape:

```text
quests: {
  records: {},
  active: [],
  offered: [],
  rumored: [],
  readyToTurnIn: [],
  completed: [],
  failed: [],
  abandoned: [],
  rewardLedger: {},
  warnings: [],
  notes: []
}
```

If existing code uses arrays, keep stable quest keys and owner helpers.

Do not maintain multiple conflicting quest records for the same quest key.

---

# 6. QuestRecord schema

Required quest record fields:

```text
QuestRecord {
  id,
  key,
  title,
  normalizedTitle,
  status,
  originStatus,
  giverActorKey,
  giverDisplayName,
  turnInActorKey,
  turnInPlaceKey,
  locationKeys,
  objectives,
  rewards,
  promisedRewards,
  paidRewards,
  failedReasons,
  abandonedReason,
  sourceOperationKeys,
  createdTurn,
  updatedTurn,
  completedTurn,
  rewardedTurn,
  historyKeys,
  warnings,
  notes
}
```

## 6.1 Quest identity rules

```text
quest key is stable
quest key is family-scoped
quest key does not contain full raw output
quest key does not contain command/card/context text
quest title is readable
quest title is not just raw objective paragraph unless no better title exists
same giver + same core objective should usually bind to existing quest
```

## 6.2 Quest origin status

Quest origin status should identify how the quest entered state:

```text
rumored
offered
accepted_from_input
accepted_from_output
structured_startup
system_generated_later_disabled
unknown
```

Origin status helps prevent rumor/offer from becoming active automatically.

---

# 7. Quest status model

Allowed quest statuses:

```text
rumored
offered
active
progressing
ready_to_turn_in
completed
rewarded
failed
abandoned
expired
unknown
```

Rules:

```text
rumored is not active
offered is not accepted
active is not completed
ready_to_turn_in is not rewarded
completed is not automatically rewarded
rewarded requires payment/status event
failed/abandoned should preserve history
expired requires explicit time/deadline policy later
```

---

# 8. ObjectiveRecord schema

Required objective fields:

```text
ObjectiveRecord {
  id,
  key,
  questKey,
  verb,
  targetKey,
  targetDisplayName,
  description,
  status,
  progressCurrent,
  progressRequired,
  sourceOperationKeys,
  createdTurn,
  updatedTurn,
  completedTurn,
  evidence,
  warnings,
  notes
}
```

Objective statuses:

```text
inactive
active
in_progress
complete
failed
optional
hidden
unknown
```

Rules:

```text
objective mention is not completion
partial progress is not completion
completion requires accepted objective_complete event
completion of one objective does not complete unrelated objectives
automatic all-objectives-complete status change must be explicit and tested
```

---

# 9. Objective verb support

QuestObjectiveSanitizer must support broad objective types without over-confirming them.

Required verbs:

```text
recover
find
repair
escort
clear
deliver
investigate
protect
bring
retrieve
rescue
defeat
survey
track
report to
speak with
collect
gather
return
seal
open
close
survive
train
learn
stabilize
heal
cure
```

Adding a verb to objective support does not mean the objective is completed when the verb is mentioned.

Examples:

```text
repair the north wall -> objective title Repair the North Wall
escort Bram to the chapel -> Escort Bram to the Chapel
investigate the burned tavern -> Investigate the Burned Tavern
train Ember Ward -> Train Ember Ward, ability objective only
```

---

# 10. RewardRecord schema

Required reward fields:

```text
RewardRecord {
  id,
  key,
  questKey,
  rewardType,
  description,
  amount,
  currencyType,
  itemKey,
  itemDisplayName,
  xpAmount,
  status,
  promisedByActorKey,
  paidByActorKey,
  sourceOperationKeys,
  paymentOperationKey,
  promisedTurn,
  readyTurn,
  paidTurn,
  deniedTurn,
  disputedTurn,
  warnings,
  notes
}
```

Reward types:

```text
currency
item
xp
favor
information
access
title
reputation_deferred
unknown
```

Reward statuses:

```text
promised
ready
paid
denied
disputed
duplicate_skipped
forfeited
unknown
```

Rules:

```text
promised is not paid
ready is not paid
paid requires accepted payment event
item reward requires InventoryReducer item event if actually handed over
currency reward requires InventoryReducer currency event
XP reward requires ProgressionReducer event if progression active
reputation reward remains deferred unless reputation spec active
```

---

# 11. QuestObjectiveSanitizer depth

QuestObjectiveSanitizer should clean objective text without losing meaning.

Examples:

```text
repair the north wall -> Repair the North Wall
escort Bram to the chapel -> Escort Bram to the Chapel
investigate the burned tavern -> Investigate the Burned Tavern
clear the mill wolves -> Clear the Mill Wolves
retrieve the Crown of Dawn from the ash shrine -> Retrieve the Crown of Dawn from the Ash Shrine
train Ember Ward until stable -> Train Ember Ward Until Stable
```

It must not:

```text
strip destination from escort objectives
turn objective mention into completion
turn reward text into objective completion
turn quest title into active quest without acceptance
```

---

# 12. RewardNameSanitizer depth

RewardNameSanitizer must separate promise, readiness, and payment.

Must recognize:

```text
15 gold
fifteen gold
3 silver
one hundred copper
red healing potion as reward
training scroll as reward
20 XP if progression reward active later
favor from Old Bren
access to the north gate
```

Promise markers:

```text
promises to pay
will pay
reward will be
offers if
payment after
```

Ready markers:

```text
your reward is ready
come collect your reward
Bren has the coins waiting
```

Payment markers:

```text
pays you
hands you the promised coins
gives you 15 gold
places the reward in your hand
counts out the gold
```

Denial/dispute markers:

```text
refuses to pay
denies the reward
claims you failed
offers less than promised
disputes the reward
withholds payment
```

---

# 13. Quest CanonValidator expansion

CanonValidator must enforce quest state transitions.

## 13.1 Accept quest offer when

```text
safe story output has NPC/job/board/request offering a task
objective target is bound enough
not quoted hypothetically only
not dream/rumor only unless status = rumored
not command/context/card source
```

Result:

```text
quest offered or rumored
not active unless accepted
```

## 13.2 Accept quest acceptance when

```text
player explicitly accepts in input and output/structure supports quest start
or safe output clearly states quest accepted/started
or structured startup quest starts active before lock
```

Result:

```text
quest active
objectives initialized
```

## 13.3 Accept objective progress when

```text
safe output directly shows partial progress toward active objective
objective exists or can bind to active quest
not merely intent
not command/context/card source
```

## 13.4 Accept objective completion when

```text
safe output directly confirms required objective completed
objective target binds to active quest objective
not duplicate
not merely arriving near objective
not merely seeing target
```

## 13.5 Accept quest completion when

```text
safe output directly confirms quest completed
or all required objectives complete and policy allows status ready/completed
not duplicate
```

Completion should normally lead to:

```text
ready_to_turn_in
```

unless reward/payment is also explicitly validated.

---

# 14. Reward CanonValidator expansion

## 14.1 Promise accepted as promise only

Example:

```text
Old Bren promises to pay you 15 gold if you clear the wolves.
```

Expected:

```text
reward.promise_candidate accepted/pending
currency.gain rejected
payment rejected
```

## 14.2 Ready reward accepted as ready only

Example:

```text
Old Bren says your 15 gold reward is ready.
```

Expected:

```text
reward.ready_candidate accepted
currency unchanged
```

## 14.3 Payment accepted only when

```text
safe output directly confirms payment/handoff
amount/item reward target is bound
payer/source is plausible
not denied/disputed/withheld
operation key not duplicate
```

## 14.4 Payment rejected when

```text
promises to pay
will pay later
could pay
reward is listed on quest board
/ref command displays reward
Story Card says reward paid
context says reward paid
refuses/denies/disputes payment
```

---

# 15. Quest ProposalBuilder event types

Add or verify event types:

```text
quest.rumor_candidate
quest.offer_candidate
quest.accept_candidate
quest.objective_progress_candidate
quest.objective_complete_candidate
quest.ready_to_turn_in_candidate
quest.completed_candidate
quest.failed_candidate
quest.abandoned_candidate
quest.expired_candidate
reward.promise_candidate
reward.ready_candidate
reward.payment_candidate
reward.denied_candidate
reward.disputed_candidate
currency.gain_candidate
item.reward_handoff_candidate
xp.reward_candidate_deferred
```

Current chunk may commit safe:

```text
quest rumor/offer/acceptance
objective progress/completion
ready_to_turn_in
quest completion status
reward promise/ready/paid/denied/disputed
currency gain only through explicit InventoryReducer split-result payment
item reward handoff only through InventoryReducer split-result item event
```

XP reward remains deferred unless ProgressionReducer exact XP handling is already active and ReleaseCheck allows it.

---

# 16. EventScannerLite quest/reward hints

EventScannerLite may detect quest/reward hints, but detection is not canon.

Allowed scan families:

```text
request/help/job/task/quest/mission
accept/agree/take the job
repair/recover/escort/clear/deliver/investigate/protect/rescue/defeat
progress markers
completion markers
turn-in markers
reward promise markers
reward ready markers
reward payment markers
reward denial/dispute markers
```

Must flag:

```text
rumorOrClaim
quotedSpeech
hypothetical
futureIntent
attemptOnly
dreamOrVision
commandSource
contextSource
cardSource
auditSource
duplicateRisk
promiseOnly
readyOnly
paymentConfirmed
deniedOrDisputed
```

Parser cannot mutate quest or reward state.

---

# 17. QuestReducerLite detailed behavior

QuestReducerLite owns:

```text
quests.records
quests.active
quests.offered
quests.rumored
quests.readyToTurnIn
quests.completed
quests.failed
quests.abandoned
quests.rewardLedger
quests.warnings
```

## 17.1 Rumor commit

Accepted rumor should:

```text
create/update quest as rumored
store source/evidence
not activate quest
mark quest_log/context dirty if useful
```

## 17.2 Offer commit

Accepted offer should:

```text
create/update quest as offered
store giver if known
store objective candidates
store promised rewards if present
not activate quest unless accepted separately
```

## 17.3 Acceptance commit

Accepted acceptance should:

```text
create/update quest as active
initialize objectives
preserve promised rewards
record operation key
mark quest_log/context dirty
```

## 17.4 Objective progress commit

Accepted progress should:

```text
update objective progressCurrent/status
append evidence/history
not complete unless threshold/direct completion reached and accepted
```

## 17.5 Objective completion commit

Accepted completion should:

```text
set objective status complete
record completedTurn
check duplicate key
if all required objectives complete, mark quest ready_to_turn_in or completed according to policy
not pay reward automatically
```

## 17.6 Quest completion commit

Accepted quest completion should:

```text
set quest status completed or ready_to_turn_in according to source
preserve reward status as promised/ready unless payment exists
not award currency/items/XP automatically
```

## 17.7 Failure/abandonment commit

Accepted failure/abandonment should:

```text
set status failed/abandoned
record reason/source
preserve history
not delete quest immediately
```

---

# 18. Reward behavior details

## 18.1 Reward promise commit

Accepted promise should:

```text
create/update RewardRecord
status promised
store amount/item/xp/favor/access if parsed
store promisedByActorKey if known
mark quest_log/context dirty
no inventory/currency/progression mutation
```

## 18.2 Reward ready commit

Accepted ready should:

```text
set reward status ready
record readyTurn
no inventory/currency/progression mutation
```

## 18.3 Reward payment commit

Accepted payment should:

```text
check reward payment key
set reward status paid
record paidTurn/paidByActorKey
mark quest_log/context dirty
emit or pair with explicit InventoryReducer currency/item event if reward is currency/item
```

Preferred safe policy:

```text
QuestReducer marks reward paid.
InventoryReducer receives separate accepted currency.gain_candidate or item.reward_handoff_candidate.
Both share a related payment key.
```

If split result cannot be implemented yet, mark payment status and defer currency mutation rather than paying unsafely.

## 18.4 Reward denied/disputed commit

Accepted denial/dispute should:

```text
set status denied or disputed
record reason/source
no payment
no currency/item/XP mutation
```

---

# 19. QuestReducer defensive guards

QuestReducer must reject/no-op if routed result has unsafe markers:

```text
command source
context source
card source
audit/debug source
rumor-only as active quest
hypothetical offer as active quest
promise as payment
ready as payment
objective mention as completion
unbound quest target for durable completion
duplicate payment key
missing quest key
wrong owner reducer
```

Reducer should not blindly trust upstream.

If unsafe routed data reaches QuestReducer, it must not mutate quest/reward truth.

---

# 20. Split-owner reward payment contract

Because inventory/currency is owned by InventoryReducer, reward payment needs explicit handoff.

## 20.1 Currency reward flow

Example:

```text
Old Bren pays you the promised 15 gold.
```

Validated results:

```text
reward.payment_candidate -> QuestReducer marks reward paid
currency.gain_candidate -> InventoryReducer adds 15 gold
```

Both should share:

```text
rewardPaymentKey
payerKey
questKey
amount
currencyType
sourceClauseHash
```

## 20.2 Item reward flow

Example:

```text
Bren hands you the silver wolf charm as your reward.
```

Validated results:

```text
reward.payment_candidate -> QuestReducer marks item reward paid/received
item.reward_handoff_candidate -> InventoryReducer adds item
```

## 20.3 XP reward flow

XP reward should remain deferred unless ProgressionReducer exact XP support is active.

If deferred:

```text
reward status can be ready/paid as narrative status
XP mutation deferred/pending
ReleaseCheck warns honestly
```

---

# 21. Operation keys

Required quest/reward operation keys:

```text
quest.rumor.<questKey>.<sourceHash>
quest.offer.<questKey>.<giverKey>.<clauseHash>
quest.accept.<questKey>.<giverKey>.<clauseHash>
quest.objective.progress.<questKey>.<objectiveKey>.<progressHash>
quest.objective.complete.<questKey>.<objectiveKey>.<clauseHash>
quest.ready.<questKey>.<sourceKey>
quest.complete.<questKey>.<sourceKey>
quest.fail.<questKey>.<sourceKey>
quest.abandon.<questKey>.<sourceKey>
quest.reward.promise.<questKey>.<rewardKey>.<sourceHash>
quest.reward.ready.<questKey>.<rewardKey>.<sourceHash>
quest.reward.pay.<questKey>.<payerKey>.<rewardKey>
quest.reward.deny.<questKey>.<payerKey>.<rewardKey>
quest.reward.dispute.<questKey>.<payerKey>.<rewardKey>
```

Inventory/Progression related keys:

```text
currency.gain.<amount>.<currencyType>.<rewardPaymentKey>
item.reward.<itemKey>.<rewardPaymentKey>
xp.reward.<amount>.<rewardPaymentKey> deferred until progression active
```

Rules:

```text
payment keys are durable
objective completion keys are durable or quest-scoped durable
progress keys prevent spam on output replay
promise keys prevent duplicate promised reward records
```

---

# 22. Dirty targets

Quest/reward updates should mark:

```text
quest_log
context
inventory_equipment when currency/item reward split result occurs
player_sheet if progression reward eventually applies
social_memory if actor promise/payment memory later applies
```

Mapping:

```text
quest rumor/offer/accept/progress/complete/fail/abandon -> quest_log, context
reward promise/ready/paid/denied/disputed -> quest_log, context
currency/item payment split -> quest_log, inventory_equipment, context
```

Dirty targets do not write cards directly.

CardSync remains mirror/dry-run unless later live writes are enabled.

---

# 23. `/quests` updates

`/quests` must display quest and reward states honestly.

Required sections:

```text
Active quests
Offered quests
Rumored quests
Ready to turn in
Completed quests
Failed / abandoned quests
Objectives
Promised rewards
Ready rewards
Paid rewards
Denied / disputed rewards
Warnings
```

Display rules:

```text
rumored quests not listed as active
offered quests not listed as accepted
objective progress not listed as complete
completed quest not listed as rewarded unless reward paid
promised reward not listed as paid
ready reward not listed as paid
paid reward shown once
XP reward displayed as deferred if progression inactive
```

Example:

```text
[AIDRPG] Quests

Active:
- Clear the Mill Wolves
  Objective: Defeat or drive off wolves near Old Mill — in progress
  Reward: 15 gold promised by Old Bren — not paid

Ready to turn in:
- None recorded

Paid rewards:
- None recorded
```

---

# 24. `/inventory` reward reference consistency

`/inventory` should not count promised rewards as currency.

If promised reward exists:

```text
Promised rewards are shown in /quests and do not count as paid currency.
```

If reward is paid and InventoryReducer applied currency:

```text
Currency: Gold 15 confirmed
```

If QuestReducer marks paid but InventoryReducer currency event is deferred due missing split support:

```text
Reward marked paid in quest log, currency update pending/deferred due split-result policy.
```

Do not silently lie.

---

# 25. ContextBuilder quest packet

ContextBuilder should include concise quest facts.

Include:

```text
active quest objectives
ready turn-ins
urgent failed/deadline states
promised vs paid reward status when relevant
```

Do not include:

```text
all old completed quests every turn
rumored quest as active
promised reward as paid
full raw quest JSON
old quarantined quest claims
```

Example packet:

```text
Quest: Clear the Mill Wolves is active. Old Bren promised 15 gold, but it has not been paid. Objective: drive off or defeat the wolves near Old Mill.
```

---

# 26. IntegrityChecker quest/reward checks

Add checks:

```text
active quest records have stable keys
objectives have stable keys and statuses
reward promises and payments are separate
paid reward has paymentOperationKey
paid currency reward has matching inventory/currency key or deferred warning
no duplicate paid reward keys
rumored/offered quests not also active unless accepted key exists
completed quest not automatically rewarded without payment key
manual/card/context quest sources not active truth
```

IntegrityChecker may warn/quarantine bad records.

It may not complete quests or pay rewards.

---

# 27. Cleanup quest/reward protections

Cleanup may compact:

```text
old low-risk quest history entries
old completed quest summaries if archived policy exists
old rejected reward claim audits
quest warnings cap
```

Cleanup must not delete:

```text
active quests
ready-to-turn-in quests
unpaid promised rewards
paid reward ledger keys
objective evidence needed for audit
failed/abandoned important quest history
```

Cleanup must not convert promised rewards into paid rewards or remove duplicate protection keys.

---

# 28. ReleaseCheck gates for this chunk

Add or verify PC07-specific gates:

```text
PC07-G01 QuestRecord schema exists or equivalent stable quest record exists
PC07-G02 ObjectiveRecord schema exists or equivalent objective structure exists
PC07-G03 RewardRecord schema exists or equivalent reward structure exists
PC07-G04 quest status model separates rumored/offered/active/ready/completed/rewarded/failed/abandoned
PC07-G05 objective status model separates progress/completion/failure
PC07-G06 reward status model separates promised/ready/paid/denied/disputed/duplicate_skipped
PC07-G07 QuestObjectiveSanitizer supports broad objective verbs without auto-completion
PC07-G08 RewardNameSanitizer separates promise/ready/payment/denial/dispute
PC07-G09 CanonValidator rejects rumor-only active quest and promise-as-payment
PC07-G10 QuestReducer rejects unsafe routed quest/reward events
PC07-G11 split-owner reward payment contract exists or payment currency mutation is safely deferred
PC07-G12 reward/currency duplicate keys exist
PC07-G13 /quests displays quest/reward states honestly
PC07-G14 /inventory does not show promised rewards as paid currency
PC07-G15 command/context/card quest/reward text cannot become canon
PC07-G16 cleanup does not delete active quests or reward keys
PC07-G17 economy/reputation/domain remain disabled
PC07-G18 player-breaker quest/reward tests registered
```

ReleaseCheck must fail if promised rewards can become currency, command quest output can pay rewards, or duplicate reward payment is possible.

---

# 29. Required tests

## PC07-001 — Quest offer not active

Output:

```text
Old Bren asks you to clear the wolves from the Old Mill.
```

Expected:

```text
quest offered or pending
not active unless accepted
```

---

## PC07-002 — Rumor not active quest

Output:

```text
People say someone should clear the wolves from the Old Mill.
```

Expected:

```text
rumored quest or rumor note
not active
```

---

## PC07-003 — Accept quest active

Input/output:

```text
I accept Old Bren's job to clear the wolves from the Old Mill.
```

Expected:

```text
quest active
objective initialized
operation key added
```

---

## PC07-004 — Objective progress not completion

Output:

```text
You drive one wolf away from the Old Mill, but others remain.
```

Expected:

```text
objective progress updated
objective not complete unless threshold reached
quest not completed
```

---

## PC07-005 — Objective completion

Output:

```text
The last wolf flees, leaving the Old Mill clear.
```

Expected:

```text
clear wolves objective complete if active quest binds
quest ready_to_turn_in or completed according to policy
reward not paid
```

---

## PC07-006 — Completion does not pay reward

Setup:

```text
quest has promised 15 gold reward
objective complete
```

Expected:

```text
currency unchanged
reward still promised/ready, not paid
```

---

## PC07-007 — Reward promise no currency

Output:

```text
Old Bren promises to pay you 15 gold if you clear the wolves.
```

Expected:

```text
reward promised
currency unchanged
```

---

## PC07-008 — Reward ready no currency

Output:

```text
Old Bren says your 15 gold reward is ready.
```

Expected:

```text
reward ready
currency unchanged
```

---

## PC07-009 — Reward payment once

Output:

```text
Old Bren pays you the promised 15 gold.
```

Expected:

```text
reward paid
currency +15 only if split InventoryReducer currency result accepted
payment key added
```

---

## PC07-010 — Reward payment duplicate skipped

Repeat:

```text
Old Bren pays you the promised 15 gold.
```

Expected:

```text
no second payment
reward duplicate skipped/audited
currency not added twice
```

---

## PC07-011 — Reward denied no payment

Output:

```text
Old Bren refuses to pay, claiming you failed the job.
```

Expected:

```text
reward denied/disputed
currency unchanged
```

---

## PC07-012 — Reward disputed no payment

Output:

```text
Old Bren offers only 5 gold instead of the promised 15, disputing the reward.
```

Expected:

```text
reward disputed
currency unchanged unless a separate accepted partial payment event exists
```

---

## PC07-013 — Quest board reward not paid

Output:

```text
The quest board lists a reward of 15 gold for clearing the wolves.
```

Expected:

```text
reward promise/listing only
currency unchanged
```

---

## PC07-014 — Command quests non-canon

Run:

```text
/quests
```

Feed command output through Output hook.

Expected:

```text
no quest/reward/currency mutation
```

---

## PC07-015 — Story Card quest prose non-canon

Story Card text:

```text
Quest Clear the Mill Wolves is completed and paid.
```

Expected:

```text
no quest completion/payment mutation
quarantine/audit only if surfaced
```

---

## PC07-016 — Context quest prose non-canon

Context text:

```text
Old Bren has paid the player 15 gold.
```

Expected:

```text
no currency/reward mutation from context text
```

---

## PC07-017 — Objective verb breadth repair

Input/output:

```text
I accept the job to repair the north wall.
```

Expected:

```text
quest objective Repair the North Wall
verb repair recognized
quest active if accepted
```

---

## PC07-018 — Escort objective preserved

Input/output:

```text
I accept the job to escort Bram to the chapel.
```

Expected:

```text
objective Escort Bram to the Chapel
Bram and Chapel preserved
```

---

## PC07-019 — Ability objective does not grant ability

Quest:

```text
Train Ember Ward until stable.
```

Expected:

```text
quest objective can reference Ember Ward
Ember Ward not made stable by quest text alone
```

---

## PC07-020 — Item objective does not grant item

Quest:

```text
Retrieve the Crown of Dawn.
```

Expected:

```text
quest objective references Crown of Dawn
Crown not added to inventory
```

---

## PC07-021 — Completed quest display honest

State:

```text
quest completed
reward promised but unpaid
```

Run:

```text
/quests
```

Expected:

```text
quest completed or ready
reward shown unpaid/promised or ready, not paid
```

---

## PC07-022 — Paid reward display honest

State:

```text
reward paid
currency event applied
```

Run:

```text
/quests
/inventory
```

Expected:

```text
/quests shows reward paid once
/inventory shows confirmed currency
```

---

## PC07-023 — Cleanup does not delete active quest

Setup:

```text
active quest with objective and promised reward
```

Run cleanup.

Expected:

```text
active quest preserved
promised reward preserved
reward keys preserved
```

---

## PC07-024 — Manual quest commands disabled

Run:

```text
/completequest Clear the Mill Wolves
/payreward
```

Expected:

```text
disabled messages
no quest/reward/currency mutation
```

---

## PC07-025 — Reputation reward deferred

Output:

```text
Old Bren promises the village will honor your name if you clear the wolves.
```

Expected:

```text
favor/reputation_deferred reward promise or note
no reputation automation active
```

---

## PC07-026 — XP reward deferred unless active

Output:

```text
The quest promises 20 XP.
```

Expected:

```text
XP reward promise/deferred
no XP mutation unless ProgressionReducer exact XP reward system active and separately validated
```

---

# 30. Regression placement

Add PC07 tests to regression tiers:

```text
T2 Parser/Proposal/Validator Safety Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T8 Deferred System Disabled Tests
T7 Long-Run Stability Tests
```

Minimum expectation:

```text
26 PC07 tests registered
0 critical PC07 failures
```

---

# 31. State fields added or deepened

Expected additions/deepening:

```text
quests.records
quests.active
quests.offered
quests.rumored
quests.readyToTurnIn
quests.completed
quests.failed
quests.abandoned
quests.rewardLedger
QuestRecord shape
ObjectiveRecord shape
RewardRecord shape
quest status model
objective status model
reward status model
quest/reward history keys
sourceOperationKeys
```

These fields should already be prepared by PC03, but PC07 gives them behavior and display depth.

---

# 32. Parser/proposal/validator changes

Allowed:

```text
quest offer/accept/progress/completion/failure/abandonment detection refinement
reward promise/ready/payment/denial/dispute detection refinement
objective sanitizer breadth
promise/payment separation
operation key suggestions
```

Forbidden:

```text
reputation automation parser
full economy transaction parser
manual quest mutation parser
card/context quest import
quest generation engine
```

---

# 33. Reducer changes

Allowed:

```text
QuestReducer rumor/offer/accept/progress/objective complete/ready/completed/failed/abandoned/reward promise/reward ready/reward payment/reward denied/reward disputed behavior
QuestReducer unsafe routed-data rejection
reward split-result coordination with InventoryReducer
history recording
dirty targets
duplicate skip results
```

Forbidden:

```text
manual complete quest reducer
manual pay reward reducer
XP/stat award unless progression active and validated
reputation/faction mutation
inventory/currency mutation inside QuestReducer except explicit split contract if chosen
```

---

# 34. Command/context/card changes

Allowed:

```text
/quests display depth
/inventory reward reference consistency
/context quest packet
/cardaudit quest mirror labels if dry-run
/releasecheck PC07 gates
/playerbreakercheck quest/reward cases
```

Forbidden:

```text
/completequest active
/payreward active
/acceptquest manual mutation command if it bypasses story validation
live card writes that create quest truth
```

---

# 35. Beginner implementation order

When implementing this chunk in code, follow this order.

## Step 1 — Copy accepted ability build

Start from:

```text
WorldLedgerV0.17.6_ABILITY_DEPTH_ACCEPTED.txt
```

Create working file:

```text
WorldLedgerV0.17.7_QUEST_REWARD_DEPTH_WORKING.txt
```

## Step 2 — Add/verify quest helper functions

Create helpers for:

```text
ensureQuestsShape
makeQuestRecord
makeObjectiveRecord
makeRewardRecord
normalizeQuestStatus
normalizeObjectiveStatus
normalizeRewardStatus
isUnsafeQuestSource
isPaymentConfirmed
isPromiseOnly
isReadyOnly
```

## Step 3 — Deepen QuestObjectiveSanitizer

Support broad objective verbs and preserve important targets/destinations.

## Step 4 — Deepen RewardNameSanitizer

Separate promise, ready, payment, denial, dispute, amount, currency, item, XP, favor/access.

## Step 5 — Update quest/reward CanonValidator rules

Add strict accept/reject/pending logic for:

```text
rumor
offer
acceptance
objective progress
objective completion
quest completion
reward promise
reward ready
reward payment
reward denial/dispute
```

## Step 6 — Update ProposalBuilder quest/reward support

Ensure proposals carry:

```text
quest target
objective target
reward target
giver/payer target
amount/currency/item/XP/favor if parsed
source/localClause/safety flags
operation key suggestion
```

## Step 7 — Update QuestReducerLite

Implement safe commits for:

```text
rumor
offer
acceptance
objective progress
objective completion
ready/completed
failed/abandoned
reward promise/ready/payment/denied/disputed
```

## Step 8 — Coordinate split reward payment with InventoryReducer

Use separate accepted inventory/currency results when possible.

If not ready, defer currency mutation honestly.

## Step 9 — Update `/quests` and `/inventory` reward references

Make displays honest.

## Step 10 — Update ContextBuilder quest packet

Keep it concise and state-accurate.

## Step 11 — Update IntegrityChecker quest/reward checks

Add promise/payment separation, duplicate reward, active quest, objective status, and source checks.

## Step 12 — Add PC07 ReleaseCheck gates

Add PC07-G01 through PC07-G18.

## Step 13 — Add PC07 tests

Add PC07-001 through PC07-026.

## Step 14 — Run baseline checks

Run:

```text
/foundationcheck
/releasecheck
/integrity
/playerbreakercheck
/regressioncheck
/quests
/inventory
```

## Step 15 — Save accepted quest/reward build

Recommended output:

```text
WorldLedgerV0.17.7_QUEST_REWARD_DEPTH_ACCEPTED.txt
```

---

# 36. What this chunk produces

Production Chunk 07 should produce:

```text
WorldLedger V0.17.7 quest/reward-depth accepted script if code changes are made
quest helper depth
QuestReducer state transition behavior
QuestObjectiveSanitizer depth
RewardNameSanitizer depth
quest/reward CanonValidator refinement
/quests display improvement
inventory reward reference consistency
quest context packet
quest/reward integrity checks
PC07 ReleaseCheck gates
PC07 regression/player-breaker tests
quest/reward acceptance report
updated planning zip with Production Chunk 07 added
```

It should not produce:

```text
full economy system
full reputation system
full domain/faction automation
manual quest/reward mutation commands
progression XP automation unless separately active
generated quest cards
live CardSync writes
old card prose quest import
```

---

# 37. Pass / warn / fail criteria

## PASS

Production Chunk 07 passes if:

```text
quest statuses are separated
objective statuses are separated
reward statuses are separated
rumor/offer does not become active automatically
objective progress/completion works without reward payment
reward promise/ready/payment separation works
reward/currency duplicates are prevented
QuestReducer rejects unsafe routed quest/reward events
split-owner payment contract exists or payment mutation is safely deferred
/quests displays quest/reward states honestly
/inventory does not show promised rewards as paid currency
command/context/card quest/reward text cannot become canon
cleanup preserves active quests and reward keys
PC07 tests pass
ReleaseCheck has no PC07 FAIL/BLOCKED gates
```

## WARN

Warn but allow progress if:

```text
XP rewards are deferred
reputation rewards are deferred
favor/access rewards are descriptive only
quest generation is not active
economy integration is deferred
some automatic all-objectives-complete behavior is conservative
```

## FAIL

Fail and stop if:

```text
rumor-only quest becomes active
quest offer becomes accepted without acceptance
objective mention completes objective
quest completion pays reward automatically
reward promise gives currency
reward ready gives currency
reward payment duplicates
command/context/card text completes quest or pays reward
/quests displays promised as paid
/inventory displays promised reward as currency
manual /completequest or /payreward mutates state
QuestReducer trusts unsafe routed data
cleanup deletes active quest or reward keys
```

---

# 38. Handoff to Production Chunk 08

When this chunk passes, Production Chunk 08 begins:

```text
Production Chunk 08 — Actor Presence, Social Memory, Witnessing, and Relationship Depth
```

Production Chunk 08 may use quest giver, payer, witness, and turn-in actor references, but should not turn private quest interactions into public reputation automatically.

Production Chunk 07 must leave quest/reward state reliable enough that future chunks can ask:

```text
Is this quest rumored, offered, active, ready, completed, failed, abandoned, or rewarded?
Which objectives are active, progressing, or complete?
Was the reward promised, ready, paid, denied, or disputed?
Was currency actually added?
Has this reward already been paid?
```

---

# 39. Final doctrine

Production Chunk 07 is where quest truth stops being a loose story note.

Final rule:

```text
Quests have states.
Objectives have states.
Rewards have states.
Promises are not payment.
Completion is not payment.
Payment is duplicate-safe.
Commands, cards, context, rumors, and vague mentions cannot complete quests or pay rewards.
```

Once this passes, the project can safely deepen actors, witnesses, social memory, and relationship state in Production Chunk 08.


====================================================================================================
08_AIDRPG_Production_Chunk_08_Actor_Presence_Social_Memory_Witnessing_and_Relationship_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 08 — Actor Presence, Social Memory, Witnessing, and Relationship Depth

## 0. Document status

**Document name:** AIDRPG Production Chunk 08 — Actor Presence, Social Memory, Witnessing, and Relationship Depth  
**Production sequence:** 08 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.7 quest/reward-depth accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen actor records, actor presence, scene-only actors, reference-only actors, absent actors, witness memory, direct-informed memory, relationship evidence, and social continuity without false presence, duplicate actors, or unsafe reputation automation.  
**Status:** Core gameplay-depth production chunk.  
**Follows:** Production Chunk 07 — Quest, Objective, Reward, and Promise/Payment Depth.  
**This document does not contain final script code.**

Production Chunk 08 exists because actors are where AI Dungeon continuity can either feel alive or become badly corrupted. Old rough attempts showed the same risks repeatedly: duplicate actors from titles/names, absent actors treated as present, reference-only names treated as scene participants, private witness memory becoming global reputation, generic guards becoming permanent major NPCs, command/card/context text creating memories, and actor harm being applied to a statue, painting, title, or training dummy named after someone.

This chunk turns actors into structured, useful, bounded state. The goal is not full reputation, faction, romance, party, summon, or NPC combat automation yet. The goal is reliable actor presence, source-bound memory, and relationship evidence that future systems can safely build on.

---

# 1. Chunk doctrine

```text
Mentioned is not present.
Absent is not present.
Reference-only is not present.
Generic is not automatically persistent.
Witnessed is not global reputation.
Private is not public.
Relationship claim is evidence, not absolute truth.
Actor memory requires presence or direct information.
A training dummy named after Mira is not Mira.
```

Actor truth must be local, source-aware, duplicate-safe, scene-aware, and AI-Dungeon-safe.

---

# 2. Why this chunk comes now

Production Chunk 07 made quest givers, reward payers, turn-ins, and quest references safer. Actors come next because they connect to quest givers, reward payers, witnesses, companions, enemies, merchants, trainers, faction contacts, social memory, relationship changes, scene presence, future reputation, future party/summon logic, and world persistence.

If actors are not safe now, later systems will create false witnesses, false public knowledge, duplicate NPCs, and broken relationship state.

---

# 3. What this chunk is allowed to change

Allowed changes:

```text
Actor schema helpers
ActorReducerLite
ActorNameSanitizer
CanonValidator actor/presence/memory rules
ProposalBuilder actor event types
EventScannerLite actor/social hints
CommandRenderers /actors
ContextBuilder actor/social packets
OperationLedger actor/memory keys
DirtyQueue actor/social targets
IntegrityChecker actor checks
ReleaseCheck actor gates
Regression/player-breaker actor tests
```

Allowed additions:

```text
ActorRecord schema depth
SceneActorRecord schema
ActorMemoryRecord schema
RelationshipEvidenceRecord schema
PresenceStatus model
PersistenceClass model
WitnessScope model
ActorRole model
Alias/title handling
Compound actor suppression rules
Generic actor promotion policy
Social memory audit summaries
```

Forbidden in this chunk:

```text
full reputation automation
faction standing automation
romance system
party/summon control system
merchant economy system
NPC combat AI engine
manual /spawnactor or /setrelationship commands
generated actor Story Cards
old card prose actor import
```

---

# 4. Modules touched

Allowed modules:

```text
ActorState helpers
ActorReducerLite
ActorNameSanitizer
CanonValidator actor rules
ProposalBuilder actor proposals
EventScannerLite actor/social hints
Resolver actor target binding
CommandRenderers: /actors and relevant quest-giver references
ContextBuilderLite actor/social packet
OperationLedger actor/memory keys
DirtyQueue actor dirty targets
AuditLogger actor/social lines
InvariantChecker actor checks
ReleaseCheckLite PC08 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to:

```text
QuestReducer except actor references for giver/payer/turn-in display
InventoryReducer except custody-holder references
AbilityReducer except trainer references only
BodyReducer except NPC injury/death remains conservative
PlaceReducer except current-scene actor references
ProgressionReducer
Reputation/faction/domain systems
Party/summon systems
CardSync live writes
WorldBible import
manual mutation commands
```

---

# 5. Actor state model

Actors bucket should support:

```text
records
sceneActors
present
absent
referenceOnly
memories
relationshipNotes
warnings
notes
```

Recommended shape:

```text
actors: {
  records: {},
  sceneActors: {},
  present: [],
  absent: [],
  referenceOnly: [],
  memories: [],
  relationshipNotes: [],
  warnings: [],
  notes: []
}
```

If existing code uses arrays, keep stable actor keys and helper functions. Do not allow multiple active actor records for the same canonical named actor.

---

# 6. ActorRecord schema

```text
ActorRecord {
  id,
  key,
  displayName,
  normalizedName,
  aliases,
  titles,
  type,
  role,
  importance,
  persistenceClass,
  presenceStatus,
  currentPlaceKey,
  currentSceneKey,
  knownToPlayer,
  relationshipSummary,
  memories,
  relationshipEvidence,
  custodyRefs,
  questRefs,
  sourceOperationKeys,
  createdTurn,
  updatedTurn,
  lastSeenTurn,
  lastMentionedTurn,
  warnings,
  notes
}
```

Rules:

```text
key is stable and family-scoped
key does not contain full raw output
key does not come from command/card/context text
important titles are preserved when identity-relevant
aliases are stored, not automatically separate actors
bad compound/connectors are cleaned without creating fake actors
```

Named/important actors may become persistent. Generic actors normally become scene actors.

Examples:

```text
Mira Vell -> persistent actor
Old Bren -> persistent actor
High Priestess Ilyra -> persistent actor
Veteran Guard -> important scene actor or persistent if repeated/quest-relevant
A guard -> scene actor by default
A merchant -> scene actor by default
Three bandits -> scene group actor by default
```

---

# 7. SceneActorRecord schema

```text
SceneActorRecord {
  id,
  key,
  displayName,
  normalizedName,
  actorKey,
  groupCount,
  role,
  presenceStatus,
  sceneKey,
  placeKey,
  persistenceClass,
  sourceOperationKey,
  createdTurn,
  updatedTurn,
  expiresTurn,
  notes
}
```

Rules:

```text
scene actor may link to persistent ActorRecord if known
scene-only actor does not automatically become permanent
scene actor may expire on scene change through cleanup policy
scene actor presence does not imply global knowledge
```

---

# 8. PresenceStatus model

Allowed statuses:

```text
present
scene_only
nearby
mentioned
reference_only
absent
elsewhere
unknown
left_scene
dead_pending
dead_confirmed
captured
```

Rules:

```text
present means currently in scene or directly interacting
scene_only means present but not durable by default
nearby means nearby, not necessarily interacting
mentioned/reference_only means not present
absent/elsewhere means no witness memory
actor death remains conservative and must not come from rumor/card/context/command text
```

---

# 9. PersistenceClass model

Allowed persistence classes:

```text
player
major
recurring
quest_relevant
scene_only
generic_group
summon_deferred
unknown
```

Generic actors should not become major unless named, repeatedly recurring, connected to a quest/reward/major event, or explicitly promoted by validation.

---

# 10. ActorMemoryRecord schema

```text
ActorMemoryRecord {
  memoryId,
  actorKey,
  memoryType,
  scope,
  confidence,
  summary,
  sourceOperationKey,
  sourceEventId,
  sourceTurn,
  witnessedDirectly,
  informedByActorKey,
  placeKey,
  publicness,
  createdTurn,
  updatedTurn,
  notes
}
```

Memory types:

```text
witnessed_action
heard_report
direct_conversation
quest_promise
quest_payment
threat
help_received
harm_received
relationship_evidence
private_secret
rumor
```

Scopes/publicness:

```text
private_to_actor
shared_with_present_group
public_local
public_faction_deferred
unknown
```

Rules:

```text
present witnesses can receive witnessed_action memory
absent actors do not receive scene memory
directly informed actors can receive heard_report/direct_conversation memory
private memory does not become public reputation
rumor memory is rumor, not confirmed truth
```

---

# 11. RelationshipEvidenceRecord schema

```text
RelationshipEvidenceRecord {
  evidenceId,
  actorKey,
  relationType,
  direction,
  strength,
  confidence,
  summary,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  notes
}
```

Relation types:

```text
friendly
hostile
fearful
respectful
suspicious
grateful
angry
loyal
distrustful
romantic_deferred
rival_deferred
unknown
```

Rules:

```text
relationship evidence is not numeric reputation automation
one nice action does not create permanent loyalty by itself
one private insult does not create public hostility
romance remains deferred unless future spec exists
```

---

# 12. ActorNameSanitizer depth

Must handle:

```text
Veteran Guard -> Veteran Guard, not Guard duplicate
High Priestess Ilyra -> High Priestess Ilyra
Old Bren -> Old Bren
Mira Vell -> Mira Vell
Captain Arlen of the North Gate -> Captain Arlen
three bandits -> bandits group with count 3 if supported
ACTOR:AND_MIRA -> ACTOR:MIRA
Mira and Old Bren -> two actors, not one actor named Mira and Old Bren
```

Must not create present actors from:

```text
training dummy named after Mira
statue of Mira
painting of Old Bren
rumor about the High Priestess
book title containing a name
Story Card/context/command text
```

---

# 13. Compound actor suppression

Examples:

```text
The Veteran Guard blocks the gate.
```

Expected:

```text
one actor: Veteran Guard
not Veteran Guard + Guard
```

```text
High Priestess Ilyra raises her staff.
```

Expected:

```text
one actor: High Priestess Ilyra
not High Priestess + Ilyra separately
```

```text
Mira and Old Bren argue.
```

Expected:

```text
two actors: Mira, Old Bren
not one actor named Mira and Old Bren
```

---

# 14. Actor CanonValidator expansion

Accept present actor only when safe story output places actor in the current scene or direct interaction, actor target is bound, and source is not absent/reference-only/object/statue/painting/dummy/title/dream-only/command/context/card/audit/debug.

Accept scene-only actor when a generic actor or group appears in scene and should not be persistent by default.

Accept absent/reference-only actor when source explicitly says actor is absent/elsewhere/not present, or actor is used only as comparison/title/reference/object label.

Accept memory only when actor is present and directly witnesses event, or actor is directly informed. Reject memory when actor is absent, reference-only, only rumored, not informed, or source is command/card/context/audit/debug.

---

# 15. Actor ProposalBuilder event types

```text
actor.present_candidate
actor.scene_only_candidate
actor.nearby_candidate
actor.absent_candidate
actor.reference_only_candidate
actor.elsewhere_candidate
actor.memory_candidate
actor.relationship_evidence_candidate
actor.status_candidate
actor.death_candidate
actor.custody_reference_candidate
actor.quest_reference_candidate
```

Current chunk may commit present actor, scene-only actor, nearby/reference, absent/reference-only note, memory for present/directly informed actor, relationship evidence, quest/custody references. Actor death/status remains conservative and may stay pending unless directly validated.

---

# 16. EventScannerLite actor/social hints

Detect actor/social hints, but detection is not canon.

Allowed scan families:

```text
named actors
titles and roles
generic people/groups
presence verbs
absence verbs
speech/direct information
witnessing verbs
relationship/emotion cues
custody/holding item references
quest giver/payer/turn-in references
```

Must flag absent, elsewhere, referenceOnly, objectReference, statueOrPainting, dummyOrTrainingTarget, quotedSpeechOnly, rumorOrClaim, dreamOrVision, privateNoWitness, commandSource, contextSource, cardSource, auditSource, compoundActorRisk, genericActor.

---

# 17. ActorReducerLite detailed behavior

ActorReducerLite owns:

```text
actors.records
actors.sceneActors
actors.present
actors.absent
actors.referenceOnly
actors.memories
actors.relationshipNotes
actors.warnings
```

Accepted present actor should create/update ActorRecord or SceneActorRecord, set presence/current scene if known, set lastSeenTurn, mark social_memory/current_location/context dirty, and audit compactly.

Accepted generic actor should create/update SceneActorRecord with scene_only/generic_group persistence and not create major persistent actor unless promotion rule accepts it.

Accepted absent/reference-only should record absent/reference-only status or note, not add to present list, not create witness memory, and not apply scene effects.

Accepted memory should verify actor present or directly informed, create ActorMemoryRecord, set scope/private/publicness correctly, not update public reputation, and mark social_memory/context dirty.

Accepted relationship evidence should create RelationshipEvidenceRecord, update relationshipSummary cautiously, not apply numeric reputation/faction automation, and mark social_memory/context dirty.

---

# 18. ActorReducer defensive guards

Reject/no-op if routed result has unsafe markers:

```text
command source
context source
card source
audit/debug source
absent as present
reference-only as present
statue/painting/dummy as actor presence
training dummy named after actor as actor harm
rumor-only memory as direct memory
private event as public memory
generic actor auto-major without promotion
missing actor key
wrong owner reducer
```

Reducer must not blindly trust upstream.

---

# 19. Witness memory rules

Present witness example:

```text
The Veteran Guard watches you steal the apple.
```

Expected: Veteran Guard gets witnessed_action memory, private/local scope, no global reputation.

Absent actor example:

```text
Mira is not here when you steal the apple.
```

Expected: Mira receives no witness memory.

Directly informed example:

```text
You tell Mira that you stole the apple.
```

Expected: Mira gets heard_report/direct_conversation memory only if present/directly addressed or output confirms contact.

Public group example:

```text
The whole market sees you steal the apple.
```

Expected: scene/public_local memory candidate; reputation/faction numeric automation remains deferred.

---

# 20. Relationship evidence rules

Examples:

```text
Mira smiles warmly after you help her.
Old Bren thanks you for clearing the wolves.
The guard narrows his eyes, suspicious of you.
The bandit swears revenge.
```

Expected: relationship evidence added, relationship summary may update cautiously, no faction/reputation automation.

---

# 21. Actor custody and quest references

Actors may be linked to items and quests as references.

```text
Mira holds the jeweled crown.
Old Bren promises 15 gold.
Captain Arlen gives the gate repair job.
```

ActorReducer may store social/reference memory only. InventoryReducer owns custody truth. QuestReducer owns quest/reward truth. ActorReducer must not add inventory or pay rewards.

---

# 22. Operation keys

```text
actor.present.<actorKey>.<placeOrSceneKey>.<clauseHash>
actor.scene.<actorKey>.<sceneKey>.<clauseHash>
actor.absent.<actorKey>.<clauseHash>
actor.reference.<actorKey>.<referenceKind>.<clauseHash>
actor.memory.<actorKey>.<memoryHash>
actor.relationship.<actorKey>.<relationType>.<sourceHash>
actor.custodyref.<actorKey>.<itemKey>.<sourceHash>
actor.questref.<actorKey>.<questKey>.<sourceHash>
actor.status.<actorKey>.<statusKey>.<clauseHash>
```

Present/scene keys prevent duplicate actors on replay. Memory keys prevent duplicate memory spam. Relationship keys prevent repeated emotional spam. Custody/quest refs do not replace Inventory/Quest owner keys.

---

# 23. Dirty targets

Actor updates should mark social_memory, current_location, context, quest_log when actor reference affects quest display, and inventory_equipment only when paired with InventoryReducer custody truth.

---

# 24. `/actors` updates

Required sections:

```text
Present important actors
Scene-only actors
Known persistent actors
Nearby actors
Absent / elsewhere actors
Reference-only mentions
Recent witness memories
Relationship evidence
Quest / custody references
Warnings
```

Display rules:

```text
absent actors not shown as present
reference-only actors not shown as present
scene-only actors not shown as major persistent unless promoted
private memory not shown as public reputation
relationship evidence not shown as absolute reputation score
```

---

# 25. ContextBuilder actor/social packet

Include present important actors, scene-only actors when scene-relevant, recent witness memories if relevant, important relationship evidence, and quest giver/payer/turn-in actor if relevant. Do not include all known actors every turn, absent actors as present, reference-only actors as present, private memory as public reputation, full raw memory logs, or old quarantined actor claims.

---

# 26. IntegrityChecker actor checks

Checks:

```text
no duplicate actor records with same canonical key
present list only contains present/scene actors
referenceOnly list does not overlap present unless valid transition exists
absent actor not marked present
scene-only actors not promoted without reason
memory records have actorKey and sourceOperationKey
witness memories require present/directly informed source
relationship evidence does not become numeric reputation
actor records do not originate from command/card/context source
```

IntegrityChecker may warn/quarantine bad records. It may not create actors, memories, or relationship changes.

---

# 27. Cleanup actor/social protections

Cleanup may compact low-value scene-only actors after scene expiry policy, duplicate memory warning spam, low-risk relationship evidence summaries, and actor warning caps.

Cleanup must not delete major/recurring/quest-relevant actors, active present actors, important witness memories, quest giver/payer references, custody references needed for inventory display, or relationship evidence needed for future continuity.

---

# 28. ReleaseCheck gates for this chunk

```text
PC08-G01 ActorRecord schema exists or equivalent stable actor record exists
PC08-G02 SceneActorRecord schema exists or equivalent scene actor structure exists
PC08-G03 ActorMemoryRecord schema exists or equivalent memory structure exists
PC08-G04 RelationshipEvidenceRecord schema exists or equivalent evidence structure exists
PC08-G05 presence model separates present/scene_only/nearby/mentioned/reference_only/absent/elsewhere/unknown
PC08-G06 persistence model separates major/recurring/quest_relevant/scene_only/generic_group
PC08-G07 ActorNameSanitizer preserves titles/names and suppresses compound duplicates
PC08-G08 CanonValidator rejects absent/reference-only/object/dummy/card/context actors as present
PC08-G09 ActorReducer rejects unsafe routed actor/memory events
PC08-G10 witness memory requires presence or direct information
PC08-G11 private memory does not become public reputation
PC08-G12 relationship evidence does not activate reputation automation
PC08-G13 actor custody/quest refs do not mutate inventory/quest truth directly
PC08-G14 /actors displays presence/reference/memory honestly
PC08-G15 command/context/card actor text cannot become canon
PC08-G16 cleanup does not delete important actor/memory truth
PC08-G17 reputation/faction/party/summon systems remain disabled
PC08-G18 player-breaker actor/social tests registered
```

ReleaseCheck must fail if absent/reference-only actors can become present, command/card/context actor text can create actors, or private witness memory can become public reputation.

---

# 29. Required tests

```text
PC08-001 Named actor present: Mira Vell steps into tavern -> Mira present persistent record.
PC08-002 Generic guard scene-only: A guard stands by the gate -> scene-only, not major.
PC08-003 Veteran Guard no duplicate Guard.
PC08-004 High Priestess Ilyra preserved as one actor.
PC08-005 Mira and Old Bren split into two actors.
PC08-006 Absent actor not present.
PC08-007 Reference-only actor not present.
PC08-008 Training dummy named Mira does not harm real Mira.
PC08-009 Statue of Old Bren not actor presence.
PC08-010 Present witness receives memory, no global reputation.
PC08-011 Absent actor receives no witness memory.
PC08-012 Directly informed actor memory only if present/addressed/contact confirmed.
PC08-013 Whole market sees theft -> local/public memory candidate, no reputation automation.
PC08-014 Friendly relationship evidence added, not permanent loyalty.
PC08-015 Hostile relationship evidence added, no combat/faction automation.
PC08-016 Quest giver reference does not complete quest.
PC08-017 Custody reference does not grant item.
PC08-018 /actors output is non-canon when fed into Output hook.
PC08-019 Story Card actor prose does not mutate presence/trust.
PC08-020 Context actor prose does not mutate actor memory/reputation.
PC08-021 Duplicate actor presence skipped.
PC08-022 Duplicate memory skipped.
PC08-023 Scene-only actor cleanup preserves major actors/memories.
PC08-024 /actors display separates present/scene-only/reference/absent.
PC08-025 /reputation /faction /setrelationship disabled/no mutation.
PC08-026 /partycontrol /summoncontrol disabled/no actor promotion.
```

Minimum expectation:

```text
26 PC08 tests registered
0 critical PC08 failures
```

---

# 30. Regression placement

Add PC08 tests to:

```text
T2 Parser/Proposal/Validator Safety Tests
T3 Resolver/Sanitizer Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T8 Deferred System Disabled Tests
T7 Long-Run Stability Tests
```

---

# 31. State fields added or deepened

```text
actors.records
actors.sceneActors
actors.present
actors.absent
actors.referenceOnly
actors.memories
actors.relationshipNotes
ActorRecord shape
SceneActorRecord shape
ActorMemoryRecord shape
RelationshipEvidenceRecord shape
presence status model
persistence class model
witness scope model
actor role model
sourceOperationKeys
```

---

# 32. Parser/proposal/validator changes

Allowed: actor presence/absence/reference detection refinement, witness/direct-informed memory detection, relationship evidence detection, compound actor suppression, generic scene-only classification, operation key suggestions.

Forbidden: full reputation/faction parser, party/summon command parser, actor combat AI parser, manual relationship mutation parser, card/context actor import.

---

# 33. Reducer changes

Allowed: ActorReducer present/scene-only/absent/reference-only/memory/relationship evidence behavior, unsafe routed-data rejection, memory logging, dirty targets, duplicate skip results.

Forbidden: public reputation mutation, faction standing mutation, party/summon activation, manual spawn/setrelationship reducer, inventory/quest mutation inside ActorReducer, combat damage automation.

---

# 34. Command/context/card changes

Allowed: /actors display depth, context actor/social packet, cardaudit actor mirror labels if dry-run, releasecheck PC08 gates, playerbreakercheck actor/social cases.

Forbidden: /setrelationship, /spawnactor, /addcompanion, active /reputation, active /faction, live card writes that create actor truth.

---

# 35. Beginner implementation order

```text
1. Copy WorldLedgerV0.17.7_QUEST_REWARD_DEPTH_ACCEPTED.txt.
2. Create WorldLedgerV0.17.8_ACTOR_SOCIAL_DEPTH_WORKING.txt.
3. Add/verify actor helpers: ensureActorsShape, makeActorRecord, makeSceneActorRecord, makeActorMemoryRecord, makeRelationshipEvidenceRecord, normalizePresenceStatus, normalizePersistenceClass, isPresentEnoughForMemory, isReferenceOnlyActorMention, isUnsafeActorSource.
4. Deepen ActorNameSanitizer for names, titles, groups, compounds, connectors, and reference-only names.
5. Update actor CanonValidator rules for present, scene-only, absent, reference-only, witness memory, informed memory, relationship evidence, custody/quest refs.
6. Update ProposalBuilder actor support with target, presence, persistence, group count, memory type/scope, relationship evidence, safety flags, operation key suggestions.
7. Update ActorReducerLite safe commits.
8. Update /actors.
9. Update ContextBuilder actor/social packet.
10. Update IntegrityChecker actor checks.
11. Add PC08-G01 through PC08-G18.
12. Add PC08-001 through PC08-026.
13. Run /foundationcheck /releasecheck /integrity /playerbreakercheck /regressioncheck /actors.
14. Save WorldLedgerV0.17.8_ACTOR_SOCIAL_DEPTH_ACCEPTED.txt.
```

---

# 36. What this chunk produces

```text
WorldLedger V0.17.8 actor/social-depth accepted script if code changes are made
actor helper depth
ActorReducer presence/memory/relationship behavior
ActorNameSanitizer depth
actor CanonValidator refinement
/actors display improvement
actor/social context packet
actor integrity checks
PC08 ReleaseCheck gates
PC08 regression/player-breaker tests
actor/social acceptance report
updated planning zip with Production Chunk 08 added
```

It should not produce full reputation, full faction standing, party/summon control, manual actor/relationship mutation commands, NPC combat AI, generated actor cards, live CardSync writes, or old card prose actor import.

---

# 37. Pass / warn / fail criteria

PASS if actor presence/reference/absence states are separated, named/generic actors are classified safely, compound duplicates are suppressed, reference-only and absent actors do not become present, witness memory requires presence/direct information, private memory does not become public reputation, relationship evidence remains evidence, ActorReducer rejects unsafe routed data, /actors displays honestly, command/context/card actor text cannot become canon, cleanup preserves important actors/memories, PC08 tests pass, and ReleaseCheck has no PC08 FAIL/BLOCKED gates.

WARN if reputation/faction, party/summon, relationship automation, generic actor promotion, scene-only cleanup, or NPC combat/death remain conservative/deferred.

FAIL if absent/reference-only actors become present, training dummy named after Mira harms Mira, Veteran Guard creates duplicate Guard, command/context/card text creates actor/memory, private witness memory becomes public reputation, generic guard becomes permanent major actor without promotion, relationship evidence mutates faction/reputation automation, ActorReducer trusts unsafe routed data, or cleanup deletes major actor/important memory.

---

# 38. Handoff to Production Chunk 09

When this chunk passes, Production Chunk 09 begins:

```text
Production Chunk 09 — Place, Travel, Time, Scene, and Deadline Depth
```

Production Chunk 09 may use actor presence and scene actor state to improve current scene/location display, but should not let location text create false actors or let actor references overwrite location.

---

# 39. Final doctrine

```text
Actors exist in states.
Presence is local.
Memory is source-bound.
Relationships are evidence-based.
Reputation is deferred.
No unsafe text source creates people, witnesses, trust, hatred, or public knowledge.
```

Once this passes, the project can safely deepen places, travel, scenes, time, and deadlines in Production Chunk 09.


====================================================================================================
09_AIDRPG_Production_Chunk_09_Place_Travel_Time_Scene_and_Deadline_Depth.txt
====================================================================================================

# AIDRPG Production Chunk 09 — Place, Travel, Time, Scene, and Deadline Depth

## 0. Document status

**Document name:** AIDRPG Production Chunk 09 — Place, Travel, Time, Scene, and Deadline Depth  
**Production sequence:** 09 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.8 actor/social-depth accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen current location, current scene, travel intent, visible/nearby references, route/interstitial handling, time passage, time claims, deadlines, and age-safety behavior without weak mention overwrites or fake time/age canon.  
**Status:** Core gameplay-depth production chunk.  
**Follows:** Production Chunk 08 — Actor Presence, Social Memory, Witnessing, and Relationship Depth.  
**This document does not contain final script code.**

Production Chunk 09 exists because place and time continuity determine whether AI Dungeon gameplay feels like a real world or a constantly-resetting hallucination.

Old attempts often failed by:

```text
weak place mentions overwriting current location
visible places becoming arrival
travel intent becoming arrival
roads/path/interstitial phrases beating named destinations
context/card/command location text becoming canon
quoted or narrated time skips changing age
long time passage duplicating on output replay
deadlines being forgotten or completed from vague text
scene actors/items not matching current scene after movement
```

This chunk makes location and time persistent, conservative, duplicate-safe, and readable. The goal is not full world simulation yet. The goal is to make current place, current scene, intended travel, known references, time passage, and deadlines safe enough for later world bible, region, faction, encounter, and long-run persistence systems.

---

# 1. Chunk doctrine

```text
Visible is not current location.
Nearby is not arrival.
Travel intent is not arrival.
Road is not destination when a named destination exists.
Route is not scene unless confirmed.
Mentioned place is not current place.
Command location text is not canon.
Context location text is not canon.
Story Card location text is not canon.
Quoted time skip is not time passage.
Dream time skip is not real aging.
Literal time passage is duplicate-sensitive.
Age mutation requires strict policy.
Deadline mention is not completion.
```

Place and time truth must be local, source-aware, duplicate-safe, and conservative.

---

# 2. Why this chunk comes now

Production Chunk 08 made actor presence and scene witnesses safer. Places and time come next because they connect to scene actors, scene items, quest objectives, travel and arrival, deadlines, actor memory location, region pressure, creature encounters, world bible references, CardSync current-location mirrors, context prioritization, and long-run persistence.

If place/time is not safe now, later world systems will misfire. A wolf encounter in the market, a chapel scene overwriting a tavern, or a twenty-year quote aging the player can break the whole game.

---

# 3. What this chunk is allowed to change

Production Chunk 09 may change:

```text
Place schema helpers
Time schema helpers
PlaceReducerLite
TimeReducerLite
PlaceNameSanitizer
CanonValidator place/time rules
ProposalBuilder place/time event types
EventScannerLite place/time hints
CommandRenderers /location and /time
ContextBuilder place/time packets
OperationLedger place/time/deadline keys
DirtyQueue place/time targets
Cleanup scene-expiry shell
IntegrityChecker place/time checks
ReleaseCheck place/time gates
Regression/player-breaker place/time tests
```

Production Chunk 09 may add:

```text
PlaceRecord schema depth
SceneRecord schema
TravelIntentRecord schema
PlaceReferenceRecord schema
RouteReferenceRecord schema
TimeAdvanceRecord schema
TimeClaimRecord schema
DeadlineRecord schema
SceneTransitionRecord schema
arrival-strength model
weak-reference model
age policy shell
scene expiry coordination shell
location/time audit summaries
```

Production Chunk 09 may not add:

```text
full world bible import
full region/faction pressure model
full weather/calendar system
full encounter generator
full travel simulation with pathfinding
full settlement/world structure extraction
manual /teleport or /settime commands
live CardSync writes
old card prose place/time import
```

---

# 4. Modules touched

Allowed modules:

```text
PlaceState helpers
TimeState helpers
PlaceReducerLite
TimeReducerLite
PlaceNameSanitizer
CanonValidator place/time rules
ProposalBuilder place/time proposals
EventScannerLite place/time hints
Resolver place target binding
CommandRenderers: /location and /time
ContextBuilderLite place/time packets
OperationLedger place/time/deadline keys
DirtyQueue place/time dirty targets
AuditLogger place/time lines
InvariantChecker place/time checks
CleanupManagerLite scene expiry shell
ReleaseCheckLite PC09 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to ActorReducer, InventoryReducer, QuestReducer, BodyReducer, ProgressionReducer, WorldBible import, region/faction systems, encounter/creature systems, CardSync live writes, or manual mutation commands.

---

# 5. Place state model

Places bucket should support:

```text
currentLocation
currentScene
records
travelIntent
visibleReferences
nearbyReferences
routeReferences
sceneHistory
warnings
notes
```

Recommended shape:

```text
places: {
  currentLocation: null,
  currentScene: null,
  records: {},
  travelIntent: null,
  visibleReferences: [],
  nearbyReferences: [],
  routeReferences: [],
  sceneHistory: [],
  warnings: [],
  notes: []
}
```

Do not create multiple conflicting current locations.

---

# 6. PlaceRecord schema

Required place record fields:

```text
PlaceRecord {
  id,
  key,
  displayName,
  normalizedName,
  type,
  status,
  regionKey,
  parentPlaceKey,
  knownToPlayer,
  currentStrength,
  lastConfirmedTurn,
  firstDiscoveredTurn,
  sourceOperationKeys,
  references,
  warnings,
  notes
}
```

Place identity rules:

```text
key is stable
key is family-scoped
key does not contain full raw output
key does not contain command/card/context text
key preserves meaningful proper names
key suppresses route/interstitial noise when named destination exists
```

Examples:

```text
The Drunken Huntsman -> drunken_huntsman
Market Lane -> market_lane
Old Mill -> old_mill
Chapel of Ash -> chapel_of_ash
North Gate Road -> north_gate_road only if road is actually the relevant place
```

---

# 7. Place type model

Allowed place types:

```text
room
building
street
road
path
landmark
settlement
region
wilderness
dungeon
interior
exterior
route
threshold
vehicle
unknown
```

Rules:

```text
road/path/route are often interstitial
building/room/street/landmark can be current scene/location if confirmed
region is broader than current scene
settlement is broader than street/building
vehicle can be current location if player is inside/on it
village well is a landmark, not the whole village
market lane is a street/scene, not the whole city
road to chapel is a route, not chapel arrival
chapel visible is a visible reference, not current location
```

---

# 8. SceneRecord schema

Current scene is more immediate than current location.

```text
SceneRecord {
  id,
  key,
  placeKey,
  displayName,
  summary,
  sceneType,
  activeActorKeys,
  sceneItemKeys,
  mood,
  dangerLevel,
  createdTurn,
  updatedTurn,
  sourceOperationKey,
  notes
}
```

Rules:

```text
scene can update without changing location
location can change and create/reset scene
scene summary must be concise
scene actors/items coordinate through DirtyQueue/Cleanup, not direct deletion by PlaceReducer unless explicit policy exists
```

---

# 9. TravelIntentRecord schema

Travel intent is what the player is trying to do, not where they are.

```text
TravelIntentRecord {
  id,
  targetPlaceKey,
  targetDisplayName,
  originPlaceKey,
  routeHint,
  status,
  sourceOperationKey,
  createdTurn,
  updatedTurn,
  notes
}
```

Statuses:

```text
intended
in_progress
interrupted
arrived
cancelled
unknown
```

Rules:

```text
I go to the chapel -> travel intent
I head toward the chapel -> travel intent
You enter the chapel -> arrival
You arrive at the chapel -> arrival
You can see the chapel -> visible reference, not travel intent unless player action says travel
```

---

# 10. PlaceReferenceRecord schema

Weak references must be tracked separately.

```text
PlaceReferenceRecord {
  id,
  placeKey,
  displayName,
  referenceType,
  strength,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  updatedTurn,
  notes
}
```

Reference types:

```text
visible
nearby
mentioned
rumored
known
route_hint
background
memory
unknown
```

Rules:

```text
weak/medium references do not overwrite current location
visible references can appear in /location but not as current place
known references can support future travel target binding
background references should not spam context
```

---

# 11. Route/interstitial model

Routes and interstitials are common in AI Dungeon prose.

Examples:

```text
after the road
along the path
through the alley
past the gate
across the bridge
at the threshold
near the doorway
```

Rules:

```text
route/interstitial can be scene if the narrative stops there
route/interstitial should not beat a named arrival destination
route/interstitial may be stored as routeReference
route/interstitial should not reset destination if arrival is strong
```

Critical example:

```text
After the road, you enter The Drunken Huntsman.
```

Expected:

```text
current location = The Drunken Huntsman
road = route reference
not current location
```

---

# 12. Arrival strength model

Place CanonValidator should score arrival strength.

```text
none
weak_reference
travel_intent
scene_confirmation
strong_arrival
explicit_location_set
```

Examples:

```text
You see the chapel from the market lane -> weak_reference
I head toward the chapel -> travel_intent
The market lane remains crowded -> scene_confirmation
You enter the chapel -> strong_arrival
You arrive at The Drunken Huntsman -> strong_arrival
```

Rules:

```text
strong_arrival can change currentLocation
scene_confirmation can update scene/current location if it matches current state
travel_intent does not change currentLocation
weak_reference does not change currentLocation
explicit_location_set is reserved for safe startup/structured state only, not commands
```

---

# 13. PlaceNameSanitizer depth

PlaceNameSanitizer must preserve named places and suppress route noise.

Must handle:

```text
The Drunken Huntsman -> The Drunken Huntsman
Chapel of Ash -> Chapel of Ash
market lane -> Market Lane
north gate road -> North Gate Road
Old Mill -> Old Mill
road to the chapel -> Chapel target + route road, depending clause
village well -> Village Well, not whole village
```

Must not create current locations from visible chapel, rumored city, book title place, Story Card place, command output location, context reminder, or route phrase when named destination exists.

---

# 14. Time state model

Time bucket should support:

```text
text
turn
sequence
lastAdvance
acceptedAdvances
pendingClaims
deadlines
agePolicy
warnings
notes
```

Rules:

```text
turn/sequence is script metadata, not necessarily in-world time
text can be loose in MVP
time advances require accepted event
age mutation requires stricter age policy
```

---

# 15. TimeAdvanceRecord schema

```text
TimeAdvanceRecord {
  id,
  key,
  amountText,
  normalizedAmount,
  unit,
  literal,
  affectsAge,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  notes
}
```

Units:

```text
moments
minutes
hours
days
weeks
months
years
unknown
```

Rules:

```text
literal accepted time passage can update lastAdvance/text
nonliteral/quote/dream time passage cannot
same output replay cannot duplicate time advance
years/months time passage may be pending for age mutation unless age policy is active
```

---

# 16. TimeClaimRecord schema

Uncertain time claims should be stored separately from accepted advances.

```text
TimeClaimRecord {
  id,
  claimText,
  claimType,
  confidence,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  status,
  notes
}
```

Claim types:

```text
quote
rumor
dream
metaphor
hypothetical
planning
uncertain_literal
calendar_reference
unknown
```

Rules:

```text
pending claim is not accepted time advance
pending claim does not age player
pending claim can be shown in /time if useful
```

---

# 17. DeadlineRecord schema

```text
DeadlineRecord {
  id,
  key,
  title,
  relatedQuestKey,
  relatedActorKey,
  relatedPlaceKey,
  dueText,
  dueTurn,
  status,
  sourceOperationKey,
  createdTurn,
  updatedTurn,
  completedTurn,
  missedTurn,
  notes
}
```

Deadline statuses:

```text
active
pending
completed
missed
cancelled
unknown
```

Rules:

```text
deadline mention creates pending/active deadline only if clear
deadline completion requires accepted completion event
deadline missed requires accepted time/deadline miss event
deadline does not auto-fail quest unless future policy says so
```

---

# 18. Age policy shell

Age updates are high-risk.

Required policy questions:

```text
is player age known?
is player species/race aging model known?
is time passage literal?
is time passage long enough?
is passage duplicate-safe?
is body/identity lock compatible?
is there a reason to update exact age instead of just recording time passage?
```

Rules:

```text
quoted "twenty years pass" does not age player
dream twenty years pass does not age player
narration practice does not age player
literal exile for twenty years may be accepted time passage
exact player age mutation may remain pending if age model incomplete
```

This chunk may record long literal passage without changing exact age unless age policy is implemented.

---

# 19. Place CanonValidator expansion

Accept travel intent when safe input/output says the player heads/goes/travels toward a destination but has not arrived. Result: update travelIntent, currentLocation unchanged.

Accept strong arrival when safe output directly confirms arrival/entry/entering/reaching destination, target is bound, and it is not command/context/card source, dream/quote only, hypothetical, or merely visible/nearby. Result: update currentLocation/currentScene and clear/mark matching travelIntent.

Accept scene confirmation when safe output confirms current scene/location details and target matches current place or is strong current-scene phrase. Result: update scene/confirm location without overwriting from weak reference.

Accept weak reference when safe source mentions visible/nearby/known/background place with no direct arrival. Result: reference record only.

---

# 20. Time CanonValidator expansion

Accept time advance when safe output directly narrates literal time passage, not quote/dream/metaphor/hypothetical/planning, not command/context/card, and operation key is unique.

Reject or pending when time wording is quoted, storytelling/narration practice, metaphorical, planning intent, hypothetical, prophecy, card/context/command text, or dream/vision.

Examples to reject or keep pending:

```text
You say, "Twenty years pass."
You feel like years pass.
In a dream, a century passes.
If a week passed, the town would change.
[AIDRPG] Time: three days passed.
```

Exact age update requires a stricter policy. Default safe result for long passage is accepted/pending time advance with age update pending/deferred unless age policy is active.

---

# 21. ProposalBuilder event types

Place event types:

```text
place.travel_intent_candidate
place.arrival_candidate
place.current_scene_confirm_candidate
place.visible_reference_candidate
place.nearby_reference_candidate
place.route_reference_candidate
place.known_reference_candidate
place.scene_transition_candidate
place.conflict_candidate
```

Time event types:

```text
time.advance_candidate
time.claim_candidate
time.age_candidate
time.deadline_candidate
time.deadline_complete_candidate
time.deadline_missed_candidate
time.rest_candidate
time.calendar_reference_candidate
```

Current chunk may commit travel intent, strong arrival, scene confirmation, visible/nearby/route references, literal time advance, pending/claim records, deadlines, and age pending/deferred status.

---

# 22. EventScannerLite place/time hints

Place hint families:

```text
enter/arrive/reach/step into
head toward/go to/travel to
see/visible/nearby/in the distance
road/path/bridge/gate/doorway/threshold
current scene confirmation phrases
```

Time hint families:

```text
moments pass
minutes/hours/days/weeks/months/years pass
later
at dawn/dusk/night/morning
deadline/due before/by/sunset/tomorrow
rest/sleep/wait/travel time
```

Must flag weakReference, travelIntentOnly, strongArrival, routeInterstitial, namedDestination, quotedSpeech, dreamOrVision, metaphor, hypothetical, planningOnly, commandSource, contextSource, cardSource, auditSource, duplicateRisk, and ageRisk.

Parser cannot mutate place/time state.

---

# 23. PlaceReducerLite behavior

PlaceReducerLite owns:

```text
places.currentLocation
places.currentScene
places.records
places.travelIntent
places.visibleReferences
places.nearbyReferences
places.routeReferences
places.sceneHistory
places.warnings
```

Travel intent commit:

```text
create/update TravelIntentRecord
status intended or in_progress
currentLocation unchanged
mark current_location/context dirty
audit compactly
```

Strong arrival commit:

```text
create/update PlaceRecord
set currentLocation
create/update currentScene
mark matching travelIntent arrived/clear if applicable
record SceneTransitionRecord
coordinate scene actor/item expiry through DirtyQueue/Cleanup shell
mark current_location/context dirty
audit compactly
```

Scene confirmation commit updates currentScene and confirms currentLocation only if target matches current place or is stronger current-scene phrase.

Weak/visible/nearby/reference commit creates a PlaceReferenceRecord without changing currentLocation.

Route/interstitial commit stores routeReference and does not beat a named destination arrival.

---

# 24. TimeReducerLite behavior

TimeReducerLite owns:

```text
time.text
time.turn
time.sequence
time.lastAdvance
time.acceptedAdvances
time.pendingClaims
time.deadlines
time.agePolicy
time.warnings
```

Time advance commit:

```text
check operation key
create TimeAdvanceRecord
update lastAdvance
update time.text if useful
update deadlines only if explicit deadline policy supports it
mark current_location, quest_log, context dirty
audit compactly
```

Time claim commit creates TimeClaimRecord without updating lastAdvance, age, or deadlines.

Deadline create/complete/miss commits update DeadlineRecord and do not auto-complete/fail quests unless QuestReducer receives a separate accepted event or future policy exists.

Age update default policy: pending/deferred unless agePolicy active and all safety checks pass.

---

# 25. Reducer defensive guards

PlaceReducer must reject/no-op if routed result has command/context/card/audit/debug source, visible/nearby as arrival, travel intent as arrival, route phrase overriding named destination, weak mention as current location, dream/quote-only location change, missing place key, or wrong owner reducer.

TimeReducer must reject/no-op if routed result has command/context/card/audit/debug source, quoted time skip, dream time skip, metaphor, hypothetical/planning only, age update without policy, duplicate time advance, missing operation key for repeat-sensitive advance, or wrong owner reducer.

Reducers must not blindly trust upstream.

---

# 26. Operation keys

Place keys:

```text
place.intent.<placeKey>.<inputHash>
place.arrive.<placeKey>.<clauseHash>
place.scene.confirm.<placeKey>.<clauseHash>
place.reference.visible.<placeKey>.<clauseHash>
place.reference.nearby.<placeKey>.<clauseHash>
place.reference.route.<placeKey>.<clauseHash>
place.transition.<fromPlaceKey>.<toPlaceKey>.<clauseHash>
```

Time/deadline keys:

```text
time.advance.<amountKey>.<clauseHash>
time.claim.<claimType>.<claimHash>
time.age.<amountKey>.<clauseHash>
time.deadline.create.<deadlineKey>.<sourceHash>
time.deadline.complete.<deadlineKey>.<sourceHash>
time.deadline.miss.<deadlineKey>.<sourceHash>
```

Rules:

```text
arrival keys prevent repeated scene transitions on replay
time advance keys prevent repeated time passage
deadline keys prevent duplicate deadlines
age keys are durable/high-risk
```

---

# 27. Dirty targets

Place/time updates should mark:

```text
current_location
context
quest_log when deadlines/objective locations are affected
social_memory when present actor scene association changes
inventory_equipment when scene items need expiry/display refresh
card_registry only through mirror dirty, not direct write
```

---

# 28. Scene actor/item coordination shell

When strong arrival changes scene:

```text
PlaceReducer records scene transition
DirtyQueue marks social_memory/current_location/inventory_equipment/context
CleanupManagerLite may expire scene-only actors/items if policy exists
ActorReducer remains owner of actor records
InventoryReducer remains owner of scene item records
```

PlaceReducer must not directly delete actor or inventory records unless a narrow, tested scene-expiry contract exists.

MVP safe behavior:

```text
mark scene transition
let cleanup/support layer expire scene-only records conservatively
preserve persistent actors/items/history
```

---

# 29. `/location` updates

`/location` must display:

```text
Current location
Current scene
Travel intent
Visible references
Nearby references
Route/interstitial references
Known places
Scene transition history summary
Warnings
```

Display rules:

```text
visible place not shown as current
travel intent not shown as arrived
route not shown as destination when named destination exists
unknown current location shown unknown, not guessed
```

Example:

```text
[AIDRPG] Location

Current location:
- Market Lane

Current scene:
- Crowded market street near the old fountain

Travel intent:
- Chapel of Ash — intended destination, not arrived

Visible references:
- Chapel of Ash — visible from Market Lane

Route references:
- North Gate Road — route/interstitial, not destination
```

---

# 30. `/time` updates

`/time` must display:

```text
Current time text
Turn / sequence marker if used
Last accepted time advance
Pending/nonliteral time claims
Deadlines
Age policy status
Warnings
```

Display rules:

```text
command does not advance time
pending claims not shown as accepted advances
age update status shown separately
deadlines not marked complete/missed unless accepted
```

---

# 31. ContextBuilder place/time packets

Place packet should include current location, current scene, travel intent if active, important visible/nearby references, and scene-relevant actors/items already owned by their systems.

Time packet should include current time text if known, recent accepted time advance, urgent deadlines, and age policy warnings only when relevant.

Do not include full place registry, all known places, weak references as current location, pending time claims as accepted passage, hidden lore locations before discovery, or old quarantined place/time claims.

---

# 32. IntegrityChecker place/time checks

Add checks:

```text
only one currentLocation
currentScene has placeKey or unknown-safe state
travelIntent separate from currentLocation
visible/nearby/route references not marked current
route/interstitial not overriding named destination
arrival history operation keys exist
accepted time advances have operation keys
time claims not in acceptedAdvances
age update not applied without policy
active deadlines have stable keys/statuses
command/card/context source not active place/time truth
```

IntegrityChecker may warn/quarantine bad records. It may not teleport the player or advance time.

---

# 33. Cleanup place/time protections

Cleanup may compact old low-value place references, route reference spam, time claim spam, scene history summaries, and resolved deadline warnings if archive policy exists.

Cleanup must not delete currentLocation, currentScene, active travelIntent, active deadlines, accepted time advances needed for audit, important scene transition history, or known major places.

Cleanup must not convert weak references into current location or erase accepted time passage.

---

# 34. ReleaseCheck gates for this chunk

Add or verify PC09-specific gates:

```text
PC09-G01 PlaceRecord schema exists or equivalent stable place record exists
PC09-G02 SceneRecord schema exists or equivalent current scene structure exists
PC09-G03 TravelIntentRecord schema exists or equivalent travel intent structure exists
PC09-G04 PlaceReferenceRecord schema exists or equivalent visible/nearby/route reference structure exists
PC09-G05 TimeAdvanceRecord schema exists or equivalent accepted time advance structure exists
PC09-G06 TimeClaimRecord schema exists or equivalent pending claim structure exists
PC09-G07 DeadlineRecord schema exists or equivalent deadline structure exists
PC09-G08 PlaceNameSanitizer preserves named destinations and suppresses route noise
PC09-G09 CanonValidator rejects weak visible/nearby reference as current location
PC09-G10 CanonValidator separates travel intent from arrival
PC09-G11 CanonValidator rejects quoted/dream/metaphorical/planning time passage
PC09-G12 PlaceReducer rejects unsafe routed place events
PC09-G13 TimeReducer rejects unsafe routed time/age events
PC09-G14 duplicate arrival/time/deadline keys exist
PC09-G15 /location displays current/travel/reference/route states honestly
PC09-G16 /time displays accepted/pending/deadline/age-policy states honestly
PC09-G17 command/context/card place/time text cannot become canon
PC09-G18 cleanup does not delete current location/time/deadlines
PC09-G19 world bible/region/faction/encounter systems remain deferred
PC09-G20 player-breaker place/time tests registered
```

ReleaseCheck must fail if weak place references overwrite current location, travel intent becomes arrival, quoted/dream time changes age, or command/card/context place/time text mutates state.

---

# 35. Required tests

## PC09-001 — Visible chapel no overwrite

Output:

```text
The chapel is visible from the market lane.
```

Expected:

```text
chapel visible reference
currentLocation remains Market Lane if already there
```

## PC09-002 — Nearby place no overwrite

```text
The old mill is nearby beyond the trees.
```

Expected: old mill nearby reference; currentLocation unchanged.

## PC09-003 — Travel intent not arrival

```text
I head toward the chapel.
```

Expected: travelIntent Chapel; currentLocation unchanged.

## PC09-004 — Strong arrival updates location

```text
You enter the chapel.
```

Expected: currentLocation Chapel; matching travelIntent arrived/cleared; currentScene updated.

## PC09-005 — Road loses to named destination

```text
After the road, you enter The Drunken Huntsman.
```

Expected: currentLocation The Drunken Huntsman; road route reference only.

## PC09-006 — Route as scene only when narrative stops there

```text
You stop on the narrow bridge as fog rolls over the river.
```

Expected: bridge may become current scene/location if strong enough; not discarded merely because bridge is route-like.

## PC09-007 — Current scene confirmation

Setup: currentLocation = Market Lane. Output:

```text
The market lane remains crowded and loud.
```

Expected: currentScene confirmed/updated; currentLocation remains Market Lane.

## PC09-008 — Weak mention of distant city no overwrite

```text
Someone mentions the capital far to the east.
```

Expected: capital mentioned/known reference if useful; currentLocation unchanged.

## PC09-009 — Command location non-canon

Run `/location`, feed command output through Output hook. Expected: no location/travel/reference mutation.

## PC09-010 — Story Card location prose non-canon

Story Card text:

```text
The player is currently in the Chapel of Ash.
```

Expected: no currentLocation mutation; quarantine/audit only if surfaced.

## PC09-011 — Context location prose non-canon

Context text:

```text
Current location: The Drunken Huntsman.
```

Expected: no currentLocation mutation.

## PC09-012 — Literal night passes

```text
The night passes uneventfully while you rest.
```

Expected: accepted time advance; lastAdvance updated; no healing unless separate recovery event accepted.

## PC09-013 — Rest intent no time advance

```text
I plan to rest until morning.
```

Expected: intent only; no accepted time advance until output confirms.

## PC09-014 — Quote time skip rejected

```text
You say, "Twenty years pass," while practicing narration.
```

Expected: no accepted time advance; no age update.

## PC09-015 — Dream time skip rejected

```text
In a dream, a century passes before you wake.
```

Expected: no accepted real time advance; no age update.

## PC09-016 — Metaphorical years rejected

```text
The wait feels like years.
```

Expected: no literal time advance; possible metaphor claim only.

## PC09-017 — Literal long passage age cautious

```text
Twenty years pass during your exile.
```

Expected: literal time advance accepted or pending by safety mode; exact age update pending/deferred unless age policy active.

## PC09-018 — Duplicate time advance skipped

Repeat night-passes output. Expected: time advance not duplicated; operation key skip/audit.

## PC09-019 — Deadline creation

```text
Old Bren says the wolves must be cleared before sunset.
```

Expected: deadline active/pending linked if bound; quest not failed/completed automatically.

## PC09-020 — Deadline completion explicit

```text
You return before sunset, meeting Old Bren's deadline.
```

Expected: deadline completed if bound; quest/reward changes only through separate accepted events.

## PC09-021 — Deadline missed explicit

```text
Sunset passes, and you miss Old Bren's deadline.
```

Expected: deadline missed if bound; quest failure only if separate event/policy applies.

## PC09-022 — Command time non-canon

Run `/time`, feed command output through Output hook. Expected: no time/deadline/age mutation.

## PC09-023 — Story Card time prose non-canon

Story Card text:

```text
Ten years have passed since the player entered town.
```

Expected: no accepted time advance; no age update; quarantine/audit only if surfaced.

## PC09-024 — Scene transition marks dirty

Strong arrival occurs. Expected: dirty targets include current_location and context; social_memory/inventory_equipment marked for scene actor/item refresh if applicable.

## PC09-025 — Location display honest

State: currentLocation Market Lane, travelIntent Chapel of Ash, visibleReference Chapel of Ash, routeReference North Gate Road. `/location` should show Market Lane current, Chapel as travel/visible not current, Road as route.

## PC09-026 — Time display honest

State: accepted night passed, pending quoted twenty years, active before-sunset deadline, cautious age policy. `/time` should separate all of these.

## PC09-027 — Cleanup preserves current place/time/deadlines

Cleanup must preserve currentLocation, currentScene, active deadlines, and accepted time advances while compacting only low-value old references.

---

# 36. Regression placement

Add PC09 tests to regression tiers:

```text
T2 Parser/Proposal/Validator Safety Tests
T3 Resolver/Sanitizer Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T7 Long-Run Stability Tests
T8 Deferred System Disabled Tests
```

Minimum expectation:

```text
27 PC09 tests registered
0 critical PC09 failures
```

---

# 37. State fields added or deepened

Expected additions/deepening:

```text
places.currentLocation
places.currentScene
places.records
places.travelIntent
places.visibleReferences
places.nearbyReferences
places.routeReferences
places.sceneHistory
PlaceRecord shape
SceneRecord shape
TravelIntentRecord shape
PlaceReferenceRecord shape
SceneTransitionRecord shape
time.text
time.lastAdvance
time.acceptedAdvances
time.pendingClaims
time.deadlines
time.agePolicy
TimeAdvanceRecord shape
TimeClaimRecord shape
DeadlineRecord shape
```

---

# 38. Parser/proposal/validator changes

Allowed: place travel/arrival/reference/route detection refinement, time advance/claim/deadline detection refinement, PlaceNameSanitizer route/named destination handling, quoted/dream/metaphor/planning time rejection, and operation key suggestions.

Forbidden: world bible import parser, region/faction pressure parser, encounter generator parser, manual teleport/time mutation parser, and card/context place/time import.

---

# 39. Reducer changes

Allowed: PlaceReducer travel intent/arrival/scene confirmation/weak reference/route behavior; TimeReducer time advance/time claim/deadline behavior; unsafe routed-data rejection; dirty targets; duplicate skip results.

Forbidden: region/faction mutation, world event simulation, random encounter generation, manual teleport/settime reducer, age update without policy, and quest failure/payment from deadline unless separately validated.

---

# 40. Command/context/card changes

Allowed:

```text
/location display depth
/time display depth
/context place/time packet
/cardaudit place/time mirror labels if dry-run
/releasecheck PC09 gates
/playerbreakercheck place/time cases
```

Forbidden:

```text
/teleport
/setlocation
/settime
/advanceyears
/manual deadline complete command
live card writes that create place/time truth
```

---

# 41. Beginner implementation order

1. Copy accepted actor/social build: `WorldLedgerV0.17.8_ACTOR_SOCIAL_DEPTH_ACCEPTED.txt`.
2. Create working file: `WorldLedgerV0.17.9_PLACE_TIME_DEPTH_WORKING.txt`.
3. Add/verify place/time helpers: `ensurePlacesShape`, `ensureTimeShape`, `makePlaceRecord`, `makeSceneRecord`, `makeTravelIntentRecord`, `makePlaceReferenceRecord`, `makeTimeAdvanceRecord`, `makeTimeClaimRecord`, `makeDeadlineRecord`, `normalizeArrivalStrength`, `normalizePlaceType`, `isWeakPlaceReference`, `isStrongArrival`, `isUnsafePlaceSource`, and `isUnsafeTimeSource`.
4. Deepen PlaceNameSanitizer for named destinations, routes, visible/nearby references, and landmark precision.
5. Update place/time CanonValidator rules for travel intent, arrival, scene confirmation, weak references, literal time advance, nonliteral claims, deadlines, and cautious age policy.
6. Update ProposalBuilder to carry place target, event strength, reference type, route/named destination info, time amount/unit/literal flag, deadline target, safety flags, and operation key suggestions.
7. Update PlaceReducerLite for travel intent, strong arrival, scene confirmation, weak references, route references, and scene transitions.
8. Update TimeReducerLite for literal time advance, claims, deadlines, and age pending/deferred policy.
9. Update `/location` and `/time` displays.
10. Update ContextBuilder place/time packets.
11. Update IntegrityChecker place/time checks.
12. Add PC09-G01 through PC09-G20 ReleaseCheck gates.
13. Add PC09-001 through PC09-027 tests.
14. Run `/foundationcheck`, `/releasecheck`, `/integrity`, `/playerbreakercheck`, `/regressioncheck`, `/location`, and `/time`.
15. Save accepted build as `WorldLedgerV0.17.9_PLACE_TIME_DEPTH_ACCEPTED.txt`.

---

# 42. What this chunk produces

Production Chunk 09 should produce:

```text
WorldLedger V0.17.9 place/time-depth accepted script if code changes are made
place/time helper depth
PlaceReducer travel/arrival/reference behavior
TimeReducer time/deadline behavior
PlaceNameSanitizer depth
place/time CanonValidator refinement
/location and /time display improvement
place/time context packets
place/time integrity checks
PC09 ReleaseCheck gates
PC09 regression/player-breaker tests
place/time acceptance report
updated planning zip with Production Chunk 09 added
```

It should not produce full world bible import, full region/faction model, encounter generator, manual teleport/settime commands, calendar/weather engine, age mutation without strict policy, live CardSync writes, generated place cards, or old card prose place/time import.

---

# 43. Pass / warn / fail criteria

## PASS

Production Chunk 09 passes if:

```text
current location/current scene/travel intent/references/routes are separated
weak visible/nearby mentions do not overwrite location
travel intent does not equal arrival
strong arrival updates location safely
road/interstitial does not beat named destination
literal time advances are duplicate-safe
quoted/dream/metaphorical/planning time does not advance time or age
age updates are cautious/pending unless policy active
deadlines are stateful and do not auto-complete/fail quests without rules
PlaceReducer and TimeReducer reject unsafe routed events
/location and /time display honestly
command/context/card place/time text cannot become canon
cleanup preserves current location/time/deadlines
PC09 tests pass
ReleaseCheck has no PC09 FAIL/BLOCKED gates
```

## WARN

Warn but allow progress if exact calendar system is deferred, weather/time-of-day details are loose, age mutation is pending/deferred, region/faction/world pressure systems are deferred, scene-only actor/item expiry is conservative, or long-route travel simulation is not active.

## FAIL

Fail and stop if visible chapel becomes current location, heading toward chapel becomes arrival, road beats The Drunken Huntsman, command/context/card location text changes currentLocation, quoted or dream time skip advances time/age, repeated time passage duplicates, rest intent advances time without output confirmation, deadline mention completes/fails quest automatically, cleanup deletes current location/current scene/active deadline, or PlaceReducer/TimeReducer trusts unsafe routed data.

---

# 44. Handoff to Production Chunk 10

When this chunk passes, Production Chunk 10 begins:

```text
Production Chunk 10 — Formula Matrix and Progression Baseline
```

Production Chunk 10 may use time and place context for training, rest, travel, and progression pacing, but it must not turn every time passage into XP or every region into world scaling.

Production Chunk 09 must leave place/time state reliable enough that future chunks can ask:

```text
Where is the player now?
What is the current scene?
Where is the player trying to go?
What places are only visible or nearby?
Did time literally pass?
Was time only mentioned, quoted, dreamed, or planned?
Are deadlines active, completed, or missed?
Can age safely update, or should it stay pending?
```

---

# 45. Final doctrine

Production Chunk 09 is where the world stops sliding under the player’s feet.

Final rule:

```text
A place must be arrived at before it becomes current.
A time skip must be literal before it changes time.
A deadline must be explicit before it matters.
Age must be protected.
Weak references, command text, context text, card prose, dreams, quotes, routes, and plans cannot rewrite reality.
```

Once this passes, the project can safely begin progression and formula baseline work in Production Chunk 10.


====================================================================================================
10_AIDRPG_Production_Chunk_10_Formula_Matrix_and_Progression_Baseline.txt
====================================================================================================

# AIDRPG Production Chunk 10 — Formula Matrix and Progression Baseline

## 0. Document status

**Document name:** AIDRPG Production Chunk 10 — Formula Matrix and Progression Baseline  
**Production sequence:** 10 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.9 place/time-depth accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen XP, level, progression events, formula registry, progression history, stat-baseline placeholders, and world-scaling boundaries without vague XP, duplicate XP, hidden rubber-band scaling, or unsafe stat automation.  
**Status:** Core systems production chunk.  
**Follows:** Production Chunk 09 — Place, Travel, Time, Scene, and Deadline Depth.  
**This document does not contain final script code.**

Production Chunk 10 exists because progression is where many RPG systems silently become unfair, inconsistent, or impossible to test.

Old attempts and rough builds often risked:

```text
vague “learning experience” becoming XP
ability training awarding XP automatically
quest reward promises granting XP before payment/completion
repeat outputs duplicating XP/levels
level formulas changing mid-save
stats increasing without owner/rule/test support
world enemies rubber-banding to player level
high-level numbers becoming meaningless
manual /setlevel shortcuts corrupting balance
```

This chunk creates the formula and progression baseline.

It does **not** build the full final stat engine yet.

It makes progression deterministic, source-bound, duplicate-safe, inspectable, and ready for Production Chunk 11.

---

# 1. Chunk doctrine

```text
Vague experience is not XP.
Training is not XP unless an exact XP event says so.
Quest promise is not XP.
Quest completion is not XP unless reward/XP event is accepted.
Level does not change without formula authority.
Stats do not change without owner rules.
World threat does not rubber-band to player level.
Duplicate output cannot duplicate XP.
Formula changes must be versioned.
```

Progression truth must be formula-driven, versioned, repeat-safe, and explainable.

---

# 2. Why this chunk comes now

Production Chunk 09 made place/time safer.

Progression comes next because later chunks depend on it:

```text
stats and derived values
body resources and conditions
combat damage/healing
ability reliability and costs
world-threat benchmarks
creature/encounter difficulty
race/form consequences
quest rewards
long-run advancement
```

If progression is not locked before stats and combat, later code will invent numbers ad hoc.

---

# 3. What this chunk is allowed to change

Production Chunk 10 may change:

```text
Progression schema helpers
ProgressionReducerLite
FormulaRegistry / FormulaMatrix shell
CanonValidator progression/XP rules
ProposalBuilder progression event types
EventScannerLite progression/XP hints
CommandRenderers /sheet and progression/status display
ContextBuilder progression packet
OperationLedger XP/level/progression keys
DirtyQueue progression targets
IntegrityChecker progression checks
ReleaseCheck progression gates
Regression/player-breaker progression tests
```

Production Chunk 10 may add:

```text
FormulaProfile schema
FormulaRuleRecord schema
XPEventRecord schema
LevelEventRecord schema
ProgressionHistoryRecord schema
ProgressionMilestoneRecord schema
WorldScalingBoundaryRecord schema
explicit XP validation rules
level calculation boundary
formula versioning
no-rubber-band world rule
progression audit summaries
```

Production Chunk 10 may not add:

```text
full stat/derived value engine
full level 1–1000 final balancing matrix unless Formula Appendix locks it
full combat XP automation
full class/talent tree
ability XP economy
manual /setlevel /givexp commands
reputation/faction progression
world enemy auto-scaling
absorption/evolution stat gain
```

Stats and derived values are Production Chunk 11. Combat is Production Chunk 12. World threat benchmark is Production Chunk 14.

---

# 4. Modules touched

Allowed modules:

```text
ProgressionState helpers
ProgressionReducerLite
FormulaRegistry / FormulaMatrix helper
CanonValidator progression rules
ProposalBuilder progression proposals
EventScannerLite XP/progression hints
Resolver progression target binding
CommandRenderers: /sheet, /status if needed, /progression if supported
ContextBuilderLite progression packet
OperationLedger progression/XP/level keys
DirtyQueue progression dirty targets
AuditLogger progression lines
InvariantChecker progression checks
ReleaseCheckLite PC10 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to:

```text
BodyReducer except resource/stat interactions remain deferred
AbilityReducer except no-direct-XP guard
QuestReducer except XP reward split/deferred contract
InventoryReducer
ActorReducer
PlaceReducer
TimeReducer except time passage does not grant XP automatically
Combat systems
World threat/encounter systems beyond no-rubber-band boundary
manual mutation commands
```

---

# 5. Progression state model

Progression bucket should support:

```text
level
currentXP
totalXP
xpToNext
stats
baseStats
modifiers
derivedSnapshot
statXP
levelHistory
xpHistory
formulaProfile
formulasEnabled
milestones
warnings
notes
```

Recommended shape:

```text
progression: {
  level: 1,
  currentXP: 0,
  totalXP: 0,
  xpToNext: null,
  stats: {},
  baseStats: {},
  modifiers: [],
  derivedSnapshot: {},
  statXP: {},
  levelHistory: [],
  xpHistory: [],
  formulaProfile: null,
  formulasEnabled: false,
  milestones: [],
  warnings: [],
  notes: []
}
```

If the current script already tracks progression differently, this chunk should normalize or bridge without deleting valid data.

Do not invent high-level stats if formulas are not active.

---

# 6. FormulaProfile schema

FormulaProfile controls which progression formulas are active.

```text
FormulaProfile {
  profileId,
  profileName,
  version,
  source,
  active,
  locked,
  xpCurveId,
  levelCap,
  statFormulaSetId,
  derivedFormulaSetId,
  worldScalingPolicyId,
  createdTurn,
  updatedTurn,
  warnings,
  notes
}
```

Rules:

```text
active formula profile must be explicit
locked profile cannot change silently mid-save
formula source must be known: Formula Appendix, setup, or current build default
changing formula profile requires migration/audit
if formula profile missing, XP can be tracked but level-up may be pending/deferred
```

---

# 7. FormulaRuleRecord schema

Formula rules should be inspectable.

```text
FormulaRuleRecord {
  ruleId,
  family,
  name,
  version,
  expressionType,
  expressionText,
  enabled,
  source,
  inputs,
  outputs,
  caps,
  clamps,
  roundingPolicy,
  warnings,
  notes
}
```

Families:

```text
xp_curve
level
base_stat
derived_stat
resource
condition_modifier
ability_modifier
world_threat_boundary
unknown
```

Rules:

```text
formula rule must declare inputs and outputs
formula rule must not mutate state directly
formula evaluator returns computed result only
reducers decide whether state changes
```

---

# 8. XPEventRecord schema

XP events must be explicit and duplicate-safe.

```text
XPEventRecord {
  id,
  key,
  amount,
  sourceType,
  sourceKey,
  reason,
  relatedQuestKey,
  relatedAbilityKey,
  relatedActorKey,
  relatedPlaceKey,
  operationKey,
  acceptedTurn,
  applied,
  duplicateOf,
  warnings,
  notes
}
```

Allowed source types:

```text
explicit_story_xp
quest_reward_xp
structured_startup
system_award_later
manual_deferred
unknown
```

Rules:

```text
amount must be numeric and positive
source must be safe
operationKey must exist
vague experience is rejected
training does not grant XP unless exact XP event exists
quest promise does not grant XP
command/card/context text cannot grant XP
```

---

# 9. LevelEventRecord schema

Level changes must be formula-authorized.

```text
LevelEventRecord {
  id,
  key,
  fromLevel,
  toLevel,
  totalXPAtLevel,
  formulaProfileId,
  operationKey,
  acceptedTurn,
  warnings,
  notes
}
```

Rules:

```text
level cannot decrease unless a future special spec supports it
level cannot skip unexpectedly unless formula says totalXP crosses multiple thresholds
level-up requires active/locked formula profile or explicit safe policy
level-up does not auto-assign stats unless PC11 supports it
```

---

# 10. ProgressionHistoryRecord schema

```text
ProgressionHistoryRecord {
  historyId,
  eventType,
  summary,
  sourceOperationKey,
  relatedKeys,
  turn,
  notes
}
```

Event types:

```text
xp_gain
level_up
formula_profile_set
formula_profile_warning
xp_rejected
xp_duplicate_skipped
progression_deferred
```

History should be compact and capped/summarized later, but durable XP/level operation keys must remain protected.

---

# 11. ProgressionMilestoneRecord schema

Milestones allow story-relevant progression without messy stat changes.

```text
ProgressionMilestoneRecord {
  id,
  key,
  title,
  family,
  status,
  sourceOperationKey,
  relatedAbilityKey,
  relatedQuestKey,
  createdTurn,
  notes
}
```

Examples:

```text
first confirmed XP gain
first level-up
first stabilized ability
first major quest completion
survived first deadly injury
```

Milestones do not trigger hidden rewards unless future rules say so.

---

# 12. WorldScalingBoundaryRecord schema

This chunk must explicitly prevent rubber-band scaling.

```text
WorldScalingBoundaryRecord {
  id,
  policyName,
  enabled,
  rule,
  worldLevelMode,
  playerScalingAllowed,
  regionThreatAuthority,
  creatureThreatAuthority,
  notes
}
```

Required policy:

```text
No automatic rubber-band scaling.
```

Rules:

```text
player level does not automatically change all enemies
region/creature/world bible threat authority controls later threat modeling
low-level player can encounter high-threat region if story/world supports it
high-level player does not make all rats legendary
world threat benchmark comes later in PC14
```

---

# 13. Formula activation levels

## 13.1 Stage 0 — XP tracked, level pending

Allowed if formula profile missing.

```text
XPEventRecord can be stored
level does not update automatically
ReleaseCheck warns formula profile missing
```

## 13.2 Stage 1 — Simple locked XP curve

Allowed if formula profile exists.

```text
numeric XP events update totalXP/currentXP
level calculated from locked formula/table
xpToNext computed
level history recorded
```

## 13.3 Stage 2 — Stat formulas

Deferred to PC11 unless Formula Appendix and tests are fully integrated.

## 13.4 Stage 3 — World threat formulas

Deferred to PC14.

---

# 14. XP source rules

Accepted XP sources:

```text
safe output explicitly says “You gain 20 XP.”
validated quest reward payment/completion produces exact XP reward and ProgressionReducer is active
structured startup exact XP if before lock and source is safe
future combat reward if Combat Event Model supports it
future system award if owner/rule/test/gate exists
```

Rejected XP sources:

```text
You learn from the experience.
This was a valuable experience.
You feel stronger.
You are more skilled now.
The quest promises 20 XP.
The quest board lists 20 XP.
[AIDRPG] XP: +20.
Context says you gained 20 XP.
Story Card says the player is level 10.
```

Pending/deferred XP sources:

```text
quest promises XP but not paid/completed
teacher says training will make you stronger
long training montage without exact XP
combat victory without XP rule active
```

---

# 15. XP and ability training separation

Ability training and XP are separate systems.

```text
I train Ember Ward all afternoon.
```

Expected:

```text
ability training evidence may update
XP unchanged
```

```text
After training, you gain 10 XP.
```

Expected:

```text
ability evidence may update if ability target exists
XPEventRecord +10 only if safe output and exact amount accepted
```

Rules:

```text
trainingCount does not equal XP
useCount does not equal XP
ability stabilization does not grant XP unless separate accepted XP event exists
```

---

# 16. XP and quest reward separation

Quest reward handling was locked in PC07.

Rules:

```text
XP reward promise does not grant XP
XP reward ready does not grant XP unless policy explicitly says ready applies; default no
XP reward paid/awarded can grant XP only once through ProgressionReducer
quest completion alone does not grant XP unless validated XP reward event exists
```

Preferred split result:

```text
reward.payment_candidate -> QuestReducer marks reward paid
xp.reward_candidate -> ProgressionReducer adds XP if exact and active
```

Both share reward payment/XP key.

---

# 17. XP and combat separation

Combat is not fully active until PC12.

Rules:

```text
defeating enemy does not automatically award XP in PC10
combat XP remains pending/deferred unless exact XP output occurs
future Combat Event Model may emit explicit XP result if spec allows
```

---

# 18. Level formula requirements

If level automation is active, it must be formula-driven.

```text
level is derived from totalXP or currentXP according to locked profile
xpToNext computed from next threshold
levelHistory records level-ups
level-up is duplicate-safe
level-up does not auto-assign stats unless PC11 enables it
```

If formula is not locked:

```text
XP tracked
level-up pending/deferred
ReleaseCheck WARN, not fake PASS as full progression
```

---

# 19. Stat automation boundary

PC10 prepares stat baseline but does not fully implement PC11.

Allowed:

```text
baseStats placeholder
modifiers placeholder
derivedSnapshot placeholder
statXP placeholder
formula rule records for stat family disabled
/stat or /sheet display shows stat automation deferred/limited
```

Forbidden:

```text
random STR/DEX/INT increases from training
HP max increases from level without formula
ability use increasing stats automatically
world threat scaling from stats
manual stat commands
```

---

# 20. Progression CanonValidator expansion

CanonValidator must enforce XP/progression safety.

## 20.1 Accept XP gain when

All are true:

```text
source is safe story output or safe structured startup
local clause explicitly says exact numeric XP gained/awarded/received
target is progression/XP, not vague experience
amount is positive number
not promised/ready/listed/deferred only
not command/context/card/audit/debug source
operation key not duplicate
```

## 20.2 Reject XP gain when

Any are true:

```text
vague experience
feels stronger
training only
quest promise only
quest board listing only
combat victory without XP event
output command/debug/context/card source
negative or nonnumeric amount
manual command
```

## 20.3 Accept level-up when

```text
formula profile active and XP threshold crossed
or safe structured startup explicitly sets level before lock
or future owner/rule/test/gate supports level change
```

## 20.4 Reject level-up when

```text
Story Card says level 10
command says level 10
context says level 10
NPC boasts you are level 10
manual /setlevel in MVP
formula profile missing and no safe structured source
```

---

# 21. Progression ProposalBuilder event types

Add or verify event types:

```text
progression.xp_gain_candidate
progression.xp_reward_candidate
progression.xp_rejected_candidate
progression.level_up_candidate
progression.level_set_candidate
progression.formula_profile_candidate
progression.stat_gain_deferred_candidate
progression.milestone_candidate
progression.world_scaling_boundary_candidate
```

Current chunk may commit safe:

```text
exact XP gain
XP reward if exact and split result active
formula profile set/lock if source is safe
level-up if formula active
milestone records
world scaling boundary record
```

Stat gains remain deferred to PC11 unless explicitly scoped.

---

# 22. EventScannerLite progression hints

EventScannerLite may detect progression hints, but detection is not canon.

Allowed scan families:

```text
XP gained/awarded/received
level up/reach level
experience points
reward XP
formula profile/setup progression
feel stronger/valuable experience as rejected/vague hints
```

Must flag:

```text
exactNumericXP
vagueExperience
promiseOnly
readyOnly
trainingOnly
combatOnly
commandSource
contextSource
cardSource
auditSource
manualMutation
negativeAmount
nonnumericAmount
duplicateRisk
formulaRequired
```

Parser cannot mutate progression.

---

# 23. ProgressionReducerLite detailed behavior

ProgressionReducerLite owns:

```text
progression.level
progression.currentXP
progression.totalXP
progression.xpToNext
progression.levelHistory
progression.xpHistory
progression.formulaProfile
progression.milestones
progression.warnings
```

## 23.1 XP gain commit

Accepted XP gain should:

```text
1. Confirm validation accepted.
2. Confirm safe source and exact positive amount.
3. Confirm operation key.
4. If duplicate, return skipped_duplicate.
5. Add XPEventRecord.
6. Increase totalXP/currentXP according to formula profile.
7. Add operation key.
8. Check level-up only if formula profile active.
9. Mark progression/player_sheet/context dirty.
10. Audit compactly.
```

## 23.2 Level-up commit

If formula profile active and threshold crossed:

```text
calculate new level
create LevelEventRecord
update level/xpToNext
record levelHistory
mark player_sheet/progression/context dirty
```

Do not assign stats automatically in PC10.

## 23.3 Formula profile commit

Accepted formula profile setup should:

```text
set formulaProfile
set formulasEnabled according to profile
lock profile if safe
record FormulaRuleRecords or profile reference
write audit/migration note
```

Do not silently change locked profile.

## 23.4 Milestone commit

Accepted milestone should:

```text
create ProgressionMilestoneRecord
not grant stats/XP unless separate accepted event exists
```

---

# 24. ProgressionReducer defensive guards

ProgressionReducer must reject/no-op if routed result has unsafe markers:

```text
command source
context source
card source
audit/debug source
vague experience
training-only XP
quest promise XP
quest ready XP unless policy active
combat-only XP without XP event
manual mutation command
negative XP
nonnumeric XP
level set without formula/startup authority
stat gain without PC11 support
world scaling mutation
missing operation key
wrong owner reducer
```

Reducer must not blindly trust upstream.

---

# 25. Operation keys

Required progression operation keys:

```text
xp.gain.<amount>.<sourceKey>.<clauseHash>
xp.reward.<amount>.<questKey>.<rewardPaymentKey>
xp.startup.<amount>.<startupKey>
level.up.<fromLevel>.<toLevel>.<totalXP>.<formulaProfileId>
level.set.startup.<level>.<startupKey>
formula.profile.set.<profileId>.<version>.<sourceKey>
formula.profile.lock.<profileId>.<version>
progression.milestone.<milestoneKey>.<sourceHash>
world.scaling.boundary.<policyId>.<version>
```

Rules:

```text
XP and level keys are durable
formula profile keys are durable
recentKeys must not delete durable progression keys
same XP output replay cannot duplicate XP
same level threshold cannot add duplicate level history
```

---

# 26. Dirty targets

Progression updates should mark:

```text
player_sheet
progression
context
quest_log if XP reward is tied to quest
body_status only later if stat/resource formulas affect body in PC11
```

---

# 27. `/sheet` and progression display updates

`/sheet` should show progression honestly.

Required progression display fields:

```text
level
current XP
total XP
XP to next if formula active
formula profile/status
stat automation status
world scaling policy summary
warnings/deferred notes
```

Display rules:

```text
if formula profile missing, say level automation pending/deferred
if XP is tracked but level not calculated, say so
if stats are deferred, say stat automation deferred to PC11
if world scaling is disabled, say no automatic rubber-band scaling
```

---

# 28. Optional `/progression` command

If command count permits, add:

```text
/progression
```

If not, include progression in:

```text
/sheet
/stateaudit
/integrity
```

`/progression` should show:

```text
XP totals
recent XP events
level history
formula profile
pending/deferred progression events
world scaling boundary
```

All output must start with `[AIDRPG]` and be non-canon.

---

# 29. ContextBuilder progression packet

ContextBuilder should include concise progression facts.

Include:

```text
level if known
important XP/level milestones
formula/deferred warning only if relevant
no-rubber-band reminder only if world/encounter context needs it
```

Do not include pending XP claims as accepted XP, stat placeholders as real stats, or world-scaling rules as enemy stats.

---

# 30. IntegrityChecker progression checks

Add checks:

```text
XP totals are numeric and nonnegative
level is positive integer
level matches formula profile if active
XPEventRecords have operation keys
LevelEventRecords have formula profile IDs
no duplicate XP event keys
no duplicate level-up history for same threshold
formula profile is explicit if formulasEnabled true
locked formula profile not silently changed
stat placeholders not treated as active stats
world scaling boundary exists and disables rubber-band scaling
command/card/context source not active progression truth
```

IntegrityChecker may warn/quarantine bad records.

It may not grant XP, levels, or stats.

---

# 31. Cleanup progression protections

Cleanup may compact:

```text
old low-value rejected XP audits
old duplicate XP warning spam
old verbose formula debug logs
progression warnings cap
```

Cleanup must not delete:

```text
XPEventRecords needed for audit
LevelEventRecords
formula profile records
formula profile operation keys
XP/level durable keys
progression milestones
world scaling boundary policy
```

---

# 32. ReleaseCheck gates for this chunk

Add or verify PC10-specific gates:

```text
PC10-G01 FormulaProfile schema exists or equivalent profile structure exists
PC10-G02 FormulaRuleRecord schema exists or equivalent formula registry exists
PC10-G03 XPEventRecord schema exists or equivalent XP event structure exists
PC10-G04 LevelEventRecord schema exists or equivalent level history structure exists
PC10-G05 ProgressionHistoryRecord or compact progression audit exists
PC10-G06 WorldScalingBoundaryRecord exists and disables rubber-band scaling
PC10-G07 CanonValidator rejects vague/training/promise/card/context/command XP
PC10-G08 exact numeric XP gains are accepted only from safe sources
PC10-G09 ProgressionReducer rejects unsafe routed progression events
PC10-G10 XP/level duplicate operation keys exist and are durable
PC10-G11 level-up requires active formula profile or safe structured startup source
PC10-G12 stat automation remains deferred to PC11 unless fully specified
PC10-G13 quest XP reward follows split/deferred reward contract
PC10-G14 ability training does not award XP directly
PC10-G15 combat victory does not award XP without exact XP event/spec
PC10-G16 /sheet or /progression displays formula/progression status honestly
PC10-G17 command/context/card progression text cannot become canon
PC10-G18 cleanup does not delete XP/level/formula durable records
PC10-G19 world threat/encounter systems remain deferred and not rubber-banded
PC10-G20 player-breaker progression tests registered
```

ReleaseCheck must fail if vague XP, command/card/context XP, duplicate XP, manual level setting, or rubber-band scaling is possible.

---

# 33. Required tests

## PC10-001 — Exact XP gain accepted

Output:

```text
You gain 20 XP.
```

Expected:

```text
XPEventRecord amount 20
totalXP/currentXP update if active
operation key added
```

## PC10-002 — Vague experience rejected

```text
You learn from the experience.
```

Expected: no XP gain.

## PC10-003 — Feel stronger no stat/XP

```text
You feel stronger after the climb.
```

Expected: no XP and no stat increase.

## PC10-004 — Ability training no XP

```text
I train Ember Ward all afternoon.
```

Expected: ability training may update; XP unchanged.

## PC10-005 — Training plus exact XP

```text
After training Ember Ward, you gain 10 XP.
```

Expected: ability evidence if bound; XP +10 only once from exact XP clause.

## PC10-006 — Quest XP promise no XP

```text
Old Bren promises 20 XP if you clear the wolves.
```

Expected: XP reward promise/deferred; XP unchanged.

## PC10-007 — Quest board XP no XP

```text
The quest board lists a reward of 20 XP.
```

Expected: listed/promise reward only; XP unchanged.

## PC10-008 — Paid XP reward once

```text
Old Bren awards you the promised 20 XP.
```

Expected: XP +20 only if quest XP reward split/progression result accepted; operation key added.

## PC10-009 — Paid XP duplicate skipped

Repeat paid XP reward output.

Expected: no second XP gain.

## PC10-010 — Combat victory no XP by itself

```text
You defeat the wolf.
```

Expected: no XP unless exact XP event also appears.

## PC10-011 — Combat victory with exact XP

```text
You defeat the wolf and gain 5 XP.
```

Expected: XP +5 from exact XP clause only; no combat automation beyond this chunk.

## PC10-012 — Command progression non-canon

Run `/sheet` or `/progression`, then feed command output through Output hook.

Expected: no XP/level/stat mutation from command text.

## PC10-013 — Story Card level prose non-canon

```text
The player is level 10 with 5000 XP.
```

Expected: no level/XP mutation; quarantine/audit only if surfaced.

## PC10-014 — Context XP prose non-canon

```text
The player gained 20 XP last turn.
```

Expected: no XP mutation from context text.

## PC10-015 — Manual setlevel disabled

Run:

```text
/setlevel 999
/givexp 1000
```

Expected: disabled/safe unknown command; no mutation.

## PC10-016 — Level formula missing warning

Setup: XP exists, formulaProfile missing or inactive.

Expected: XP tracked; level-up pending/deferred; ReleaseCheck WARN, not fake full PASS.

## PC10-017 — Level-up with locked formula

Setup: formula profile active and XP threshold crossed.

Expected: level updates according to formula; LevelEventRecord created; no stat auto-allocation.

## PC10-018 — Duplicate level-up skipped

Repeat same XP/threshold event.

Expected: no duplicate level history.

## PC10-019 — Formula profile cannot silently change

Setup: locked formulaProfile = basic_curve_v1; incoming profile = different_curve.

Expected: warning/block/migration-needed; no silent formula replacement.

## PC10-020 — Negative XP rejected

```text
You gain -20 XP.
```

Expected: rejected/quarantined; no XP decrease unless future loss spec exists.

## PC10-021 — Nonnumeric XP rejected

```text
You gain many XP.
```

Expected: no XP gain.

## PC10-022 — World does not rubber-band

Setup: player level increases; threat profile exists.

Expected: threat not automatically rescaled to player level.

## PC10-023 — Low-level player can face high threat later

Expected: system does not force fair fight scaling; region/creature threat authority deferred to PC14.

## PC10-024 — Progression display honest

State: level 1, totalXP 20, formulaProfile inactive, stat automation deferred, world scaling disabled.

Expected: display says exactly that.

## PC10-025 — Cleanup preserves progression keys

Setup: XPEventRecord, LevelEventRecord, formula profile key, world scaling boundary.

Expected: durable progression records/keys preserved.

---

# 34. Regression placement

Add PC10 tests to regression tiers:

```text
T2 Parser/Proposal/Validator Safety Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T7 Long-Run Stability Tests
T8 Deferred System Disabled Tests
```

Minimum expectation:

```text
25 PC10 tests registered
0 critical PC10 failures
```

---

# 35. State fields added or deepened

Expected additions/deepening:

```text
progression.formulaProfile
progression.formulasEnabled
progression.xpHistory
progression.levelHistory
progression.milestones
FormulaProfile shape
FormulaRuleRecord shape
XPEventRecord shape
LevelEventRecord shape
ProgressionHistoryRecord shape
ProgressionMilestoneRecord shape
WorldScalingBoundaryRecord shape
```

---

# 36. Parser/proposal/validator changes

Allowed:

```text
exact XP detection
vague XP rejection
quest XP reward split/deferred validation
level formula authority validation
formula profile validation
no-rubber-band boundary validation
operation key suggestions
```

Forbidden:

```text
full stat formula application
combat XP automation without exact XP event
manual XP/level mutation parser
card/context progression import
world enemy scaling parser
```

---

# 37. Reducer changes

Allowed:

```text
ProgressionReducer exact XP gain
ProgressionReducer XP reward gain if split result active
ProgressionReducer formula profile set/lock
ProgressionReducer level-up if formula active
ProgressionReducer milestone records
ProgressionReducer unsafe routed-data rejection
progression dirty targets
duplicate skip results
```

Forbidden:

```text
manual setlevel/givexp reducer
stat allocation reducer
combat reward automation without spec
world scaling mutation
ability training XP by default
```

---

# 38. Command/context/card changes

Allowed:

```text
/sheet progression display
/progression optional command
/context progression packet
/cardaudit progression mirror labels if dry-run
/releasecheck PC10 gates
/playerbreakercheck progression cases
```

Forbidden:

```text
/setlevel
/givexp
/addxp
/resetlevel
manual formula switch command without migration/spec
live card writes that create progression truth
```

---

# 39. Beginner implementation order

When implementing this chunk in code, follow this order.

## Step 1 — Copy accepted place/time build

Start from:

```text
WorldLedgerV0.17.9_PLACE_TIME_DEPTH_ACCEPTED.txt
```

Create working file:

```text
WorldLedgerV0.17.10_PROGRESSION_BASELINE_WORKING.txt
```

## Step 2 — Add/verify progression helper functions

Create helpers for:

```text
ensureProgressionShape
makeFormulaProfile
makeFormulaRuleRecord
makeXPEventRecord
makeLevelEventRecord
makeProgressionHistoryRecord
makeProgressionMilestoneRecord
makeWorldScalingBoundaryRecord
normalizeXPAmount
isUnsafeProgressionSource
isVagueExperienceClaim
```

## Step 3 — Add FormulaRegistry / FormulaMatrix shell

Start small:

```text
registry structure
profile lookup
formula active/locked checks
level threshold helper if formula active
no-rubber-band policy record
```

Do not implement all final stat math here.

## Step 4 — Update progression CanonValidator rules

Add strict accept/reject/pending logic for exact XP, vague experience, quest XP promise/ready/payment, training-only XP, combat-only XP, level-up authority, formula profile changes, and world scaling boundary.

## Step 5 — Update ProposalBuilder progression support

Ensure proposals carry XP amount, source type, related quest/ability/actor/place keys, formula profile id if relevant, safety flags, and operation key suggestion.

## Step 6 — Update ProgressionReducerLite

Implement safe commits for exact XP gain, XP reward if split result active, formula profile set/lock, level-up if formula active, milestone, and world scaling boundary.

## Step 7 — Update `/sheet` and optional `/progression`

Make progression display honest.

## Step 8 — Update ContextBuilder progression packet

Keep it concise and state-accurate.

## Step 9 — Update IntegrityChecker progression checks

Add XP totals, formula profile, duplicate XP/level, deferred stats, and no-rubber-band checks.

## Step 10 — Add PC10 ReleaseCheck gates

Add PC10-G01 through PC10-G20.

## Step 11 — Add PC10 tests

Add PC10-001 through PC10-025.

## Step 12 — Run baseline checks

Run:

```text
/foundationcheck
/releasecheck
/integrity
/playerbreakercheck
/regressioncheck
/sheet
/progression if available
```

## Step 13 — Save accepted progression build

Recommended output:

```text
WorldLedgerV0.17.10_PROGRESSION_BASELINE_ACCEPTED.txt
```

---

# 40. What this chunk produces

Production Chunk 10 should produce:

```text
WorldLedger V0.17.10 progression-baseline accepted script if code changes are made
progression helper depth
FormulaRegistry / FormulaMatrix shell
ProgressionReducer exact XP/level/formula behavior
progression CanonValidator refinement
/sheet and optional /progression display improvement
progression context packet
progression integrity checks
PC10 ReleaseCheck gates
PC10 regression/player-breaker tests
progression acceptance report
updated planning zip with Production Chunk 10 added
```

It should not produce:

```text
full stat engine
full derived value engine
combat XP automation
manual XP/level commands
world rubber-band scaling
race/lineage stat automation
absorption/evolution stat gain
live CardSync writes
old card prose progression import
```

---

# 41. Pass / warn / fail criteria

## PASS

Production Chunk 10 passes if:

```text
formula profile/registry baseline exists
exact XP gains are accepted from safe sources only
vague/training/promise/card/context/command XP is rejected or deferred
XP and level operations are duplicate-safe
level-up requires formula authority
stat automation remains deferred to PC11
quest XP reward uses split/deferred reward contract
ability training does not award XP directly
combat victory does not award XP without exact XP event/spec
world scaling boundary disables rubber-band scaling
/sheet or /progression displays status honestly
command/context/card progression text cannot become canon
cleanup preserves XP/level/formula durable records
PC10 tests pass
ReleaseCheck has no PC10 FAIL/BLOCKED gates
```

## WARN

Warn but allow progress if:

```text
formula profile exists but final XP curve is conservative
level automation is pending because formula profile is missing
stats are deferred to PC11
combat XP is deferred to PC12
world threat scaling is deferred to PC14
XP reward integration is conservative/deferred if split-result support is incomplete
```

## FAIL

Fail and stop if:

```text
vague experience grants XP
training grants XP by default
quest promise grants XP
command/context/card text grants XP or level
XP duplicates on replay
level changes without formula/startup authority
manual /setlevel or /givexp mutates state
stat increases happen without PC11 rules
world enemies automatically scale to player level
cleanup deletes XP/level/formula durable records
ProgressionReducer trusts unsafe routed data
```

---

# 42. Handoff to Production Chunk 11

When this chunk passes, Production Chunk 11 begins:

```text
Production Chunk 11 — Stats, Derived Values, Conditions, and Invariant Checker
```

Production Chunk 11 may now safely build:

```text
base stats
modifiers
derived values
resource formulas
condition modifiers
stat invariants
safe stat changes
```

Production Chunk 10 must leave progression reliable enough that PC11 can ask:

```text
What level is the player?
How much XP is confirmed?
Which formula profile is active?
Are formulas locked?
Can stats safely derive from level/base/modifiers?
Are XP and level keys duplicate-safe?
Is world scaling explicitly not rubber-banded?
```

---

# 43. Final doctrine

Production Chunk 10 is where progression stops being vibes and starts being a controlled system.

Final rule:

```text
XP must be exact.
Levels must be formula-authorized.
Stats must wait for stat rules.
World threat must not rubber-band.
Every progression change must have source, key, formula status, and audit.
```

Once this passes, the project can safely deepen stats, derived values, conditions, and invariants in Production Chunk 11.


====================================================================================================
11_AIDRPG_Production_Chunk_11_Stats_Derived_Values_Conditions_and_Invariant_Checker.txt
====================================================================================================

# AIDRPG Production Chunk 11 — Stats, Derived Values, Conditions, and Invariant Checker

## 0. Document status

**Document name:** AIDRPG Production Chunk 11 — Stats, Derived Values, Conditions, and Invariant Checker  
**Production sequence:** 11 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.10 progression-baseline accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Deepen base stats, stat modifiers, derived values, resource formulas, condition modifiers, invariant checking, stat audit, and safe repair/quarantine behavior without vague stat gains, hidden recalculation drift, impossible values, or cleanup deleting truth.  
**Status:** Core systems production chunk.  
**Follows:** Production Chunk 10 — Formula Matrix and Progression Baseline.  
**This document does not contain final script code.**

Production Chunk 11 is the math-safety wall between the progression baseline and the combat system. It makes stats useful while preventing the classic failure pattern where vague narration, cleanup, or a stale formula silently rewrites the character.

---

# 1. Chunk doctrine

```text
Base stats are truth.
Derived stats are calculated snapshots.
Modifiers need source, owner, duration, status, and operation keys.
Conditions may affect stats, but cleanup cannot heal them.
Level does not grant stats unless a stat rule exists.
Vague strength is not STR.
Feeling faster is not DEX.
Training is not a stat increase by default.
Impossible values must warn, repair structurally, or quarantine.
No derived value becomes permanent truth without formula authority.
```

Stats must be deterministic, versioned, explainable, and ready for AI Dungeon gameplay, where output text can be messy and repeated.

---

# 2. Why this chunk comes now

Production Chunk 10 locked exact XP, formula profile authority, duplicate-safe level events, and no-rubber-band world scaling. Stats come next because future systems depend on them:

```text
combat damage/healing
body resources
ability costs and reliability
conditions and injuries
race/form consequences
creature threat comparison
world threat benchmarks
equipment modifiers
long-run balance
```

If stats and invariants are not safe before combat, combat will be built on unstable math and patch-loop risk.

---

# 3. Allowed scope

Production Chunk 11 may change:

```text
Progression stat helpers
Stat schema helpers
Modifier helpers
DerivedValueCalculator shell
ConditionModifierBridge
InvariantChecker
Body resource invariant checks
CanonValidator stat/progression modifier rules
ProposalBuilder stat/modifier event types
EventScannerLite stat/condition modifier hints
/sheet, /status, optional /stats
ContextBuilder stat/body packet
OperationLedger stat/modifier/resource keys
DirtyQueue stat/body targets
Cleanup protected paths
ReleaseCheck stat/invariant gates
Regression/player-breaker stat tests
```

It may not add:

```text
full combat damage engine
full equipment stat bonus system
full class/talent tree
race/lineage power automation
absorption/evolution stat gains
manual /setstat or /addstat commands
world enemy auto-scaling
full final level 1–1000 balancing unless the formula authority is locked and tested
```

---

# 4. Stat authority model

Every stat value must have an authority level:

```text
unknown
setup_confirmed
formula_derived
validated_story_change
temporary_modifier
condition_modifier
equipment_modifier_deferred
race_form_modifier_deferred
manual_deferred
invalid_quarantined
```

Rules:

```text
unknown does not become 10 by default unless the active profile says so
setup_confirmed can initialize base stats before lock
formula_derived calculates snapshots but does not overwrite base stats
validated_story_change requires strict validation and ReleaseCheck coverage
temporary_modifier must have source/duration/status
condition_modifier must link to an active condition/injury
manual_deferred is blocked/display only, not active mutation
```

---

# 5. StatBlock schema

Required shape:

```text
StatBlock {
  baseStats,
  modifiers,
  derivedValues,
  resources,
  formulaProfileId,
  lastCalculatedTurn,
  lastValidTurn,
  warnings,
  notes
}
```

Recommended storage:

```text
progression.statBlock
```

or a clearly bridged equivalent across:

```text
progression.baseStats
progression.modifiers
progression.derivedSnapshot
body.resources
```

Do not create a second unrelated stat root.

---

# 6. BaseStatRecord schema

Required fields:

```text
BaseStatRecord {
  key,
  displayName,
  value,
  known,
  min,
  max,
  source,
  sourceOperationKey,
  locked,
  createdTurn,
  updatedTurn,
  warnings,
  notes
}
```

Possible stat keys if the active formula profile supports them:

```text
strength
dexterity
constitution
intelligence
wisdom
charisma
willpower
perception
agility
endurance
```

The final active stat list must come from FormulaProfile / Formula Appendix authority. If no stat profile is active, base stats should stay unknown/deferred instead of being guessed.

---

# 7. StatModifierRecord schema

Required fields:

```text
StatModifierRecord {
  id,
  key,
  targetStatKey,
  modifierType,
  value,
  stackingPolicy,
  sourceFamily,
  sourceKey,
  sourceOperationKey,
  status,
  durationType,
  expiresTurn,
  createdTurn,
  updatedTurn,
  notes
}
```

Modifier types:

```text
add
multiply
set_min
set_max
cap
floor
advantage_label
disadvantage_label
unknown
```

Source families:

```text
condition
injury
ability_deferred
equipment_deferred
race_form_deferred
quest_deferred
manual_deferred
unknown
```

Rules:

```text
condition modifiers require active condition/injury
expired modifiers do not affect derived values
duplicate modifiers require operation keys and stacking policy
equipment, ability, race, and form modifiers remain deferred unless their owner systems are active
```

---

# 8. DerivedValueRecord schema

Derived values are calculated outputs, not independent truth.

Required fields:

```text
DerivedValueRecord {
  key,
  displayName,
  value,
  known,
  formulaRuleId,
  formulaProfileId,
  inputsHash,
  calculatedTurn,
  stale,
  warnings,
  notes
}
```

Examples:

```text
maxHP
maxMP
maxEP
armorClass_deferred
carryCapacity_deferred
initiative_deferred
attackBonus_deferred
defense_deferred
```

Rules:

```text
derived values recalculate from base stats, active modifiers, and formula rules
derivedSnapshot can be stored for display/performance
stale derived values must be marked stale, not trusted
cleanup cannot delete formula inputs and leave a snapshot as truth
```

---

# 9. ResourceFormulaRecord schema

Body resources connect body and stats.

Required fields:

```text
ResourceFormulaRecord {
  resourceKey,
  formulaRuleId,
  formulaProfileId,
  currentPolicy,
  maxPolicy,
  clampPolicy,
  source,
  enabled,
  warnings,
  notes
}
```

Rules:

```text
maxHP/maxMP/maxEP may derive from stat formulas only if formula active
current resource does not auto-fill to new max unless a policy says so
structural clamp is not healing
current cannot exceed max when both are known
current cannot go below 0 when known
```

---

# 10. ConditionModifierRecord schema

Conditions and injuries may apply modifiers.

Required fields:

```text
ConditionModifierRecord {
  id,
  conditionKey,
  injuryKey,
  targetStatKey,
  targetDerivedKey,
  effectLabel,
  modifierType,
  value,
  severityScale,
  active,
  sourceOperationKey,
  createdTurn,
  notes
}
```

Examples:

```text
fatigued -> minor endurance/agility disadvantage label
exhausted -> major endurance/strength/agility disadvantage label
bleeding -> endurance / HP-risk label
sprained ankle -> movement/agility disadvantage label
blinded -> perception/attack labels deferred to combat
```

Unknown severity should use cautious labels, not guessed math.

---

# 11. StatChangeRecord schema

Direct stat changes must be rare and audited.

Required fields:

```text
StatChangeRecord {
  id,
  statKey,
  fromValue,
  toValue,
  delta,
  changeType,
  sourceType,
  sourceOperationKey,
  acceptedTurn,
  formulaProfileId,
  warnings,
  notes
}
```

Allowed change types:

```text
setup_initialize
formula_level_up
validated_story_change
migration_repair
quarantine_rejected
manual_deferred
unknown
```

Default direct story stat changes should be pending/deferred unless a strict stat-change policy is active.

---

# 12. InvariantViolationRecord and RepairActionRecord schemas

Invariant problems must be recorded clearly.

```text
InvariantViolationRecord {
  id,
  family,
  severity,
  path,
  expected,
  actualPreview,
  action,
  sourceOperationKey,
  detectedTurn,
  resolvedTurn,
  notes
}
```

```text
RepairActionRecord {
  id,
  violationId,
  actionType,
  path,
  beforePreview,
  afterPreview,
  source,
  safe,
  createdTurn,
  notes
}
```

Allowed actions:

```text
report_only
repair_structural
clamp_structural
quarantine_payload
block_release
```

Forbidden repairs:

```text
award stats
award XP
heal player
remove active condition
remove stable ability
remove active inventory
silently rewrite formula profile
```

---

# 13. Formula and stat activation levels

## Stage 0 — Unknown stats

```text
baseStats empty/unknown
progression tracks XP
combat/stat math deferred
```

## Stage 1 — Setup-confirmed base stats

```text
structured startup can set base stats before lock
values must pass invariant checks
```

## Stage 2 — Derived values active

```text
formula profile has enabled derived rules
derivedSnapshot recalculates from base stats/modifiers
```

## Stage 3 — Level-up stat changes

```text
formula profile supports stat progression
level-up emits stat changes through owner rules
```

Stage 3 is allowed only if formulas and tests are fully defined. Otherwise it must remain WARN/deferred.

---

# 14. CanonValidator stat/modifier rules

Accept setup stat initialization only when:

```text
structured startup source before lock
stat profile active or setup schema explicitly supports stats
stat value numeric and within allowed range
operation key present
```

Reject stat change when any are true:

```text
vague stronger/faster/smarter phrasing
training-only claim
ability use claim
dream/quote/rumor/prophecy
command/context/card/audit/debug source
manual command
impossible negative/nonnumeric value
formula profile missing for formula-derived change
```

Accept modifier only when:

```text
active condition/injury/status exists or is accepted in same batch
modifier mapping rule exists
source is safe
operation key present
```

Reject modifier when:

```text
condition is only rumored/quoted/dreamed
source is command/context/card/audit/debug
equipment/race/ability modifier tries to activate before its owner chunk
```

---

# 15. DerivedValueCalculator behavior

DerivedValueCalculator should:

```text
read base stats
read active modifiers
read formula rules
calculate derived values
write derivedSnapshot only through owner reducer/checker path
mark stale if inputs change
never mutate base stats
never mutate active conditions
never award resources as healing
```

If formula missing:

```text
derived value remains unknown/deferred
ReleaseCheck WARN if feature claimed active
```

---

# 16. InvariantChecker behavior

InvariantChecker should run after reducer batches and through diagnostic commands.

It should check:

```text
required stat structures exist
base stat values valid
resource current/max valid
modifiers have source/status/duration
condition modifiers link to active conditions
expired modifiers inactive
derived values match inputs if active
level/formula profile consistency
no duplicate active modifiers without stacking policy
no command/card/context source as stat truth
cleanup protected paths intact
```

Allowed checker actions:

```text
report warning
clamp structural resource overflow
quarantine malformed modifier/stat record
mark derivedSnapshot stale
block ReleaseCheck
```

---

# 17. ProgressionReducer stat-safe hooks

ProgressionReducer may interact with stats only through explicit stat rules.

Allowed:

```text
on level-up, check stat formula profile
if stat formula active, emit stat change records according to formula
if inactive, add level history only and stat progression pending/warn
```

Forbidden:

```text
level-up automatically gives random stat points
level-up heals to full by default
level-up changes HP max without resource formula
level-up changes world scaling
```

---

# 18. Body/resource integration

Rules:

```text
maxHP/maxMP/maxEP may derive from stats/formulas
currentHP/currentMP/currentEP does not auto-fill to new max unless policy active
structural clamp from above max is not healing
if max decreases below current, current clamps down structurally with audit
if max increases, current remains same unless heal/recovery/fill policy is accepted
```

This prevents false healing from formula recalculation.

---

# 19. Equipment/ability/race modifier boundaries

Equipment modifiers:

```text
iron sword equipped does not add attack bonus yet
armor does not change AC unless equipment formula spec is active
```

Ability modifiers:

```text
ability cost/cooldown labels remain descriptive
ability use does not change stats unless a future ability effect spec validates it
```

Race/form modifiers:

```text
race/form consequences are mostly PC13
wolf form may produce constraint labels if validated, but not full stat formula unless rule active
```

---

# 20. Operation keys

Required keys:

```text
stat.setup.<statKey>.<startupKey>
stat.change.<statKey>.<deltaOrValue>.<sourceHash>
stat.modifier.add.<modifierKey>.<sourceHash>
stat.modifier.expire.<modifierKey>.<sourceHash>
derived.recalc.<formulaProfileId>.<inputsHash>
resource.max.recalc.<resourceKey>.<inputsHash>
resource.clamp.<resourceKey>.<sourceHash>
invariant.check.<turnBucket>.<schemaVersion>
invariant.violation.<family>.<pathHash>.<actualHash>
repair.structural.<pathHash>.<sourceHash>
quarantine.stat.<pathHash>.<sourceHash>
```

Stat change keys are durable. Derived recalculation keys may be cache-like. Repair/quarantine keys should prevent repeated spam.

---

# 21. Dirty targets

Stat/resource updates should mark:

```text
player_sheet
body_status
progression
context
```

Mapping:

```text
base stat change -> player_sheet, progression, context
modifier change -> player_sheet, body_status if condition-related, context
derived recalculation -> player_sheet, body_status if resource-related, context
resource clamp -> body_status, player_sheet, context
invariant warning -> stateaudit/releasecheck only unless gameplay-relevant
```

---

# 22. Command and context updates

`/sheet` should show:

```text
level/XP summary
base stats if known
formula profile
derived values if active
stat automation warnings/deferred notes
world scaling boundary summary
```

`/status` should show:

```text
resources current/max/known
active conditions
condition modifiers if active
injuries
death boundary
resource formula warnings
```

Optional `/stats` should show:

```text
base stats
active modifiers
derived values
formula profile
last invariant result
```

ContextBuilder should include only relevant stat/body facts, not raw stat JSON or unknown stats as fake values.

---

# 23. Cleanup protections

Cleanup may compact:

```text
old low-risk invariant logs
old duplicate stale-derived warnings
old repair spam after summary
old inactive modifier history after archive policy
```

Cleanup must not delete:

```text
base stats
active modifiers
active condition sources
derived formula rules
formula profile
XP/level records
active body resources
unresolved invariant violations
quarantine records
repair action records needed for audit
```

Cleanup must not heal, level, assign stats, or change formulas.

---

# 24. ReleaseCheck gates for this chunk

Add or verify PC11-specific gates:

```text
PC11-G01 StatBlock schema exists or equivalent stat structure exists
PC11-G02 BaseStatRecord schema exists or equivalent base stat record exists if stats active
PC11-G03 StatModifierRecord schema exists or equivalent modifier structure exists
PC11-G04 DerivedValueRecord schema exists or equivalent derived snapshot structure exists
PC11-G05 ResourceFormulaRecord schema exists or equivalent resource formula structure exists
PC11-G06 ConditionModifierRecord schema exists or equivalent condition modifier bridge exists
PC11-G07 InvariantViolationRecord and RepairActionRecord exist or equivalent audit structures exist
PC11-G08 CanonValidator rejects vague/training/command/card/context stat gains
PC11-G09 DerivedValueCalculator does not mutate base stats
PC11-G10 ProgressionReducer does not auto-assign stats without formula authority
PC11-G11 resource max/current recalculation does not heal by default
PC11-G12 condition modifiers link to active conditions and deactivate on resolution
PC11-G13 InvariantChecker detects impossible stat/resource/modifier states
PC11-G14 cleanup preserves base stats, active modifiers, active conditions, formula profiles, and unresolved violations
PC11-G15 command/context/card stat text cannot become canon
PC11-G16 equipment/ability/race/form stat modifiers remain deferred unless owner rules active
PC11-G17 world scaling remains no-rubber-band
PC11-G18 optional /stats or /sheet+/status displays stat state honestly
PC11-G19 durable stat/modifier/resource/repair keys exist
PC11-G20 player-breaker stat/invariant tests registered
```

ReleaseCheck must fail if vague stat gains apply, derived values overwrite base stats, cleanup heals/removes active modifiers, or resource recalculation heals the player silently.

---

# 25. Required tests

```text
PC11-001 Unknown stats not defaulted silently
PC11-002 Structured setup stats accepted with operation keys
PC11-003 Vague “you feel stronger” no stat gain
PC11-004 Training no stat gain by default
PC11-005 Command stat text non-canon
PC11-006 Story Card stat prose non-canon
PC11-007 Context stat prose non-canon
PC11-008 Level-up without stat formula does not assign stats
PC11-009 Level-up with stat formula applies only formula-defined changes
PC11-010 Derived value recalculates from inputs
PC11-011 Derived value does not overwrite base stat
PC11-012 Resource max increase does not heal
PC11-013 Resource max decrease clamps structurally
PC11-014 Resource current cannot exceed max
PC11-015 Negative resource rejected/quarantined or structurally clamped
PC11-016 Condition modifier active only with active condition
PC11-017 Resolved condition deactivates modifier
PC11-018 Cleanup does not heal condition modifier
PC11-019 Duplicate modifier skipped without stacking policy
PC11-020 Equipment modifier deferred
PC11-021 Ability modifier deferred
PC11-022 Race/form modifier deferred unless rule active
PC11-023 Invalid stat record quarantined
PC11-024 Formula profile cannot silently change stats
PC11-025 World scaling remains disabled
PC11-026 Stats display honest
PC11-027 Integrity command actionable and non-mutating
```

Minimum expectation:

```text
27 PC11 tests registered
0 critical PC11 failures
```

Regression tiers:

```text
T1 State/Runtime/Command Foundation Tests
T2 Parser/Proposal/Validator Safety Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T7 Long-Run Stability Tests
T8 Deferred System Disabled Tests
```

---

# 26. Beginner implementation order

1. Copy accepted progression build:

```text
WorldLedgerV0.17.10_PROGRESSION_BASELINE_ACCEPTED.txt
```

2. Create working file:

```text
WorldLedgerV0.17.11_STATS_INVARIANTS_WORKING.txt
```

3. Add helper functions:

```text
ensureStatBlockShape
makeBaseStatRecord
makeStatModifierRecord
makeDerivedValueRecord
makeResourceFormulaRecord
makeConditionModifierRecord
makeInvariantViolationRecord
makeRepairActionRecord
normalizeStatKey
normalizeModifierType
isUnsafeStatSource
isVagueStatGain
```

4. Add DerivedValueCalculator shell.
5. Add ModifierManager / ConditionModifierBridge.
6. Update stat CanonValidator rules.
7. Update ProgressionReducer stat-safe hooks.
8. Update InvariantChecker.
9. Update `/sheet`, `/status`, and optional `/stats`.
10. Update ContextBuilder stat/body packet.
11. Add PC11-G01 through PC11-G20 ReleaseCheck gates.
12. Add PC11-001 through PC11-027 tests.
13. Run:

```text
/foundationcheck
/releasecheck
/integrity
/playerbreakercheck
/regressioncheck
/sheet
/status
/stats if available
```

14. Save accepted build:

```text
WorldLedgerV0.17.11_STATS_INVARIANTS_ACCEPTED.txt
```

---

# 27. Pass / warn / fail criteria

## PASS

```text
stat structures exist and are source-aware
base stats and derived values are separated
modifiers have source/status/duration/keys
condition modifiers link to active conditions
derived values recalculate from formula inputs only
resource max/current recalculation does not heal by default
level-up does not assign stats without formula authority
vague/training/command/card/context stat gains are rejected
InvariantChecker detects impossible values and records actions
cleanup preserves active stat/body/formula truth
stats display honestly
PC11 tests pass
ReleaseCheck has no PC11 FAIL/BLOCKED gates
```

## WARN

```text
stats remain unknown because no profile is active
stat formulas are partially deferred
combat derived values are deferred
race/form/equipment modifiers are deferred
derived snapshots are display-only until combat formulas activate
```

## FAIL

```text
unknown stats default silently
vague narration increases stats
training increases stats by default
command/context/card text mutates stats
level-up assigns stats without formula authority
resource max increase heals current HP/MP/EP by default
derived values overwrite base stats
condition cleanup removes active penalties
modifiers duplicate without stacking policy
world enemies scale from player stats
InvariantChecker repairs gameplay truth by awarding/healing/deleting active state
```

---

# 28. Handoff to Production Chunk 12

When this chunk passes, Production Chunk 12 begins:

```text
Production Chunk 12 — Combat Event Model and Damage/Healing Dispute Resolver
```

Production Chunk 12 may now safely use:

```text
known resources
active conditions
injuries
stat labels/modifiers
derived values if active
duplicate-safe operation keys
invariant checks
```

Production Chunk 11 must leave stat/body math reliable enough that PC12 can ask:

```text
What HP/MP/EP values are known?
Which modifiers are active?
Are derived combat values known, stale, or deferred?
Can damage/healing safely change resources?
Will duplicate combat output apply twice?
Will cleanup accidentally heal or remove penalties?
```

---

# 29. Final doctrine

Production Chunk 11 is where numbers stop being vibes.

Final rule:

```text
Stats are source-bound.
Derived values are calculated.
Modifiers are owned.
Resources are clamped safely.
Conditions are not cleanup noise.
Invariants catch corruption before it spreads.
No unsafe text source, formula drift, or convenience repair can rewrite the character.
```

Once this passes, the project can safely build combat event and damage/healing resolution in Production Chunk 12.


====================================================================================================
12_AIDRPG_Production_Chunk_12_Combat_Event_Model_and_Damage_Healing_Dispute_Resolver.txt
====================================================================================================

# AIDRPG Production Chunk 12 — Combat Event Model and Damage/Healing Dispute Resolver

## 0. Document status

**Document name:** AIDRPG Production Chunk 12 — Combat Event Model and Damage/Healing Dispute Resolver  
**Production sequence:** 12 of 25 remaining production chunks  
**Build target:** WorldLedger V0.17.11 stats/invariants accepted build / AIDRPG Core MVP expansion path  
**Purpose:** Add a conservative combat event model, attack/result separation, damage/healing dispute resolution, duplicate-safe combat operation keys, body/resource integration, combat display support, and combat regression gates without activating full advanced combat automation.  
**Status:** Core systems production chunk.  
**Follows:** Production Chunk 11 — Stats, Derived Values, Conditions, and Invariant Checker.  
**This document does not contain final script code.**

Production Chunk 12 exists because combat is where AI Dungeon scripts can break the fastest.

Old and rough builds often risked:

```text
player input attack automatically damaging enemies
model narration applying damage twice
miss/block/graze text still applying full damage
healing from item names without validated use
healing from /status or context text
armor/equipment bonuses applying without formulas
ability names becoming damage rules
NPC death from rumor/threat/metaphor
player death from dramatic wording
combat victory granting XP automatically
duplicate combat output repeating damage, healing, loot, XP, and death
```

This chunk creates combat as a structured event system. It does **not** build the full advanced combat engine. It does **not** attempt full tactical AI, encounter balancing, party combat, warfare, absorption, or creature-specific advanced logic. It creates the safe baseline needed before those systems can exist.

---

# 1. Chunk doctrine

```text
An attack attempt is not a hit.
A hit is not damage until validated.
Damage is not death until the death boundary validates it.
Healing is not recovery unless a healing/recovery event validates it.
A miss is not damage.
A block is not full damage.
A graze is not full damage unless validated.
Input intent cannot force outcome.
Output contradiction beats input prediction.
Duplicate output cannot repeat damage or healing.
Combat victory is not XP unless exact XP is separately validated.
```

Combat truth must be proposal-based, outcome-aware, duplicate-safe, and integrated with Body/Stats without bypassing them.

---

# 2. Why this chunk comes now

Production Chunk 11 made stats, resources, conditions, derived values, and invariants safer.

Combat comes now because it uses:

```text
body resources
injuries
conditions
stat/derived labels
inventory/equipment records
ability status/evidence
actor presence
place/scene context
XP/progression boundaries
operation ledgers
invariant checks
```

If combat is built before these layers, it becomes a pile of special cases. This chunk makes combat an event model that can later be expanded into advanced combat, creature ecology, party/summon handling, and warfare.

---

# 3. What this chunk is allowed to change

Production Chunk 12 may change:

```text
Combat schema helpers
CombatEventReducer / CombatReducerLite shell
CombatDisputeResolver
DamageResolver shell
HealingResolver shell
CanonValidator combat/damage/healing rules
ProposalBuilder combat event types
EventScannerLite combat hints
CommandRenderers /status, optional /combat
ContextBuilder combat/body packet
OperationLedger combat/damage/healing/death keys
DirtyQueue combat/body/social targets
IntegrityChecker combat checks
ReleaseCheck combat gates
Regression/player-breaker combat tests
```

Production Chunk 12 may add:

```text
CombatEventRecord schema
CombatantRecord / CombatantSnapshot schema
AttackAttemptRecord schema
DefenseResultRecord schema
DamageRecord schema
HealingRecord schema
CombatDisputeRecord schema
CombatOutcomeRecord schema
CombatSceneState schema
combat result status model
damage/healing split-owner contract with BodyReducer
combat audit summaries
```

Production Chunk 12 may not add:

```text
full advanced combat automation
full enemy stat generation
full initiative/turn-order engine unless shell-only
full equipment stat bonus engine
full spell damage formula engine
full creature ecology combat rules
party/summon control
warfare/group combat
absorption/evolution on kill
automatic combat XP
manual /damage /heal /kill commands
```

---

# 4. Modules touched

Allowed modules:

```text
CombatState helpers
CombatReducerLite / CombatEventReducer
CombatDisputeResolver
DamageResolver shell
HealingResolver shell
BodyReducer integration boundary
ActorReducer reference boundary
Inventory/equipment read-only references
Ability read-only references
Stats/derived read-only references
CanonValidator combat rules
ProposalBuilder combat proposals
EventScannerLite combat hints
CommandRenderers: /status, optional /combat
ContextBuilderLite combat/body packet
OperationLedger combat keys
DirtyQueue combat/body dirty targets
AuditLogger combat lines
InvariantChecker combat checks
ReleaseCheckLite PC12 gates
Regression harness
PlayerBreaker harness
```

Avoid major changes to:

```text
ProgressionReducer except no automatic combat XP
InventoryReducer except item use/hit references remain separate owner events
AbilityReducer except ability use evidence remains separate owner event
ActorReducer except actor status/death remains conservative and owner-routed
PlaceReducer except scene danger labels only
World threat/creature ecology systems
Advanced combat systems
Manual mutation commands
CardSync live writes
```

---

# 5. Combat state model

Combat should have a bounded state area.

Recommended shape:

```text
combat: {
  active: false,
  sceneState: null,
  events: [],
  recentAttempts: [],
  recentOutcomes: [],
  disputes: [],
  warnings: [],
  notes: []
}
```

If no dedicated combat root exists, records may live in audits initially, but PC12 should prepare a stable `combat` bucket or equivalent. Combat state is not a full turn engine yet. It is an event ledger and dispute model.

---

# 6. Required combat schemas

## 6.1 CombatSceneState

```text
CombatSceneState {
  id,
  active,
  sceneKey,
  placeKey,
  combatantKeys,
  threatLevelLabel,
  lastCombatTurn,
  startedTurn,
  endedTurn,
  status,
  sourceOperationKey,
  warnings,
  notes
}
```

Statuses:

```text
none
threatening
engaged
retreating
resolved
unknown
```

Rules:

```text
combat scene state is context/support, not automatic damage
combat active does not mean every output causes damage
combat resolved does not grant XP/loot by itself
```

## 6.2 CombatantSnapshot

```text
CombatantSnapshot {
  id,
  combatantKey,
  combatantType,
  displayName,
  actorKey,
  player,
  presenceStatus,
  bodyKnown,
  resourceKnown,
  threatLabel,
  equipmentRefs,
  abilityRefs,
  conditionRefs,
  sourceOperationKey,
  createdTurn,
  notes
}
```

Combatant types:

```text
player
actor
creature_deferred
generic_enemy
object
hazard
unknown
```

## 6.3 CombatEventRecord

```text
CombatEventRecord {
  id,
  key,
  eventType,
  attackerKey,
  defenderKey,
  sourceType,
  sourceOperationKey,
  sourceEventId,
  localClauseSummary,
  resultStatus,
  damageKey,
  healingKey,
  disputeKey,
  createdTurn,
  applied,
  duplicateOf,
  warnings,
  notes
}
```

Event types:

```text
attack_attempt
defense_attempt
hit_confirmed
miss_confirmed
block_confirmed
graze_confirmed
damage_candidate
healing_candidate
combat_status_change
retreat_candidate
capture_candidate
death_candidate
victory_candidate
defeat_candidate
unknown
```

Result statuses:

```text
intent_only
pending
accepted
rejected
disputed
applied
skipped_duplicate
deferred
```

## 6.4 AttackAttemptRecord

```text
AttackAttemptRecord {
  id,
  attackerKey,
  defenderKey,
  method,
  weaponKey,
  abilityKey,
  targetBodyPart,
  intentSource,
  sourceOperationKey,
  createdTurn,
  status,
  notes
}
```

Rules:

```text
input attack creates attempt only
output confirms result
attempt can help bind later output
attempt does not apply damage
```

## 6.5 DefenseResultRecord

```text
DefenseResultRecord {
  id,
  attackerKey,
  defenderKey,
  resultType,
  method,
  sourceOperationKey,
  createdTurn,
  notes
}
```

Result types:

```text
miss
dodge
parry
block
armor_absorb
graze
partial_hit
full_hit
counter_deferred
unknown
```

## 6.6 DamageRecord

```text
DamageRecord {
  id,
  key,
  targetKey,
  targetType,
  amount,
  amountKnown,
  damageType,
  severityLabel,
  bodyPart,
  injuryKey,
  conditionKeys,
  hpDelta,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  applied,
  warnings,
  notes
}
```

Rules:

```text
known amount can affect HP if BodyReducer/resource rules allow
unknown amount can create injury/severity label if validated
injury/condition goes through BodyReducer or actor/body owner
damage does not directly set death unless death boundary validates
```

## 6.7 HealingRecord

```text
HealingRecord {
  id,
  key,
  targetKey,
  targetType,
  amount,
  amountKnown,
  healingType,
  resourceKey,
  conditionKey,
  injuryKey,
  sourceOperationKey,
  sourceEventId,
  createdTurn,
  applied,
  warnings,
  notes
}
```

Rules:

```text
healing requires accepted healing/recovery event
item name alone is not healing
rest intent is not healing
command/context/card healing text is not healing
healing must not exceed resource max unless policy says so
revival/death reversal is deferred/high-risk unless explicitly specified
```

## 6.8 CombatDisputeRecord

```text
CombatDisputeRecord {
  id,
  key,
  disputeType,
  attackerKey,
  defenderKey,
  inputClaim,
  outputClaim,
  resolvedResult,
  confidence,
  sourceOperationKey,
  createdTurn,
  notes
}
```

Dispute types:

```text
input_claimed_hit_output_miss
input_claimed_damage_output_block
output_claimed_damage_without_hit
healing_claimed_but_failed
death_claimed_but_unsafe
duplicate_output
ambiguous_target
ambiguous_amount
unknown
```

## 6.9 CombatOutcomeRecord

```text
CombatOutcomeRecord {
  id,
  eventKey,
  attackerKey,
  defenderKey,
  outcomeType,
  appliedEffects,
  blockedEffects,
  deferredEffects,
  sourceOperationKey,
  createdTurn,
  notes
}
```

Outcome types:

```text
attempt_only
miss
block
graze
hit_no_damage
hit_with_damage
healing_applied
healing_failed
capture
retreat
victory
defeat
death_pending
death_confirmed
unknown
```

---

# 7. Combat CanonValidator expansion

CanonValidator must enforce combat safety.

## 7.1 Accept attack attempt when

```text
safe input/output includes attack/swing/shoot/cast/use-hostile intent
target is bound enough
source is not command/context/card/audit/debug
```

Result:

```text
attack_attempt only
no damage yet unless output also confirms hit/damage
```

## 7.2 Accept miss/block/dodge when

```text
safe output directly says miss/dodge/parry/block/deflect/no effect
attacker/defender target can be bound
```

Result:

```text
combat outcome records miss/block
damage blocked/rejected unless separate accepted damage clause exists
```

## 7.3 Accept hit when

```text
safe output directly confirms hit/strike/connects/lands
not metaphor/quote/dream
not command/context/card source
```

Result:

```text
hit confirmed
no numeric damage unless damage clause validates
injury/severity may be proposed if direct physical consequence exists
```

## 7.4 Accept damage when

```text
safe output directly confirms wound, HP loss, injury, burn, bleeding, etc.
target is bound
not denied/blocked/missed/no effect
operation key not duplicate
```

## 7.5 Accept healing when

```text
safe output directly confirms HP restored, wound closes, bleeding stops, poison fades, etc.
target is bound
not failed/no effect/attempt only
operation key not duplicate
```

---

# 8. Combat rejection rules

Reject or defer when any are true:

```text
input-only attack tries to deal damage
miss/dodge/parry with no damage clause
blocked attack with no damage clause
dream/vision combat
quote says someone is hit
threat/intimidation only
metaphor only
training description not actual combat
command/context/card/audit/debug source
ambiguous target
ambiguous amount when numeric damage required
duplicate output
manual damage/heal command
```

Examples:

```text
I stab the goblin. -> attempt only
You miss the goblin. -> miss, no damage
The goblin blocks your sword. -> block, no damage unless separate injury says otherwise
Mira says, "The goblin is dead." -> no confirmed death
[AIDRPG] Status: HP -5 -> no damage
```

---

# 9. Damage/healing owner contract

Combat should not bypass BodyReducer.

Preferred owner contract:

```text
CombatReducer records combat event/outcome.
DamageResolver creates validated body.damage_candidate or actor.damage_candidate.
BodyReducer owns player HP, injuries, conditions, and death boundary.
ActorReducer owns actor presence/status memory; NPC body depth remains conservative unless NPC body system exists.
ProgressionReducer owns XP and must not auto-award combat XP.
InventoryReducer owns item consumption, not CombatReducer.
AbilityReducer owns ability evidence, not CombatReducer.
```

If a target owner system is not ready:

```text
record combat outcome and defer effect safely
```

Do not apply damage/healing by writing random fields.

---

# 10. DamageResolver behavior

DamageResolver should:

```text
read validated combat outcome
read target type
read amount/severity/body part if known
create DamageRecord
route player body effects to BodyReducer
route NPC damage conservatively to actor/status pending unless NPC body owner exists
apply no effect if miss/block/no-effect wins dispute
add operation key
mark dirty targets
```

DamageResolver should not:

```text
invent numeric damage from dramatic prose
kill target from damage unless death boundary validates
apply armor/equipment math unless rules active
apply ability damage formulas unless rules active
award XP/loot
```

---

# 11. HealingResolver behavior

HealingResolver should:

```text
read validated healing/recovery outcome
read target type
read resource/condition/injury target if known
create HealingRecord
route player body effects to BodyReducer
route NPC healing conservatively if owner exists
skip if failed/no effect/attempt only
add operation key
mark dirty targets
```

HealingResolver should not:

```text
heal from item name alone
consume item automatically unless InventoryReducer receives validated consume/use event
revive dead target unless revival policy active
clear all conditions broadly
heal above max
```

---

# 12. Dispute resolver behavior

CombatDisputeResolver should run before reducers commit combat consequences.

Priority rules:

```text
1. Command/context/card/debug source blocks effect.
2. Duplicate output blocks repeated effect.
3. Explicit miss/dodge/parry/no effect blocks damage.
4. Explicit block/armor blocks or defers damage unless damage clause says partial/graze.
5. Explicit hit permits hit outcome but not automatic numeric damage.
6. Explicit wound/injury/HP loss permits damage candidate.
7. Explicit healing success permits healing candidate.
8. Explicit healing failure blocks healing.
9. Death requires death-boundary validation.
10. Ambiguous target blocks damage/healing until resolved.
```

This makes combat robust against AI Dungeon contradictions.

---

# 13. Input/output dispute examples

## 13.1 Input claims damage, output says miss

Input:

```text
I slash the goblin across the chest.
```

Output:

```text
The goblin ducks under your blade.
```

Expected:

```text
attack attempt recorded
miss/dodge outcome recorded
no damage
```

## 13.2 Input attempts healing, output says no effect

Input:

```text
I drink the potion to heal my wounds.
```

Output:

```text
The potion is stale and has no effect.
```

Expected:

```text
item use/consume may be separately validated by InventoryReducer
no healing
failed healing outcome recorded
```

## 13.3 Output confirms wound

Output:

```text
The goblin's spear cuts your side, and blood spills down your armor.
```

Expected:

```text
damage/injury candidate accepted
BodyReducer records injury/bleeding if validated
```

---

# 14. Combat ProposalBuilder event types

Add or verify event types:

```text
combat.attack_attempt
combat.defense_result_candidate
combat.hit_candidate
combat.miss_candidate
combat.block_candidate
combat.graze_candidate
combat.damage_candidate
combat.healing_candidate
combat.healing_failed_candidate
combat.retreat_candidate
combat.capture_candidate
combat.victory_candidate
combat.defeat_candidate
combat.death_candidate
combat.dispute_candidate
body.damage_candidate
body.healing_candidate
body.injury_from_combat_candidate
body.condition_from_combat_candidate
```

Current chunk may commit safe:

```text
attack attempt
miss/block/hit/graze outcome
damage/healing records
player body damage/healing routed to BodyReducer
conservative actor/NPC status records
combat dispute records
combat scene active/resolved labels
```

---

# 15. EventScannerLite combat hints

EventScannerLite may detect combat hints, but detection is not canon.

Hint families:

```text
attack verbs: attack, slash, stab, shoot, strike, punch, kick, bite
hit verbs: hits, lands, connects, cuts, pierces, smashes
miss/block verbs: misses, dodges, blocks, parries, deflects, no effect
injury verbs: bleeds, breaks, burns, wounds, fractures, poisons
healing verbs: heals, restores, closes, stops bleeding, cures, stabilizes
combat outcome verbs: flees, surrenders, captures, defeats, dies
```

Must flag:

```text
inputIntentOnly
missed
blocked
noEffect
partialOrGraze
healingFailed
dreamOrVision
quotedSpeech
metaphor
threatOnly
trainingOnly
commandSource
contextSource
cardSource
auditSource
duplicateRisk
ambiguousTarget
ambiguousAmount
deathRisk
```

Parser cannot mutate combat/body state.

---

# 16. CombatReducerLite detailed behavior

CombatReducerLite owns:

```text
combat.active
combat.sceneState
combat.events
combat.recentAttempts
combat.recentOutcomes
combat.disputes
combat.warnings
```

## 16.1 Attack attempt commit

Accepted attack attempt should:

```text
create AttackAttemptRecord
create CombatEventRecord eventType attack_attempt
status intent_only or pending
not apply damage
mark combat/context dirty
audit compactly
```

## 16.2 Defense/miss/block commit

Accepted result should:

```text
update related attempt if found
create DefenseResultRecord
create CombatOutcomeRecord
block damage unless separate accepted damage exists
mark combat/context dirty
```

## 16.3 Hit commit

Accepted hit should:

```text
update related attempt if found
create hit outcome
not apply numeric damage unless damage candidate accepted
```

## 16.4 Damage/healing commit

Accepted damage/healing should:

```text
create DamageRecord or HealingRecord
route body effects through owner reducer contract
mark combat/body/context dirty
add operation key after successful owner-routing or safe defer
```

## 16.5 Dispute commit

Accepted dispute should:

```text
create CombatDisputeRecord
store resolvedResult
block/defer unsafe effects
mark combat/context dirty
```

---

# 17. CombatReducer defensive guards

CombatReducer must reject/no-op if routed result has unsafe markers:

```text
command source
context source
card source
audit/debug source
input-only damage
miss/block/no-effect damage
healing failed but healing applied
duplicate damage/healing/death
ambiguous target
death without death-boundary validation
manual damage/heal/kill command
wrong owner reducer
```

CombatReducer must not blindly trust upstream.

---

# 18. Operation keys

Required combat operation keys:

```text
combat.attempt.<attackerKey>.<defenderKey>.<method>.<inputHash>
combat.result.<attackerKey>.<defenderKey>.<resultType>.<clauseHash>
combat.hit.<attackerKey>.<defenderKey>.<clauseHash>
combat.damage.<targetKey>.<damageKey>.<clauseHash>
combat.healing.<targetKey>.<healingKey>.<clauseHash>
combat.dispute.<disputeType>.<clauseHash>
combat.retreat.<combatantKey>.<clauseHash>
combat.capture.<targetKey>.<clauseHash>
combat.victory.<winnerKey>.<loserKey>.<clauseHash>
combat.death.pending.<targetKey>.<clauseHash>
```

Body/owner linked keys:

```text
body.damage.<targetKey>.<damageKey>.<sourceCombatKey>
body.healing.<targetKey>.<healingKey>.<sourceCombatKey>
body.injury.combat.<injuryKey>.<sourceCombatKey>
body.deathboundary.<newStatus>.<sourceCombatKey>
```

Rules:

```text
damage/healing/death keys are durable
attempt keys can be recent unless needed for dispute binding
same output replay cannot repeat damage/healing/death
```

---

# 19. Dirty targets

Combat updates should mark:

```text
combat
body_status
social_memory
current_location
context
progression only if exact XP event separately accepted
inventory_equipment only if item use/consumption separately accepted
```

Mapping:

```text
attack attempt -> combat, context
hit/miss/block/graze -> combat, context
player damage/healing -> combat, body_status, context
actor damage/status -> combat, social_memory, context
retreat/capture/victory -> combat, social_memory, current_location, context
```

Dirty targets do not write cards directly.

---

# 20. `/status` and optional `/combat` updates

## 20.1 `/status`

Should show combat-relevant body state:

```text
current HP/MP/EP if known
active injuries
active combat conditions
death boundary
recent combat damage/healing if relevant
```

It must not show combat attempt as confirmed damage.

## 20.2 Optional `/combat`

If command budget permits, add:

```text
/combat
```

Should show:

```text
combat active/resolved status
recent attempts
recent confirmed outcomes
damage/healing records
blocked/deferred effects
disputes
warnings
```

If not, include combat summaries inside:

```text
/status
/debuglast
/stateaudit
```

All command output starts with `[AIDRPG]` and remains non-canon.

---

# 21. ContextBuilder combat packet

ContextBuilder should include concise combat facts.

Include:

```text
active combat/threat status
confirmed recent hit/miss/block only if relevant
active injuries/conditions through body packet
important enemy/actor status if known
```

Do not include:

```text
input attack attempts as confirmed hits
blocked/rejected damage as applied damage
full combat event ledger
formula debug text
exact enemy stats if unknown
pending death as confirmed death
```

Example:

```text
Combat: Your last sword swing was blocked; no damage was confirmed. You have a bleeding cut on your side from the goblin's spear.
```

---

# 22. IntegrityChecker combat checks

Add checks:

```text
damage/healing records have operation keys
applied damage has owner-routed body/actor effect or safe defer note
miss/block/no-effect outcomes do not have applied damage unless separate damage record exists
healing failure does not have applied healing
death pending/confirmed follows death boundary rules
duplicate damage/healing keys not repeated
combat events do not originate from command/card/context source
combat victory does not auto-create XP/loot
```

IntegrityChecker may warn/quarantine bad combat records. It may not apply damage, healing, loot, XP, or death.

---

# 23. Cleanup combat protections

Cleanup may compact:

```text
old low-value attack attempts
old duplicate combat warning spam
old resolved dispute summaries
old non-applied rejected outcomes
```

Cleanup must not delete:

```text
applied damage/healing records needed for audit
active injuries/conditions
death-boundary records
durable combat/body operation keys
important combat outcome history
unresolved disputes
```

Cleanup must not heal, damage, kill, revive, award XP, or create loot.

---

# 24. ReleaseCheck gates for this chunk

Add or verify PC12-specific gates:

```text
PC12-G01 CombatEventRecord schema exists or equivalent combat event structure exists
PC12-G02 AttackAttemptRecord schema exists or equivalent attempt structure exists
PC12-G03 DamageRecord schema exists or equivalent damage structure exists
PC12-G04 HealingRecord schema exists or equivalent healing structure exists
PC12-G05 CombatDisputeRecord schema exists or equivalent dispute structure exists
PC12-G06 CanonValidator separates attempt/hit/miss/block/graze/damage/healing/death
PC12-G07 input-only attacks do not apply damage
PC12-G08 miss/block/no-effect prevents damage unless separate accepted damage exists
PC12-G09 healing failure prevents healing
PC12-G10 CombatReducer rejects unsafe routed combat events
PC12-G11 damage/healing routes through BodyReducer/owner contract
PC12-G12 death remains protected by death-boundary validation
PC12-G13 duplicate damage/healing/death keys exist and are durable
PC12-G14 command/context/card combat text cannot become canon
PC12-G15 combat victory does not auto-award XP/loot
PC12-G16 equipment/ability/stat advanced combat formulas remain deferred unless owner rules active
PC12-G17 /status or /combat displays combat state honestly
PC12-G18 cleanup preserves active combat/body truth and durable keys
PC12-G19 advanced combat/party/warfare/absorption systems remain disabled
PC12-G20 player-breaker combat tests registered
```

ReleaseCheck must fail if input attacks apply damage directly, repeated output duplicates damage/healing, command/context/card combat text mutates state, or combat victory awards XP/loot automatically.

---

# 25. Required tests

## PC12-001 — Input attack attempt only

Input:

```text
I slash the goblin across the chest.
```

Expected:

```text
attack attempt recorded
no damage applied
```

## PC12-002 — Output miss blocks damage

Input:

```text
I stab the goblin.
```

Output:

```text
The goblin sidesteps, and your blade misses.
```

Expected:

```text
miss outcome recorded
no damage
```

## PC12-003 — Output block blocks damage

Output:

```text
The goblin blocks your sword with its shield.
```

Expected:

```text
block outcome recorded
no damage unless separate damage clause exists
```

## PC12-004 — Output graze creates minor damage only if validated

Output:

```text
Your blade grazes the goblin's arm, drawing a thin line of blood.
```

Expected:

```text
graze outcome
minor injury/damage candidate if target owner supports it
not full damage
```

## PC12-005 — Confirmed player injury

Output:

```text
The goblin's spear cuts your side, and blood spills down your armor.
```

Expected:

```text
damage/injury candidate accepted
BodyReducer records side cut/bleeding if validated
operation key added
```

## PC12-006 — Duplicate injury skipped

Repeat same output.

Expected:

```text
no duplicate injury/damage
operation key skip/audit
```

## PC12-007 — Numeric HP loss accepted if resource policy active

Output:

```text
The arrow hits you for 5 damage.
```

Expected:

```text
DamageRecord amount 5
HP decreases only if HP numeric tracking/resource policy active
otherwise damage/injury deferred or labeled
```

## PC12-008 — HP loss command non-canon

Command output or user command:

```text
[AIDRPG] Status: HP -5.
```

Expected:

```text
no HP/damage mutation
```

## PC12-009 — Healing attempt failed

Input:

```text
I drink the potion to heal.
```

Output:

```text
The potion is stale and has no effect.
```

Expected:

```text
healing failed outcome
no healing
InventoryReducer may handle item consumption only if separately validated
```

## PC12-010 — Healing success accepted

Output:

```text
The potion closes the cut on your side and restores 5 HP.
```

Expected:

```text
HealingRecord amount 5
BodyReducer heals/closes injury only according to body/resource policy
operation key added
```

## PC12-011 — Duplicate healing skipped

Repeat same healing output.

Expected:

```text
no second healing
operation key skip/audit
```

## PC12-012 — Healing from item name alone rejected

Output:

```text
You hold the red healing potion.
```

Expected:

```text
no healing
item possession/display only if inventory validates it
```

## PC12-013 — Victory no XP by itself

Output:

```text
You defeat the goblin.
```

Expected:

```text
victory/combat resolved possible
no XP
no loot
```

## PC12-014 — Victory plus exact XP only XP clause applies

Output:

```text
You defeat the goblin and gain 5 XP.
```

Expected:

```text
combat victory recorded
XP +5 only through ProgressionReducer exact XP path
no loot unless separate inventory event
```

## PC12-015 — Loot after combat not auto-owned

Output:

```text
The goblin falls, dropping a rusty dagger.
```

Expected:

```text
combat outcome possible
rusty dagger available loot scene item through InventoryReducer, not owned
```

## PC12-016 — Threat does not damage

Output:

```text
The bandit threatens to gut you.
```

Expected:

```text
no damage
possible threat/social memory only if actor system validates it
```

## PC12-017 — Dream combat no real damage

Output:

```text
In a dream, the dragon burns you to ash.
```

Expected:

```text
no real damage/death
```

## PC12-018 — Quote combat no real death

Output:

```text
Mira says, "The goblin is dead."
```

Expected:

```text
no confirmed goblin death from quote alone
```

## PC12-019 — Player death protected

Output:

```text
Your heart stops, and you die on the stone floor.
```

Expected:

```text
death boundary changes only according to BodyReducer death policy
if death automation safety-limited, death pending/deferred with audit
```

## PC12-020 — Dead tired not death

Output:

```text
After the fight, you feel dead tired.
```

Expected:

```text
fatigue maybe recorded by body system
no death boundary change
```

## PC12-021 — Armor mention no automatic formula

Output:

```text
Your iron armor turns the blade aside.
```

Expected:

```text
block/armor outcome may record
no armor stat formula unless equipment combat rule active
```

## PC12-022 — Ability damage formula deferred

Output:

```text
Ember Ward explodes and burns the wolf for massive damage.
```

Expected:

```text
ability/combat evidence may record if validated
damage amount not invented
ability damage formula deferred unless rule active
```

## PC12-023 — Ambiguous target blocks damage

Output:

```text
The blade strikes hard, and someone screams.
```

Expected:

```text
ambiguous target dispute
no damage applied until target bound
```

## PC12-024 — Command combat non-canon

Run:

```text
/status
```

or:

```text
/combat
```

Feed command output through Output hook.

Expected:

```text
no combat/damage/healing mutation
```

## PC12-025 — Story Card combat prose non-canon

Story Card text:

```text
The player has taken 10 damage from the goblin.
```

Expected:

```text
no damage mutation
quarantine/audit only if surfaced
```

## PC12-026 — Context combat prose non-canon

Context text:

```text
The goblin is dead and the player healed 5 HP.
```

Expected:

```text
no death/healing mutation from context text
```

## PC12-027 — Cleanup preserves combat/body truth

Setup:

```text
active injury
applied damage record
combat operation key
unresolved dispute
```

Run cleanup.

Expected:

```text
active injury/key/dispute preserved
only low-risk combat logs compacted
```

## PC12-028 — Manual combat commands disabled

Run:

```text
/damage goblin 10
/heal me 20
/kill goblin
```

Expected:

```text
disabled messages or safe unknown command
no combat/body mutation
```

---

# 26. Regression placement

Add PC12 tests to regression tiers:

```text
T2 Parser/Proposal/Validator Safety Tests
T4 Reducer Commit Tests
T6 Player-Breaker Adversarial Tests
T7 Long-Run Stability Tests
T8 Deferred System Disabled Tests
```

Minimum expectation:

```text
28 PC12 tests registered
0 critical PC12 failures
```

---

# 27. State fields added or deepened

Expected additions/deepening:

```text
combat.active
combat.sceneState
combat.events
combat.recentAttempts
combat.recentOutcomes
combat.disputes
CombatSceneState shape
CombatantSnapshot shape
CombatEventRecord shape
AttackAttemptRecord shape
DefenseResultRecord shape
DamageRecord shape
HealingRecord shape
CombatDisputeRecord shape
CombatOutcomeRecord shape
```

---

# 28. Parser/proposal/validator changes

Allowed:

```text
combat attempt/hit/miss/block/graze/damage/healing detection
input vs output dispute flags
damage/healing target binding
nonliteral combat rejection
duplicate key suggestions
```

Forbidden:

```text
advanced combat formulas
full turn order/initiative engine
manual damage/heal parser
world threat scaling parser
party/warfare parser
card/context combat import
```

---

# 29. Reducer/checker changes

Allowed:

```text
CombatReducer attack/result/damage/healing/dispute records
DamageResolver / HealingResolver shells
BodyReducer owner-routed damage/healing integration
combat duplicate skip results
combat integrity checks
```

Forbidden:

```text
auto XP/loot rewards
auto death bypass
combat stat generation
equipment/ability formula activation without owner rules
party/warfare automation
```

---

# 30. Command/context/card changes

Allowed:

```text
/status combat-relevant display
/combat optional display
/context combat packet
/cardaudit combat mirror labels if dry-run
/releasecheck PC12 gates
/playerbreakercheck combat cases
```

Forbidden:

```text
/damage
/heal
/kill
/spawnfight
/awardcombatxp
live card writes that create combat truth
```

---

# 31. Beginner implementation order

When implementing this chunk in code, follow this order.

## Step 1 — Copy accepted stats/invariant build

Start from:

```text
WorldLedgerV0.17.11_STATS_INVARIANTS_ACCEPTED.txt
```

Create working file:

```text
WorldLedgerV0.17.12_COMBAT_EVENTS_WORKING.txt
```

## Step 2 — Add/verify combat helper functions

Create helpers for:

```text
ensureCombatShape
makeCombatSceneState
makeCombatantSnapshot
makeCombatEventRecord
makeAttackAttemptRecord
makeDefenseResultRecord
makeDamageRecord
makeHealingRecord
makeCombatDisputeRecord
makeCombatOutcomeRecord
isUnsafeCombatSource
isInputIntentOnly
isDamageBlockedByResult
```

## Step 3 — Add CombatDisputeResolver

Implement the priority rules before reducers apply consequences.

## Step 4 — Update combat CanonValidator rules

Add strict accept/reject/pending logic for:

```text
attack attempt
hit/miss/block/graze
damage
healing
healing failure
victory/defeat
capture/retreat
death pending/confirmed
```

## Step 5 — Update ProposalBuilder combat support

Ensure proposals carry:

```text
attacker
defender
method
result type
damage/healing data
target body part if known
source/localClause/safety flags
operation key suggestion
```

## Step 6 — Update CombatReducerLite

Implement safe commits for:

```text
attack attempt
miss/block/hit/graze outcome
damage/healing record
dispute record
combat scene state
```

## Step 7 — Wire damage/healing to owner reducers

Route player body effects to BodyReducer. Defer unsafe NPC body effects unless owner exists.

## Step 8 — Update `/status` and optional `/combat`

Make combat state display honest.

## Step 9 — Update ContextBuilder combat packet

Keep it concise and state-accurate.

## Step 10 — Update IntegrityChecker combat checks

Add applied damage/healing, duplicate, dispute, and unsafe-source checks.

## Step 11 — Add PC12 ReleaseCheck gates

Add PC12-G01 through PC12-G20.

## Step 12 — Add PC12 tests

Add PC12-001 through PC12-028.

## Step 13 — Run baseline checks

Run:

```text
/foundationcheck
/releasecheck
/integrity
/playerbreakercheck
/regressioncheck
/status
/combat if available
```

## Step 14 — Save accepted combat-event build

Recommended output:

```text
WorldLedgerV0.17.12_COMBAT_EVENTS_ACCEPTED.txt
```

---

# 32. What this chunk produces

Production Chunk 12 should produce:

```text
WorldLedger V0.17.12 combat-event accepted script if code changes are made
combat helper depth
CombatEvent/Attack/Damage/Healing/Dispute schemas
CombatDisputeResolver
DamageResolver and HealingResolver shells
CombatReducerLite event/outcome behavior
BodyReducer owner-routed damage/healing integration
/status and optional /combat display improvements
combat context packet
combat integrity checks
PC12 ReleaseCheck gates
PC12 regression/player-breaker tests
combat acceptance report
updated planning zip with Production Chunk 12 added
```

It should not produce:

```text
full advanced combat engine
full creature ecology combat logic
party/summon combat
warfare/group combat
absorption/evolution on kill
manual damage/heal/kill commands
automatic combat XP/loot
equipment/ability damage formulas without owner rules
live CardSync writes
old card prose combat import
```

---

# 33. Pass / warn / fail criteria

## PASS

Production Chunk 12 passes if:

```text
attack attempts and confirmed outcomes are separated
input-only attacks do not apply damage
miss/block/no-effect prevents damage unless separate damage validates
healing failure prevents healing
damage/healing routes through owner reducer contract
death remains protected by death-boundary rules
duplicate damage/healing/death keys work
combat victory does not auto-award XP/loot
/status or /combat displays honestly
command/context/card combat text cannot become canon
cleanup preserves active combat/body truth
PC12 tests pass
ReleaseCheck has no PC12 FAIL/BLOCKED gates
```

## WARN

Warn but allow progress if:

```text
advanced combat formulas are deferred
equipment/ability combat math is deferred
NPC body depth is conservative
initiative/turn order is shell-only or absent
creature-specific combat logic is deferred
party/warfare/absorption systems are disabled
```

## FAIL

Fail and stop if:

```text
input attack directly damages target
miss/block still applies damage
healing failure still heals
damage/healing duplicates on replay
command/context/card text damages/heals/kills
combat victory grants XP or loot automatically
BodyReducer death boundary is bypassed
CombatReducer writes random body fields directly
manual damage/heal/kill commands mutate state
cleanup deletes active injury/combat keys
CombatReducer trusts unsafe routed data
```

---

# 34. Handoff to Production Chunk 13

When this chunk passes, Production Chunk 13 begins:

```text
Production Chunk 13 — Body Form, Race Logic, Transformation Consequence, and Non-Humanoid Constraints
```

Production Chunk 13 may now use combat/body evidence to reason about form constraints, non-humanoid bodies, and transformation consequences, but should not use combat as proof of race/form powers without validation.

Production Chunk 12 must leave combat reliable enough that future chunks can ask:

```text
Was there only an attempt?
Was there a miss, block, graze, or hit?
Was damage actually validated?
Was healing actually validated?
Was death only pending or confirmed?
Was this effect already applied?
Which owner reducer owns the resulting body/actor/progression/inventory change?
```

---

# 35. Final doctrine

Production Chunk 12 is where combat stops being outcome hallucination.

Final rule:

```text
Combat is an event pipeline, not a shortcut.
Attempts, outcomes, damage, healing, death, loot, and XP are separate.
Every consequence must be validated, owner-routed, duplicate-safe, and honest in display.
```

Once this passes, the project can safely deepen race/form/transformation consequences in Production Chunk 13.
